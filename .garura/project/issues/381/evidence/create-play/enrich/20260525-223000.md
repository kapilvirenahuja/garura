# create-play --rebuild enrich — evidence (#381)

## Mode
Rebuild of an existing migrated ICE play. Intent change routed correctly through
intent.yaml → /create-play (per #381 and project play-pipeline rules).

## Intent delta
- New constraint C11 (archive-gate guard): an issue is archived only when every
  approved proposal has a confirmed successful write to product LTM (incl. any
  approved Tier 1 ADR promotion); if any approved write reaches a terminal
  non-success state, the issue is not archived and the failure is surfaced.
- Sharpened F7: "applied" → "lacked a confirmed successful write to product LTM
  and was not explicitly rejected" (removes the ambiguity that allowed the bug).
- version 1.0.0 → 1.1.0. User-approved via Tether at the checkpoint.

## Expectation delta
- S1-S6 preserved verbatim; new S7 covers an approved write that fails to land
  (issue stays active, failure surfaced).
- Recovery: one per failure condition (F1-F9). REC7 trigger + direction re-worded
  to mirror sharpened F7. vetted.status approved (user Tether).

## Compile
- New hashes: intent ae780397…, expectation 30bcf257…. Hash guard + Compilation
  Metadata updated.
- Step 5 archive gate rewritten from the narrow verification_failed-only condition
  to the C11 positive check (archive only if every approved entry rejected or has
  status: applied; blocks on pending/failed/verification_failed; surfaces failure).
- Evals: 16 step (SE-1..SE-16; new SE-16 for C11; SE-7 re-sited to archive-time for
  sharpened F7), 7 scenario (SCE-1..SCE-7; new SCE-7).
- Step 3 reviewer-completeness check relabelled a C4 gate (F7's eval moved to Step 5).

## Verification
- Coverage matrix: every artifact-verifiable constraint ≥1 SE; every F ≥1 SE; every
  F exactly one recovery; every S ≥1 SCE. PASS.
- lint-components: enrich play 0 violations. (65 pre-existing tree-wide errors are
  out of scope.)
- REC7 consistent across expectation.yaml and SKILL.md. Gate prose names all three
  blocking states.

## Gate results
- G1 constraint coverage PASS; G2 FC coverage PASS; G3 scenario coverage PASS;
  G3b recovery coverage PASS (one per FC); G9 hash drift resolved (rebuilt);
  G10 required sections present; G12 Standard Play Close anchors intact.

## Pre-existing observations (not addressed — orthogonal to #381)
- repo-orchestrator skill table omits archive-issue-stm (play assigns it).
- apply-ltm-enrichment writes[].status enum omits "failed" though it produces it.

## No skill or agent changes required.
