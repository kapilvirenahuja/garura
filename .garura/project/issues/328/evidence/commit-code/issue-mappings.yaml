mappings:
  - group_id: G1
    issue: 328
    confidence: high
    reasoning: >
      Three corroborating signals. Branch name fix/328-lint-sce-range-expansion
      directly encodes issue 328. The group scope (lint-components) and commit
      type (fix) match the issue title verbatim: "fix(lint-components): expand
      SCE-N range expressions in scenario-coverage check". The changed file
      core/tools/lint-components/lib/rules/cross-reference.js is the exact
      component named in the issue body as the one containing the substring-match
      bug being fixed. Bug label on issue aligns with fix type on group.
    conflicts: []

  - group_id: G2
    issue: 328
    confidence: high
    reasoning: >
      Two corroborating signals. Branch name fix/328-lint-sce-range-expansion
      directly encodes issue 328 (branch signal). The group's files are STM
      evidence artifacts scoped to issues/328/, making them operationally bound
      to this issue by path (proximity signal). Chore type is consistent with
      bookkeeping commits on a feature branch. No competing issue candidates
      exist for STM artifacts scoped to this issue number.
    conflicts: []
