analysis:
  branch: fix/370-define-surfaced-for-review-hitl-break
  base: main
  issue_number: 370
  analyzed_at: "2026-05-16T06:40:00Z"

  suggested_title: "fix(define): add C17 Decision Surfacing Discipline + F11, recompile SKILL.md with Step 5.0/9.0 (#370)"

  context:
    branch_classification: bugfix
    branch_overrides_applied: none  # bugfix uses standard priorities
    file_patterns_matched:
      - "core/components/plays/**/*.md  (play definition — SKILL.md changed)"
      - "core/components/plays/**/reference/*.yaml  (play intent/config — intent.yaml changed)"
      - "core/components/**  (sync-claude trigger)"
      - ".garura/project/issues/370/evidence/**  (STM evidence — chore(stm) commit)"
    commit_types:
      - "fix  (3e51291 — source fix commit)"
      - "chore  (1635132 — STM evidence commit)"
    source_files_changed: 2
    total_files_in_diff: 33
    stm_evidence_files: 31  # all other files are STM artifacts

  checklist:
    must_have:
      - id: no-conflicts
        item: "No merge conflicts between fix/370-define-surfaced-for-review-hitl-break and main"
        trigger: "Universal rule — applies to every PR"
        status: PASS
        evidence: >
          git merge-tree --write-tree main fix/370-define-surfaced-for-review-hitl-break
          returned a single clean tree SHA (6ab46dc2ddfce5599a200107e1e808a80669b07a) with
          no conflict markers. Zero conflict hunks.

      - id: no-secrets
        item: "No secrets or credentials committed in changed files"
        trigger: "Universal rule — applies to every PR"
        status: PASS
        evidence: >
          Pattern scan (password|secret|token|api_key|private_key|credential) against
          the diff of both source files (intent.yaml, SKILL.md) returned zero matches.
          STM evidence files contain only YAML analysis artifacts — no credentials.

      - id: play-frontmatter
        item: "SKILL.md retains valid frontmatter (name, description, user-invocable, allowed-tools)"
        trigger: "core/components/plays/define/SKILL.md changed — play-frontmatter rule triggered"
        status: PASS
        evidence: >
          SKILL.md lines 2–6: name=garura:define, description present (feature scoping
          play), user-invocable=true, allowed-tools=Task,Read,Write,Grep,Glob,Skill,
          SendMessage,Bash. Frontmatter intact; the fix only added content inside the
          Workflow section and updated Compilation Metadata.

      - id: play-workflow
        item: "SKILL.md defines a complete workflow with all phases intact"
        trigger: "core/components/plays/define/SKILL.md changed — play-workflow rule triggered"
        status: PASS
        evidence: >
          ## Workflow section confirmed present at line 100. All 11 phases (Phase 0–10)
          present. New Step 5.0 and Step 9.0 insert before existing steps 5a/5b and 9a/9b
          respectively — no existing phases removed or reordered.

      - id: play-checkpoint
        item: "SKILL.md defines checkpoint behavior at Phase 5 and Phase 9 gates"
        trigger: "core/components/plays/define/SKILL.md changed — play-checkpoint rule triggered"
        status: PASS
        evidence: >
          Tether/Orbit/Vanish gates confirmed at Phase 5 (placement checkpoint) and
          Phase 9 (epic checkpoint). New Step 5.0 and Step 9.0 each explicitly state
          they run BEFORE the checkpoint file or the Tether/Orbit/Vanish prompt —
          checkpoint behavior is augmented, not replaced.

      - id: config-valid
        item: "intent.yaml is valid YAML after adding C17 and F11"
        trigger: "core/components/plays/define/reference/intent.yaml changed — config-valid rule triggered"
        status: PASS
        evidence: >
          python3 yaml.safe_load() against intent.yaml returned no exception.
          File parses cleanly. C17 block (31 lines) and F11 block (10 lines) are
          well-formed YAML with correct indentation under the constraints and
          failure_conditions arrays.

      - id: sync-claude
        item: "core/components/ change — /sync-claude must be run before PR merge (or as part of merge process)"
        trigger: "core/components/plays/define/ changed — sync-claude rule triggered (When: Sync Required)"
        status: REVIEW
        evidence: >
          Both changed source files are under core/components/plays/define/. The CLAUDE.md
          states: 'After editing source, run /sync-claude to deploy globally.' This is
          a pre-merge or post-merge gate, not a PR-blocking item — the PR captures the
          source change; deployment is a separate step per project convention.

      - id: play-pipeline-rule
        item: "Intent change correctly routed through intent.yaml → /create-play --rebuild define"
        trigger: >
          C17 + F11 are new constraints and failure conditions — this is an intent change
          per the Play Pipeline Rule in CLAUDE.md, which mandates the rebuild path.
        status: PASS
        evidence: >
          Compilation Metadata in SKILL.md records compiled_by=/create-play --rebuild define
          and compiled_at=2026-05-16T06:26:53Z. intent_hash updated to
          sha256:a1398e860f0c6568269734b1f235dd8f9bd4623456d1b65d8514037c6b0f3e5d.
          The fix commit message references the rebuild: 'recompile with Step 5.0/9.0 + SE-13'.
          Direct hand-edit path was NOT used.

      - id: regression-test-verdict
        item: "Independent 9-assertion regression test passes (A1–A9)"
        trigger: >
          Play intent change with recompiled SKILL.md — regression coverage verifies
          both the source-of-truth (intent.yaml) and the compiled artifact (SKILL.md)
          reflect the fix correctly.
        status: PASS
        evidence: >
          regression-test-verdict.yaml at .garura/project/issues/370/evidence/fix-it/
          records regression_test_verdict=pass, verified_at=2026-05-16T06:29:43Z.
          All 9 assertions pass: A1-A4 verify intent.yaml gained C17+F11; A5-A6 verify
          SKILL.md contains Decision Surfacing language; A7 verifies catalog-match
          manifest referenced in surfacing context (count ≥ 2); A8-A9 verify both
          Phase 5 AND Phase 9 each carry a Decision Surfacing step (count ≥ 2).
          All assertions were RED-verified on the unfixed codebase before the fix.

      - id: lint-components-pass
        item: "lint-components reports 0 errors, 0 warnings, 0 blockers on core/components/plays"
        trigger: >
          SKILL.md changed — lint-components enforces Standard Play Close anchor pair
          and play structural rules. Any SKILL.md mutation must pass this gate.
        status: PASS
        evidence: >
          lint-report.yaml at .garura/project/issues/370/evidence/lint-components/
          records errors=0, warnings=0, infos=0, blockers=0, informational=0, pass=true,
          violations=[]. Standard Play Close anchor pair (## Evidence & Close confirmed
          at SKILL.md line 661) is present and intact.

      - id: se13-both-phases
        item: "SE-13 step-eval is present at BOTH Phase 5 and Phase 9 (not just one)"
        trigger: >
          C17 mandates decision surfacing at BOTH checkpoint gates. A step-eval must
          enforce each gate independently. A single SE-13 entry would leave one gate
          unverified.
        status: PASS
        evidence: >
          grep -c 'SE-13' SKILL.md returned 4 (2 in Step Evals prose + 2 in the
          Compilation Metadata coverage fields). The eval appears in: (1) Phase 5 Step
          Evals block covering F11/C17 Phase 5 gate, (2) Phase 9 Step Evals block
          covering F11/C17 Phase 9 gate. Both instances have distinct pass/fail criteria
          tied to their respective manifest sets.

      - id: c17-f11-alignment
        item: "C17 constraint and F11 failure condition are mutually consistent — C17 specifies the rule, F11 specifies the structural violation"
        trigger: >
          intent.yaml changes added both C17 and F11. In Garura intent schema, every
          failure condition must have a backing constraint. C17/F11 must be checked for
          semantic alignment, not just presence.
        status: PASS
        evidence: >
          C17 (lines 251–281 in intent.yaml) specifies the positive obligation: surface
          every surfaced_for_review: true entry, batched by confidence tier, before each
          gate, and write user_response back. F11 (lines 353–362) specifies the structural
          violation: any surfaced_for_review: true entry carried forward past Phase 5 or
          Phase 9 without user_response populated. The two are semantically complementary —
          C17 is the rule, F11 is the failure mode when the rule is violated. No gaps.

    nice_to_have:
      - id: sibling-play-alignment
        item: "define's new C17 uses same constraint ID as specify's C17 (Decision Surfacing)"
        trigger: >
          spec (C18), design (C17+C18), arch (C18), and now define (C17) all carry
          Decision Surfacing. Consistent constraint IDs across sibling plays are a
          quality signal, not a hard requirement.
        status: PASS
        evidence: >
          grep confirm: specify/reference/intent.yaml has id: C17 (and C18).
          define/reference/intent.yaml's new constraint uses id: C17, consistent with
          specify. All three sibling plays (specify, design, arch) verified to contain
          'Decision Surfacing' in intent.yaml. define now aligns with the established
          pattern.

      - id: step-90-three-manifests
        item: "Step 9.0 references all three decision manifests (catalog-match, manage-features, generate-intent-epics)"
        trigger: >
          C17 specifies Phase 9 loads all three manifests. Step 9.0 in SKILL.md must
          reference all three by path to be verifiable.
        status: PASS
        evidence: >
          SKILL.md contains 9 total references to decision manifest paths across the
          surfacing steps and SE-13 evals. Step 9.0 bash block explicitly lists all
          three manifests: decision-manifest-catalog-match.yaml (Phase 2),
          decision-manifest-manage-features.yaml (Phase 6), and
          decision-manifest-generate-intent-epics.yaml (Phase 7). Phase 9 SE-13 eval
          references all three by name in its pass/fail criteria.

  blocking_issues: []

  ready: true

  target_branch: main
  target_branch_confidence: high

  confidence_assessment:
    overall: high
    rationale: >
      All must-have checklist items PASS or are non-blocking REVIEW. Zero blocking
      issues. Regression test 9/9 pass. lint-components 0 errors. Play pipeline rule
      correctly followed (intent change → rebuild path). No merge conflicts. No secrets.
      The only REVIEW item (sync-claude) is a post-merge deployment step per project
      convention — it is not a PR gate. Target branch is main, confidence high (standard
      fix→main flow, no special routing needed).
    decisions_with_less_than_high_confidence: []

  eval_results:
    source: ".garura/project/issues/370/evidence/fix-it/regression-test-verdict.yaml"
    verdict: pass
    assertions_passed: 9
    assertions_total: 9
    verified_at: "2026-05-16T06:29:43Z"
    summary: >
      9-assertion regression test — all pass. A1–A4: intent.yaml source-of-truth
      verified (C17, F11, Decision Surfacing token, surfaced_for_review token).
      A5–A9: SKILL.md compiled artifact verified (Decision Surfacing language present,
      surfaced_for_review present, catalog-match manifest referenced ≥2 times,
      Decision Surfacing count ≥2 confirming both Phase 5 and Phase 9 gates covered).
