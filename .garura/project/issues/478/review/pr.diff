diff --git a/.garura/install-manifest.json b/.garura/install-manifest.json
index b7a28124..bea0fb36 100644
--- a/.garura/install-manifest.json
+++ b/.garura/install-manifest.json
@@ -5,102 +5,38 @@
     "tool": "claude",
     "components": [
       ".claude/agents/change-reviewer.md",
-      ".claude/agents/code-builder.md",
-      ".claude/agents/env-operator.md",
-      ".claude/agents/evals-engineer.md",
-      ".claude/agents/intent-resolver.md",
-      ".claude/agents/product-os-keeper.md",
       ".claude/agents/project-orchestrator.md",
       ".claude/agents/quality-auditor.md",
       ".claude/agents/repo-orchestrator.md",
-      ".claude/agents/tech-designer.md",
-      ".claude/agents/test-engineer.md",
       ".claude/skills/analyze-changes",
       ".claude/skills/analyze-pr",
-      ".claude/skills/author-agentic-lens",
-      ".claude/skills/author-architecture-lens",
-      ".claude/skills/author-build-plan",
-      ".claude/skills/author-epics",
-      ".claude/skills/author-hitl-scenarios",
-      ".claude/skills/author-intent-yaml",
-      ".claude/skills/author-learnings",
-      ".claude/skills/author-marketing-lens",
-      ".claude/skills/author-measure-lens",
-      ".claude/skills/author-quality-lens",
-      ".claude/skills/author-regression-test",
-      ".claude/skills/author-roadmap",
-      ".claude/skills/author-run-lens",
-      ".claude/skills/author-shape-bundle",
-      ".claude/skills/author-steelman-evals",
-      ".claude/skills/author-ux-lens",
-      ".claude/skills/author-vision-seed",
-      ".claude/skills/check-cut-tensions",
       ".claude/skills/create-commit",
-      ".claude/skills/deploy-to-cloud-env",
-      ".claude/skills/draft-fix-design",
-      ".claude/skills/draft-implementation-plan",
-      ".claude/skills/draft-lld",
-      ".claude/skills/draft-rca",
-      ".claude/skills/draft-technical-approach",
-      ".claude/skills/enrich-capability-ice",
-      ".claude/skills/judge-validation-results",
-      ".claude/skills/kb-search",
       ".claude/skills/manage-issue",
       ".claude/skills/merge-pr",
-      ".claude/skills/plan-validation-checks",
       ".claude/skills/platform-adapter",
-      ".claude/skills/propose-kb-node",
-      ".claude/skills/quality-check",
       ".claude/skills/quality-check-scoped",
-      ".claude/skills/rank-recommendations",
-      ".claude/skills/refine-quality-profile",
-      ".claude/skills/research-domain-context",
       ".claude/skills/resolve-issues",
-      ".claude/skills/run-quality-gates",
-      ".claude/skills/search-kb",
       ".claude/skills/setup-branch",
-      ".claude/skills/stand-up-launch-env",
       ".claude/skills/submit-pr",
-      ".claude/skills/agentic",
-      ".claude/skills/arch",
       ".claude/skills/commit-change",
-      ".claude/skills/deploy",
-      ".claude/skills/fix-bug",
-      ".claude/skills/grill",
-      ".claude/skills/implement",
-      ".claude/skills/install-garura",
-      ".claude/skills/launch",
-      ".claude/skills/learn",
-      ".claude/skills/marketing",
-      ".claude/skills/measure",
       ".claude/skills/merge-change",
-      ".claude/skills/next",
       ".claude/skills/play-creator",
       ".claude/skills/play-editor",
       ".claude/skills/propose-change",
-      ".claude/skills/quality",
-      ".claude/skills/refactor",
       ".claude/skills/review-change",
-      ".claude/skills/roadmap",
-      ".claude/skills/run",
-      ".claude/skills/shape",
-      ".claude/skills/start-change",
-      ".claude/skills/understand",
-      ".claude/skills/uninstall-garura",
-      ".claude/skills/ux",
-      ".claude/skills/validate",
-      ".claude/skills/vision"
+      ".claude/skills/start-change"
     ],
     "counts": {
-      "agents": 11,
-      "skills": 46,
-      "plays": 29
+      "agents": 4,
+      "skills": 10,
+      "plays": 7
     },
     "instruction_surface": "CLAUDE.md.new",
     "config": ".garura/core/config.yaml",
     "memory": "/Users/kapilahuja/.garura/core/memory",
     "global_files": [],
     "status_gitignore": ".garura/.gitignore",
-    "retired": []
+    "retired": [],
+    "trimmed": "2026-07-04: scoped to harness set (meta plays + change chain + their workers) on user direction"
   }
-}
+}
\ No newline at end of file
diff --git a/.garura/project/issues/478/context/analysis.yaml b/.garura/project/issues/478/context/analysis.yaml
new file mode 100644
index 00000000..22998421
--- /dev/null
+++ b/.garura/project/issues/478/context/analysis.yaml
@@ -0,0 +1,45 @@
+---
+# analyze_changeset.py output (C8 fast path)
+# issue: 478
+needs_judgment: false
+
+change_groups:
+  - id: install-scope-option
+    issue: 478
+    commit_type: feat
+    scope: install-garura
+    subject: "add --scope option to filter deployed component subset"
+    files:
+      - core/components/plays/install-garura/scripts/install.py
+      - core/components/plays/install-garura/scripts/adapters/claude.py
+      - core/components/plays/install-garura/scripts/adapters/codex.py
+      - core/components/plays/install-garura/SKILL.md
+
+  - id: trim-install-manifest
+    issue: 478
+    commit_type: chore
+    scope: install-garura
+    subject: "trim garura's own install manifest to the harness component set"
+    files:
+      - .garura/install-manifest.json
+
+exclusions:
+  - path: ".garura/project/issues/478/context/analysis.yaml"
+    reason: "STM evidence artifact for this play run, not part of the code change"
+    blocking: false
+  - path: ".garura/project/issues/478/context/porcelain.txt"
+    reason: "STM scan artifact for this play run, not part of the code change"
+    blocking: false
+
+risks:
+  sensitive_files:
+    []
+  breaking_changes:
+    []
+  ambiguous_types:
+    []
+  hotfix_branch: false
+
+eval_gates:
+  SE-1: {status: PASS, evidence: "Changeset grouped into 2 concerns for issue #478: (1) feat — install.py + both adapters + SKILL.md implementing and documenting the new --scope filter, (2) chore — .garura/install-manifest.json trimmed from 86 to 21 entries as the motivating cleanup. Two untracked STM artifacts (analysis.yaml, porcelain.txt) excluded as non-code evidence."}
+  SE-5: {status: PASS, evidence: "no sensitive patterns matched"}
diff --git a/.garura/project/issues/478/context/branch.json b/.garura/project/issues/478/context/branch.json
new file mode 100644
index 00000000..8abe1139
--- /dev/null
+++ b/.garura/project/issues/478/context/branch.json
@@ -0,0 +1,6 @@
+{
+  "branch_name": "feature/478-install-scope",
+  "base_sha": "bd1ed1a8352c3f4dccdbcbfc6ef5c6bd63023c66",
+  "on_default_branch": false,
+  "worktree_path": null
+}
diff --git a/.garura/project/issues/478/context/commits.yaml b/.garura/project/issues/478/context/commits.yaml
new file mode 100644
index 00000000..be32fcd8
--- /dev/null
+++ b/.garura/project/issues/478/context/commits.yaml
@@ -0,0 +1,23 @@
+commits:
+  - sha: 63f019e
+    message: "feat(install-garura): add --scope option to filter deployed component subset (#478)"
+    files:
+      - core/components/plays/install-garura/scripts/install.py
+      - core/components/plays/install-garura/scripts/adapters/claude.py
+      - core/components/plays/install-garura/scripts/adapters/codex.py
+      - core/components/plays/install-garura/SKILL.md
+    branch: feature/478-install-scope
+  - sha: 8cea2e4
+    message: "chore(install-garura): trim garura's own install manifest to the harness component set (#478)"
+    files:
+      - .garura/install-manifest.json
+    branch: feature/478-install-scope
+excluded_files:
+  - path: ".garura/project/issues/478/context/analysis.yaml"
+    reason: "STM evidence artifact for this play run, not part of the code change"
+  - path: ".garura/project/issues/478/context/porcelain.txt"
+    reason: "STM scan artifact for this play run, not part of the code change"
+leftover_count: 0
+tree_clean: true
+pushed: false
+run_state_committed: true
diff --git a/.garura/project/issues/478/context/issue.json b/.garura/project/issues/478/context/issue.json
new file mode 100644
index 00000000..06183e73
--- /dev/null
+++ b/.garura/project/issues/478/context/issue.json
@@ -0,0 +1,6 @@
+{
+  "resolved": false,
+  "issue_number": 478,
+  "title": "[ENH] install-garura: add a component-scope option so targets can receive a subset",
+  "created": true
+}
diff --git a/.garura/project/issues/478/context/porcelain.txt b/.garura/project/issues/478/context/porcelain.txt
new file mode 100644
index 00000000..1a9f0696
--- /dev/null
+++ b/.garura/project/issues/478/context/porcelain.txt
@@ -0,0 +1,6 @@
+ M .garura/install-manifest.json
+ M core/components/plays/install-garura/SKILL.md
+ M core/components/plays/install-garura/scripts/adapters/claude.py
+ M core/components/plays/install-garura/scripts/adapters/codex.py
+ M core/components/plays/install-garura/scripts/install.py
+?? .garura/project/issues/478/context/porcelain.txt
diff --git a/.garura/project/issues/478/context/pr.json b/.garura/project/issues/478/context/pr.json
new file mode 100644
index 00000000..016a25c6
--- /dev/null
+++ b/.garura/project/issues/478/context/pr.json
@@ -0,0 +1,14 @@
+{
+  "success": true,
+  "pr": {
+    "number": 479,
+    "url": "https://github.com/kapilvirenahuja/garura/pull/479",
+    "state": "open",
+    "title": "feat(install-garura): add --scope option to filter deployed component subset (#478)"
+  },
+  "checklist": {
+    "required_count": 4,
+    "optional_count": 5
+  },
+  "base": "main"
+}
\ No newline at end of file
diff --git a/.garura/project/issues/478/context/resolved-rules.json b/.garura/project/issues/478/context/resolved-rules.json
new file mode 100644
index 00000000..70b8f431
--- /dev/null
+++ b/.garura/project/issues/478/context/resolved-rules.json
@@ -0,0 +1,6 @@
+{
+  "resolved": true,
+  "path": "/Users/kapilahuja/.garura/core/memory/standards/rules/self-review.md",
+  "source": "/Users/kapilahuja/.garura/core/memory/standards/rules",
+  "is_override": false
+}
diff --git a/.garura/project/issues/478/context/self-review.md b/.garura/project/issues/478/context/self-review.md
new file mode 100644
index 00000000..ab94ca70
--- /dev/null
+++ b/.garura/project/issues/478/context/self-review.md
@@ -0,0 +1,34 @@
+# Self-Review — Issue #478
+
+**Branch:** feature/478-install-scope → main
+**Rules source:** /Users/kapilahuja/.garura/core/memory/standards/rules/self-review.md (base, no project override)
+**Commits:** ef16072, 63f019e, 8cea2e4, 98a6f93
+
+This is an informational checklist only. It does not gate the merge — `review-change` owns the approve/reject decision.
+
+## Scope checks
+
+| Check | Status | Evidence |
+|---|---|---|
+| Matches the issue | PASS | All changed files serve #478's stated goal: giving `install-garura` a `--scope` option, plus trimming garura's own manifest to the harness set that option enables. |
+| No scope creep | PASS | 11 changed files: 4 code files (`install.py`, both adapters, `SKILL.md`), the manifest trim, and 6 STM context files (`analysis.yaml`, `branch.json`, `commits.yaml`, `issue.json`, `porcelain.txt`, `work-description.txt`) that are expected pipeline evidence for start-change/commit-change, not unrelated edits. |
+| Reasonable size | PASS | 11 files, +173/-80 lines net (manifest trim accounts for most of the deletions). Reviewable in one sitting. |
+| No stray artifacts | PASS | No commented-out code, debug prints, or scratch files. The STM files are intentional workflow evidence, not accidental output. |
+
+## Quality checks
+
+| Check | Status | Evidence |
+|---|---|---|
+| Tests present | REVIEW | No test files touched or added. install-garura has no existing test suite in the repo (`find ... -iname "*test*"` returned nothing) — the change extends an untested script family in place, consistent with prior practice, but no automated coverage confirms `--scope harness` filtering works end-to-end. |
+| Commits are clean | PASS | 4 commits, each conventional-format (`feat(install-garura): ...`, `chore(install-garura): ...`, `chore(stm): ...`), each referencing `#478`, each a coherent single concern (feature, manifest trim, two STM evidence commits). |
+| No secrets | PASS | Grepped the diff for password/secret/token/api-key/private-key patterns — none found. |
+| Docs in step | PASS | `SKILL.md` updated: `--scope` documented in the Options list, membership-list maintenance note added, and a `**Direct-edit deviation note (#478):**` footer recorded per this repo's play-pipeline rule (non-intent, direct-edit path — no ICE source exists for this bootstrap meta-play). |
+| Nothing obviously broken | PASS | `git merge-tree --write-tree main HEAD` produced a clean tree hash with no conflict markers. No TODOs or known-failing paths introduced in the diff. |
+
+## Blocking issues
+
+None found. One REVIEW item (missing automated test coverage for `--scope` filtering) is flagged for the human reviewer's judgment at `review-change`.
+
+## Overall readiness
+
+Ready to raise. 8 of 9 checks PASS; 1 REVIEW (no tests) that the reviewer should weigh — the install scripts have no pre-existing test harness, so this is consistent with the codebase's current testing posture rather than a new gap introduced by this change.
diff --git a/.garura/project/issues/478/context/work-description.txt b/.garura/project/issues/478/context/work-description.txt
new file mode 100644
index 00000000..036ce0d5
--- /dev/null
+++ b/.garura/project/issues/478/context/work-description.txt
@@ -0,0 +1,3 @@
+install-garura always deploys the FULL component set (all skills, agents, plays) into a target — it has no way to install a subset. That is wrong for harness-type targets: the garura repo itself must carry only the meta plays (play-creator, play-editor) plus the change chain (start-change, commit-change, propose-change, review-change, merge-change) and their worker skills/agents. On 2026-07-04 the full set had to be manually trimmed out of garura's own .claude/ (86 -> 21 components); the next full install would put all 86 back.
+
+Wanted: a component-scope option on install-garura (e.g. --scope harness | full, or an explicit component list) so a target receives only the subset it needs, with the install manifest recording the scope; uninstall keeps reversing exactly what was placed. This is an intent change to the play (new guarantee about what gets deployed), so it goes through reference/ice.md + play-editor recompile.
diff --git a/core/components/plays/install-garura/SKILL.md b/core/components/plays/install-garura/SKILL.md
index 4b448380..0866acea 100644
--- a/core/components/plays/install-garura/SKILL.md
+++ b/core/components/plays/install-garura/SKILL.md
@@ -1,6 +1,6 @@
 ---
 name: install-garura
-description: 'Install Garura into a target project or repository so its skills, agents, and plays become discoverable by a host coding tool — Claude Code or the OpenAI Codex CLI. Reads this garura checkout''s core/components and runs a per-tool ADAPTER that lays them down in the host''s native shape: for claude, .claude/ skills + agents with model tiers resolved to Claude models; for codex, .agents/skills Agent Skills plus AGENTS.md and ~/.codex model/sandbox/approval profiles. Always writes a .garura/ tooling tree (config + STM scaffold) and copies shared memory to the machine-global ~/.garura, and records an install manifest so uninstall-garura can reverse exactly what was placed. Use when the user wants to install, set up, bootstrap, add, or enable Garura in another folder or repo for claude or codex — "install garura into X", "set up garura in this repo for codex", "bootstrap garura", "make codex see the garura skills". Takes the target path and an optional --tool. For the reverse, see uninstall-garura.'
+description: 'Install Garura into a target project or repository so its skills, agents, and plays become discoverable by a host coding tool — Claude Code or the OpenAI Codex CLI. Reads this garura checkout''s core/components and runs a per-tool ADAPTER that lays them down in the host''s native shape: for claude, .claude/ skills + agents with model tiers resolved to Claude models; for codex, .agents/skills Agent Skills plus AGENTS.md and ~/.codex model/sandbox/approval profiles. Always writes a .garura/ tooling tree (config + STM scaffold) and copies shared memory to the machine-global ~/.garura, and records an install manifest so uninstall-garura can reverse exactly what was placed. Use when the user wants to install, set up, bootstrap, add, or enable Garura in another folder or repo for claude or codex — "install garura into X", "set up garura in this repo for codex", "bootstrap garura", "make codex see the garura skills". Takes the target path, an optional --tool, and an optional --scope (full = everything, the default; harness = meta plays + change chain + their workers only). For the reverse, see uninstall-garura.'
 user-invocable: true
 ---
 
@@ -104,6 +104,15 @@ python3 core/components/plays/install-garura/scripts/install.py --target <path>
 
 Options:
 - `--tool claude|codex` — which host tool to target (default `claude`).
+- `--scope full|harness` — which component set the target receives (default `full`,
+  everything). `harness` installs only the meta plays (play-creator, play-editor), the five
+  *change plays, and the worker skills/agents those plays dispatch — for harness-type repos
+  (garura itself) that must not carry product plays. The scope filters **components only**;
+  shared memory, config, the STM scaffold, and the manifest are written the same either way.
+  The manifest records the scope, and since retirement is manifest-driven, re-running with
+  `--scope harness` over a previously full install retires the out-of-scope components
+  cleanly. The `harness` membership list lives in `scripts/install.py` (`SCOPES`) — when a
+  kept play gains a new worker, add it there in the same change.
 - `--source <garura-checkout>` — point at the garura checkout explicitly (needed only when the
   script can't sit beside the source).
 - `--memory-dest <path>` — where shared memory goes (default `~/.garura/core/memory`). Point it
@@ -153,6 +162,13 @@ Keep it short. The detail lives in the manifest; your job is the human summary.
 `intent.yaml`); the adapter split, the `--tool` parameter, and the Codex adapter were added by
 direct edit to this SKILL.md and `scripts/`.
 
+**Direct-edit deviation note (#478):** the `--scope` option (named component subsets: `full`
+default, `harness` = meta plays + change chain + their workers) was added by direct edit to
+this SKILL.md, `scripts/install.py` (the `SCOPES` table, the manifest `scope` field), and both
+adapters' `lay_components` (an `allow` filter) — same bootstrap-meta-play path as #434; no ICE
+source exists to recompile. Uninstall needs no change: it reverses what the manifest records,
+and a scoped manifest records exactly what was placed.
+
 Level 3 note (#466): bootstrap meta-play — exempt from the goal-loop recompile (no ICE
 source, like play-creator); runs are session-stamped by the host play when invoked through
 the pipeline.
diff --git a/core/components/plays/install-garura/scripts/adapters/claude.py b/core/components/plays/install-garura/scripts/adapters/claude.py
index c3f2d029..ae9f0793 100644
--- a/core/components/plays/install-garura/scripts/adapters/claude.py
+++ b/core/components/plays/install-garura/scripts/adapters/claude.py
@@ -57,7 +57,9 @@ def _rewrite_md(path, *, drop_model):
     common.write_text(path, head + "\n".join(out) + tail + body)
 
 
-def lay_components(components, target, info):
+def lay_components(components, target, info, allow=None):
+    """allow: per-kind name sets from the orchestrator's install scope
+    ({"plays": {...}, "skills": {...}, "agents": {...}}), or None for all."""
     claude = os.path.join(target, ".claude")
     skills_dest = os.path.join(claude, "skills")
     agents_dest = os.path.join(claude, "agents")
@@ -74,6 +76,8 @@ def lay_components(components, target, info):
         for name in sorted(os.listdir(src)):
             if common.skippable(name) or not name.endswith(".md"):
                 continue
+            if allow is not None and name[:-3] not in allow["agents"]:
+                continue
             if common.file_is_deprecated(os.path.join(src, name)):
                 deprecated += 1
                 continue
@@ -92,6 +96,8 @@ def lay_components(components, target, info):
         for name in sorted(os.listdir(src)):
             if common.skippable(name) or name in common.EXCLUDED_SKILLS:
                 continue
+            if allow is not None and name not in allow[kind]:
+                continue
             sp = os.path.join(src, name)
             # a folder without a SKILL.md is not an installable artifact
             # (dev/iteration leftovers); skip it rather than deploy a broken one
diff --git a/core/components/plays/install-garura/scripts/adapters/codex.py b/core/components/plays/install-garura/scripts/adapters/codex.py
index 77bd000c..28df2523 100644
--- a/core/components/plays/install-garura/scripts/adapters/codex.py
+++ b/core/components/plays/install-garura/scripts/adapters/codex.py
@@ -110,7 +110,9 @@ def _emit_skill(md_path, sid, src_dir, skills_root, paths):
     paths.append(os.path.join(".agents", "skills", sid))
 
 
-def lay_components(components, target, info):
+def lay_components(components, target, info, allow=None):
+    """allow: per-kind name sets from the orchestrator's install scope
+    ({"plays": {...}, "skills": {...}, "agents": {...}}), or None for all."""
     skills_root = os.path.join(target, ".agents", "skills")
     os.makedirs(skills_root, exist_ok=True)
 
@@ -126,6 +128,8 @@ def lay_components(components, target, info):
         for name in sorted(os.listdir(src)):
             if common.skippable(name) or name in common.EXCLUDED_SKILLS:
                 continue
+            if allow is not None and name not in allow[kind]:
+                continue
             sp = os.path.join(src, name)
             md = os.path.join(sp, "SKILL.md")
             if not os.path.isdir(sp) or not os.path.isfile(md):
@@ -143,6 +147,8 @@ def lay_components(components, target, info):
         for name in sorted(os.listdir(src)):
             if common.skippable(name) or not name.endswith(".md"):
                 continue
+            if allow is not None and name[:-3] not in allow["agents"]:
+                continue
             if common.file_is_deprecated(os.path.join(src, name)):
                 deprecated += 1
                 continue
diff --git a/core/components/plays/install-garura/scripts/install.py b/core/components/plays/install-garura/scripts/install.py
index 01f66da4..dd8c3855 100644
--- a/core/components/plays/install-garura/scripts/install.py
+++ b/core/components/plays/install-garura/scripts/install.py
@@ -12,10 +12,12 @@
 and the source `.garura/core/config.yaml` + `CLAUDE.md`, and lays them down so a
 host coding tool can discover them in the target.
 
-    python3 install.py --target <path> [--tool claude|codex]
+    python3 install.py --target <path> [--tool claude|codex] [--scope full|harness]
 
   --target        the project directory to install into (a path, created if absent)
   --tool          which host tool to target: claude (default) or codex
+  --scope         which component set to install: full (default, everything) or
+                  harness (meta plays + change chain + their workers only)
   --source        the garura checkout to install FROM (auto-derived if omitted)
   --memory-dest   where shared memory goes (default ~/.garura/core/memory)
   --force-config  overwrite an existing target .garura/core/config.yaml
@@ -145,9 +147,39 @@ def transform_config(content, project_name, tool):
 )
 
 
+# --- install scopes -----------------------------------------------------------
+
+# A scope names WHICH components a target receives; everything else about the
+# install (shared memory, config, STM scaffold, manifest) is unchanged. `full`
+# is the default and installs every component. `harness` is for garura-style
+# harness repos that must carry only the meta plays, the change chain, and the
+# workers those plays dispatch — nothing product-facing. Membership is explicit
+# and deterministic: when a kept play gains a new worker skill or agent, add it
+# here in the same change.
+SCOPES = {
+    "full": None,  # no filter — every component installs
+    "harness": {
+        "plays": {
+            "play-creator", "play-editor",
+            "start-change", "commit-change", "propose-change",
+            "review-change", "merge-change",
+        },
+        "skills": {
+            "analyze-changes", "analyze-pr", "create-commit", "manage-issue",
+            "merge-pr", "platform-adapter", "quality-check-scoped",
+            "resolve-issues", "setup-branch", "submit-pr",
+        },
+        "agents": {
+            "change-reviewer", "project-orchestrator",
+            "quality-auditor", "repo-orchestrator",
+        },
+    },
+}
+
+
 # --- the install --------------------------------------------------------------
 
-def install(source, target, tool, force, quiet, memory_dest):
+def install(source, target, tool, force, quiet, memory_dest, scope="full"):
     adapter = get_adapter(tool)
     components = os.path.join(source, "core", "components")
     if not os.path.isdir(components):
@@ -158,6 +190,7 @@ def _info(msg):
 
     record = {
         "tool": tool,
+        "scope": scope,
         "components": [],
         "counts": {},
         "instruction_surface": None,
@@ -166,8 +199,10 @@ def _info(msg):
         "global_files": [],
     }
 
-    # 1. components — the host surface, owned by the adapter
-    paths, counts, _ = adapter.lay_components(components, target, _info)
+    # 1. components — the host surface, owned by the adapter; the scope filter
+    #    decides which components the adapter may lay down
+    allow = SCOPES[scope]
+    paths, counts, _ = adapter.lay_components(components, target, _info, allow=allow)
     record["components"] = paths
     record["counts"] = counts
 
@@ -263,6 +298,9 @@ def main(argv=None):
     ap.add_argument("--target", required=True, help="project directory to install into (a path)")
     ap.add_argument("--tool", default="claude", choices=known(),
                     help="host tool to target (default: claude)")
+    ap.add_argument("--scope", default="full", choices=sorted(SCOPES),
+                    help="component set to install: full (default) or harness "
+                         "(meta plays + change chain + their workers)")
     ap.add_argument("--source", help="garura checkout to install from (auto-derived if omitted)")
     ap.add_argument("--memory-dest", default="~/.garura/core/memory",
                     help="where shared memory goes (default ~/.garura/core/memory)")
@@ -275,8 +313,10 @@ def main(argv=None):
     target = os.path.abspath(args.target)
     os.makedirs(target, exist_ok=True)
 
-    info(args.quiet, f"installing garura ({source}) into {target} for {args.tool}")
-    record = install(source, target, args.tool, args.force_config, args.quiet, args.memory_dest)
+    scoped = "" if args.scope == "full" else f" (scope: {args.scope})"
+    info(args.quiet, f"installing garura ({source}) into {target} for {args.tool}{scoped}")
+    record = install(source, target, args.tool, args.force_config, args.quiet,
+                     args.memory_dest, scope=args.scope)
     record["retired"] = retire_stale(target, record["components"], args.quiet)
     write_manifest(target, record, args.quiet)
 
