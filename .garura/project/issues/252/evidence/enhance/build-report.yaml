status: completed
task_id: implement
issue: 252
files_created:
  - core/tools/lint-components/package.json
  - core/tools/lint-components/index.js
  - core/tools/lint-components/lib/discover.js
  - core/tools/lint-components/lib/parse-frontmatter.js
  - core/tools/lint-components/lib/rules/structural.js
  - core/tools/lint-components/lib/rules/semantic.js
  - core/tools/lint-components/lib/rules/cross-reference.js
  - core/tools/lint-components/lib/reporter.js
  - core/components/skills/lint-components/SKILL.md
files_modified: []
notes:
  - "npm install completed successfully in core/tools/lint-components/ (js-yaml@4.1.0 installed)"
  - "Verification run: node core/tools/lint-components/index.js --target core/components --output json"
  - "Output is valid JSON. Summary: {errors: 88, warnings: 20, infos: 0, total_violations: 108}"
  - "Exit code 1 (expected — errors > 0 in corpus). Exit code 2 not encountered (no fatal errors)"
  - "The newly authored lint-components SKILL.md itself has zero violations (passes all structural checks)"
  - "88 errors are real corpus violations (missing frontmatter fields, missing sections, etc.) — not false positives. Not fixed — out of scope for this issue."
  - "20 warnings include spelling inconsistency warnings for plays using user-invokable (minority form) and other structural warnings"
  - "Agent field check: all 19 agents have both domain and role fields — impl-context's 'domain OR role' is conservative; both are present in every agent"
  - "Quality profile exists at .garura/product/specification/quality-profile.yaml (status: DRAFT per file)"
  - "Semantic extraction is conservative: skill refs matched via Skill Pool tables, 'Skill: <name>' line-start pattern, and 'invokes `<name>` skill' pattern only. Agent refs matched via Agent Boundaries table rows only."
  - "Cross-reference checks cover: constraint/failure_condition/scenario ID uniqueness (ERROR on duplicates), scenario coverage (WARNING when SCE-{N} missing from SKILL.md)"
  - "Text output mode (--output text) verified: correct one-violation-per-line format, summary line at end, exit code 1"
  - "--validate-templates flag is parsed and captured in args.validateTemplates but not wired into rule checks — it is a no-op stub. Template validation was specified as 'off by default' and conservative; the stub preserves the flag for future wiring without any current behavior"
  - "sub_plays chain check (mentioned in impl-context.md line 40) is not implemented — it was not listed in the task prompt's rule specifications and was omitted intentionally. Cross-reference.js covers the three ID-uniqueness checks and scenario coverage only"
