# Discovery — Issue #324

## Issue body (verbatim)

**Title:** [CHORE] Rename /scope play to /define

**Description:**
The current `/scope` play handles both new feature scoping and re-scoping/extending existing FEAT-IDs (5-bucket classification). The name "scope" is misleading — users read it as "add new". Rename the play to `/define` so the doorway works equally well for new and WIP features needing extension. Touches: `core/components/plays/scope/` rename, `intent.yaml` name field, all cross-references in plays/skills/docs, then `/create-play --rebuild` and `/sync-claude`.

**Acceptance criteria:**
- `core/components/plays/scope/` directory renamed to `define/`
- `reference/intent.yaml` name field updated to `/define`
- All cross-references in plays, skills, and docs updated
- `/create-play --rebuild` executed for the renamed play
- `/sync-claude` run to deploy updated play globally

## Q&A — orchestrator → user

**Q1. Compatibility shim — keep `/scope` as alias forwarding to `/define`?**
A1. **No.** Hard rename. Skill name should be `garura:define` (not `garura:scope`).

**Q2. Skill frontmatter `name:` field — keep `garura:` prefix, become `garura:define`?**
A2. **Yes.** Frontmatter `name: garura:define`.

**Q3. Existing closed-issue STM evidence under `.garura/project/issues/*/evidence/scope/` — leave or rename?**
A3. **Leave as-is.** Historical evidence is not rewritten.

**Q4. Add `define` to features.yaml AM-F001 plays_inventory list?**
A4. **Yes.** Add `define` to the plays_inventory under Component Architecture / Plays.

**Q5. `product.directories.scope` config key, `scope.yaml`, and other domain "scope" usages (capability scope) — leave untouched?**
A5. **Yes.** Those refer to capability scoping (a domain noun), not the play. Untouched.

## Confirmed integration points

- `core/components/plays/scope/` (whole directory) → rename to `core/components/plays/define/`
- `core/components/plays/scope/reference/intent.yaml` — `name: scope` → `name: define`
- `core/components/plays/scope/SKILL.md` — compiled artifact; will be regenerated by `/create-play --rebuild define`
- All cross-references to `/scope` (the play, slash-command form) and `garura:scope` (skill ID) across:
  - other plays' SKILL.md and intent.yaml
  - skills under `core/components/skills/`
  - docs (`docs/`, `CLAUDE.md`, ADRs)
  - features.yaml plays_inventory (and add `define`)
- `~/.claude/skills/scope/` — must be removed during `/sync-claude` (replaced by `define/`)

## Untouched

- `.meridian/`, `.garura/product/scope/` directory and `scope.yaml` / `enriched-capabilities.yaml` content (these are capability-scope nouns, unrelated)
- `product.directories.scope` config key in `.garura/core/config.yaml`
- Past STM evidence under `.garura/project/issues/*/evidence/scope/` (historical record)
- Domain term "scope" anywhere it refers to capability scope, not the play

## Success criteria for this enhancement

1. `/define` is invocable as a slash command and behaves identically to current `/scope`.
2. `garura:scope` no longer exists as a skill ID anywhere in source or deployed harness.
3. All cross-references in source-of-truth `core/components/` point at `define`.
4. `features.yaml` AM-F001 plays_inventory lists `define`.
5. `/create-play --rebuild define` succeeds (intent.yaml is consistent).
6. `/sync-claude` deploys `define/` to `~/.claude/skills/define/` and the old `~/.claude/skills/scope/` is gone.
7. Quality gates pass (lint-components on the renamed play passes structural checks).
