analysis:
  branch: fix/330-lint-plays-no-model-required
  issue: 330
  groups:
    - id: G1
      type: fix
      scope: lint-components
      subject: "remove model from play required-fields in structural linter (#330)"
      files:
        - core/tools/lint-components/lib/rules/structural.js
      issue_mapping:
        issue: 330
        confidence: high
        rationale: >
          Branch name encodes issue #330. Single file change directly implements
          the fix: drops `model` from requiredFields for plays, with explanatory
          comment that plays inherit model from the harness session.

    - id: G2
      type: chore
      scope: stm
      subject: "record start-feature evidence for #330"
      files:
        - .garura/project/issues/330/
      issue_mapping:
        issue: 330
        confidence: high
        rationale: >
          STM evidence directory created by start-feature play for issue #330.
          Operational artifact — not part of the linter fix itself.

  risks:
    sensitive_files: []
    breaking_changes:
      - file: core/tools/lint-components/lib/rules/structural.js
        detail: >
          `model` removed from play required-fields. Plays that previously
          failed lint for missing `model` will now pass. Intentional relaxation
          — plays inherit model from harness — but downstream callers relying
          on this validation gate will no longer be protected.
    ambiguous_types: []
    hotfix_branch: false
