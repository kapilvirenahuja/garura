# Fix-It Delivery Record — #417

- **Issue:** #417 — evals-engineer agent cannot run its eval-generation skill (missing tools)
- **State:** CLOSED (auto-closed by PR)
- **Root cause:** `core/components/agents/evals-engineer.md` declared only Read, Grep, Glob, Skill in its `tools:` frontmatter (lines 7–11). A skill runs with its host agent's tool grant; the hosted `generate-encrypted-evals` skill needs Write (temp eval file + manifest.json) and Bash (openssl, rm, ls). With neither available, the skill could never execute — eval generation was structurally non-functional.
- **Fix strategy:** Added `Write` and `Bash` to the agent's tools list. Prose prohibitions (no inline eval authoring, no direct openssl) left unchanged.
- **Files modified:** `core/components/agents/evals-engineer.md` (source); `~/.claude/agents/evals-engineer.md` (deployed via /sync-claude)
- **Regression test:** `.garura/project/issues/417/evidence/fix-it/regression-test.yaml` — two grep assertions (Write present, Bash present). Verdict: RED before fix, GREEN after (verified by quality-auditor, `regression-test-verdict.yaml`).
- **PR:** #418 — merged. Body carried `Closes #417`.
- **Merge SHA:** `b741c1e` (merge commit for PR #418) on `main`; fix commit `9bee07f`.
- **Branch:** `fix/417-evals-engineer-tools` — deleted locally and remotely.
- **Resolution trace:** 2 decisions resolved from core LTM, 2 LLM-fallback (no project LTM; `has_product_ltm=false`). Flagged learning gap: no standard defines "a host agent must carry a superset of the tools its hosted skills declare."
- **Issue comment:** posted to #417 (RCA & Fix Plan — Approved). Dispatch status: completed.

## Step eval results
- SE-1 (issue open): PASS
- SE-2 (RCA specific/distinct): PASS
- SE-3 (design alternatives w/ rejection): PASS (3 alternatives)
- SE-4 (resolution trace non-empty): PASS
- SE-10 (regression test red pre-fix): PASS
- SE-13 (task_list initialized): PASS
- SE-5 (no HTML/briefs artifact): PASS
- SE-6 (tether before implement): PASS (tether 06:13:20 < implement 06:14:00)
- SE-7 (issue-comment stub before implement): PASS
- SE-8 (files_modified within affected_files): PASS
- SE-9 (builder contract clean of brief): PASS
- SE-12 (no test content in builder contract): PASS
- SE-11 (verdict from quality-auditor): PASS
- SE-14 (ship delivered: commit/PR/merged/branch deleted): PASS

## Scenario eval results
- SCE-1 (Developer — full artifact + delivery trail): PASS
- SCE-2 (Tech Lead — checkpoint completeness): PASS
- SCE-3 (QA — PR links #417, approval before implement): PASS
- SCE-4 (QA — issue audit trail comment): PASS

## Final task_list
- task-1 (add Write+Bash to source): completed
- task-2 (sync-claude deploy): completed

## Notes
- `evidence.record=false` in config: STM evidence is intentionally NOT committed (solo-dev repo policy). This record is a local artifact.
- A prior direct-edit attempt at this same fix was reverted on the user's call so the change could go through proper RCA + checkpoint + PR. The independent analysis reached the same fix and rejected three architectural alternatives on merit.
