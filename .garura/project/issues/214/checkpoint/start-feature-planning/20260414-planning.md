# Checkpoint: start-feature-planning — Issue #214

**Status:** COMPLETED (user Tethered 2026-04-14; scenario evals all PASS; evidence written)
**Date:** 2026-04-14
**Issue:** [#214 — Build product planning and design pipeline](https://github.com/kapilvirenahuja/meridian-os/issues/214)
**Branch:** `feature/214-product-planning-design-pipeline`

## Planning Artifacts Written

| Artifact | Path |
|----------|------|
| Specification | `.meridian/project/issues/214/specs/spec.md` |
| Verification | `.meridian/project/issues/214/specs/verify.md` |
| Task Breakdown | `.meridian/project/issues/214/specs/tasks.md` |
| Checkpoint | `.meridian/project/issues/214/checkpoint/start-feature-planning/20260414-planning.md` |

## Summary

The plan compiles three reference documents (`play-specifications.md`, two `compass_artifact_wf-*` research briefs), two rounds of user directives, and 5 approval-gate decisions into a 7-sub-issue decomposition spanning 39 granular tasks.

### Round 1 directives (applied)
- Folder whitelist is strict — no new siblings under `.meridian/`
- New plays built fresh; old overlapping plays deleted after pattern capture
- Scriber utility agent for background evidence writing

### Round 2 directives (applied)
- `core/config.yaml` moves to `.meridian/core/config.yaml` (214.2 T6a)
- Play levels (L1/L2) and agent-count budgets retired — CLAUDE.md section 4 rewritten (214.2 T6b)
- `intent.yaml` gains metadata: `name`, `description`, `version`, `checksum` + 4 content fields
- "Bake" terminology retired — `/create-play --bake` → `--build`
- `/prepare-architecture` deleted and replaced with fresh `/spec-arch` (214.7)
- Existing `domain-taxonomy/*.md` files extended in-place with 5 new sections; NO parallel YAML capability tree

### 5 approval-gate decisions (resolved 2026-04-14)

1. **Compile verb:** `--build` ✅
2. **CLAUDE.md section 4 replacement text:** "A play is just a play — coherence is enforced by the intent schema and evals, not by a level or agent-count budget." ✅
3. **Intent.yaml checksum algorithm:** `sha256` over the YAML-normalized content zone (sorted keys, LF line endings, UTF-8, excluding the `checksum` field itself) ✅
4. **`product-strategist` agent fate:** DELETE outright (no refactor). User directive: "after these product strategist wont be needed". Implication: `prepare-implementation` play's agent dispatches reassigned to `tech-designer`/`tech-architect` OR flagged for a follow-up redesign issue. ✅
5. **`--bake` alias during 214.2:** HARD CUT. No alias. After 214.2 lands, `--bake` fails with unknown-option. ✅

## Decomposition (7 sub-issues, strict sequencing)

| # | Title | Size |
|---|-------|------|
| 214.1 | `scriber` utility agent for background evidence writing | M (2 days) |
| 214.2 | Config move to `.meridian/core/`, folder whitelist, retire play levels, rename `--bake` → `--build` (hard cut) | M (3-4 days) |
| 214.3 | Capture patterns (incl. prepare-architecture), delete 3 deprecated plays + product-strategist agent, reassign prepare-implementation dispatches | M (2-3 days) |
| 214.4 | Extend `domain-taxonomy/*.md` + cross-tree constraints YAML + intent-yaml-schema with sha256 checksum spec | M (2-3 days) |
| 214.5 | `/plan-product` fresh build (Product Keeper + Market Analyst agents) | XL (5-7 days) |
| 214.6 | `/design-product` fresh build (Designer agent) | L (4-5 days) |
| 214.7 | `/spec-arch` fresh build (replaces deleted `/prepare-architecture`) | L (4-5 days) |

**Total estimate:** 22-32 days across 7 reviewable PRs.

## Known risks surfaced

1. Deleting 4 plays + 1 agent (discover-product, plan-roadmap, discover-product-opportunity in 214.3; prepare-architecture in 214.7; product-strategist in 214.3) silently loses production detail unless 214.3 captures every valuable pattern first. **High — mitigated by capture-first.**
2. `prepare-implementation` play currently depends on `product-strategist`. With product-strategist deleted, the play either gets reassigned to `tech-designer`/`tech-architect` OR flagged for follow-up redesign. **Medium — mitigated by T14 audit + T16 reassignment + T17 regression test.**
3. Config move breaks something grep didn't surface. **Medium — mitigated by scenario-eval runs on every rebuilt play.**
4. KB extension must enforce 5 new sections on every feature via linter. **Medium — mitigated by `validate-kb-extension` skill.**
5. Cross-tree constraint evaluation past ~50 constraints may become unreliable under pure LLM reasoning. **Medium — mitigated by normalized constraint form and explicit walk.**
6. `/spec-arch` scope is open-ended. **Medium — mitigated by dedicated design sub-task within 214.7.**

## Scenario Evals (run in Step 6 after Tether)

- **SCE-1:** tasks.md contains definitions detailed enough for T1 to start immediately without further planning
- **SCE-2:** spec.md has strategy + alternatives + risks — enough for a Tech Lead to make an informed decision
- **SCE-3:** verify.md has specific acceptance criteria, verification steps, edge cases — no ambiguity
- **SCE-4:** tasks.md dependency graph enables PM scope / critical path assessment

---

**Next action:** the 5 decisions are recorded in the spec files. User types **Tether** to finalize (writes evidence, self-commits STM artifacts, closes the planning loop) or **Vanish** to cancel (deletes the remote branch).
