# SPEC: #148 — Prepare-architecture play, three-axis profiling, quality standards
<!-- Generated by start-feature-planning | Plan sub-agent output -->

**Intent:** Introduce a three-axis project profiling model (PP + NFR + QP), a dedicated `prepare-architecture` play, quality standards, and refactor the play pipeline so that architecture is locked upstream of per-epic implementation planning — eliminating the current duplication where `prepare-implementation` performs global architecture scoping that should happen once per product.

**Constraints:**
- All profile derivation (PP, NFR, QP) must follow the existing 5-level, 7-dimension pattern with agent-derived-then-user-validated knobs.
- The existing `discover-product` → `plan-roadmap` → `prepare-implementation` pipeline ordering must be preserved; `prepare-architecture` is inserted between `plan-roadmap` and `prepare-implementation`.
- `prepare-implementation` must NOT break for any in-flight product that was prepared before this change — locked artifacts from prior runs remain valid.
- Schema changes to `product.yaml` must be additive (new `profiles` section), never removing existing fields.
- LTM files (`quality-profile.md`, `_index.md`) are additive — no existing dimension definitions in PP or NFR are modified.
- Play compilation is done via `/create-play` (or `--rebake`); SKILL.md files are compiled artifacts and must NOT be hand-edited.

**Failure Conditions:**
- `quality-profile.md` dimensions do not follow the established 5-level table format (Level | Label | Description | Examples) present in `product-profile.md` and `nfr-profile.md`.
- `product.yaml` schema loses backward compatibility (existing fields removed or renamed).
- `prepare-architecture` produces artifacts without requiring locked `product.yaml` and locked `roadmap.yaml` as upstream inputs.
- `prepare-implementation` still generates global `architecture.yaml` after refactoring (it should only read a locked one from upstream).
- Profile derivation in `discover-product` is a cold questionnaire instead of agent-derived knobs per the "Profiles Are Knobs, Not Questions" principle in `_index.md`.
- Rebaked plays fail to compile via `/create-play --rebake`.

### Summary

This issue restructures the Meridian play pipeline by introducing a **Quality Profile** as a third profiling axis alongside the existing Product Profile and NFR Profile, creating a dedicated **prepare-architecture** play, and refactoring downstream plays to consume profiles and locked architecture as upstream inputs.

Currently, `prepare-implementation` owns the global architecture scoping responsibility (Steps 9-10 draft architecture.yaml and tech.yaml per-epic). This is problematic because architecture decisions should be made once per product after roadmap planning, not re-derived per epic. The new `prepare-architecture` play sits between `plan-roadmap` and `prepare-implementation`, reading the locked `product.yaml` (which now contains PP+NFR+QP profiles) and locked `roadmap.yaml`, and producing a locked `architecture.yaml` and a new `quality-standards.yaml`. The `prepare-implementation` play is then refactored to require the locked `architecture.yaml` as an upstream input rather than generating it.

The `discover-product` play is extended so that during vision drafting (Step 3), the agent derives all three profiles from the BRD/intent. The PP is derived first (already conceptually happens), then NFR defaults are suggested from PP (per existing NFR Guidance), and finally QP defaults are suggested from both PP and NFR. All three are written to a new `profiles` section in `product.yaml`.

### Affected Files

| File | Role | Change Needed |
|------|------|---------------|
| `core/components/memory/knowledge/product/quality-profile.md` | LTM knowledge | **Create** — 7 QP dimensions (reliability, performance, security, usability, maintainability, observability, compliance) following the 5-level pattern |
| `core/components/memory/knowledge/product/_index.md` | LTM index | **Modify** — Rename "Two-Axis Model" → "Three-Axis Model", add QP description, update flow diagram, add QP to contents table |
| `core/components/skills/draft-product-vision/schemas/product.yaml` | Schema | **Modify** — Add `profiles` section with `product_profile`, `nfr_profile`, `quality_profile` sub-sections |
| `core/components/skills/draft-product-vision/SKILL.md` | Skill | **Modify** — Add profile derivation logic to the compose step; derive PP → NFR → QP in order |
| `core/components/plays/discover-product/reference/intent.yaml` | Play intent | **Modify** — Add constraints for profile derivation (3 profiles written to product.yaml), update scenarios |
| `core/components/plays/discover-product/SKILL.md` | Play (compiled) | **Rebake** via `/create-play --rebake discover-product` after intent.yaml update |
| `core/components/skills/scope-roadmap-epics/SKILL.md` | Skill | **Modify** — Reference profiles from product.yaml for epic depth reasoning |
| `core/components/skills/assess-feasibility/SKILL.md` | Skill | **Modify** — Reference profiles from product.yaml for risk calibration |
| `core/components/plays/plan-roadmap/reference/intent.yaml` | Play intent | **Modify** — Add constraint that profiles inform epic scoping and feasibility |
| `core/components/plays/plan-roadmap/SKILL.md` | Play (compiled) | **Rebake** via `/create-play --rebake plan-roadmap` after intent.yaml update |
| `core/components/skills/draft-technical-approach/schemas/architecture.yaml` | Schema | **Modify** — Add `profiles_ref` field pointing to product.yaml profiles section |
| `core/components/plays/prepare-implementation/reference/intent.yaml` | Play intent | **Modify** — Remove architecture scoping from prepare-implementation; require locked architecture.yaml as upstream input |
| `core/components/plays/prepare-implementation/SKILL.md` | Play (compiled) | **Rebake** via `/create-play --rebake prepare-implementation` after intent.yaml update |
| `core/components/plays/prepare-architecture/reference/intent.yaml` | Play intent | **Create** — New intent for prepare-architecture play |
| `core/components/plays/prepare-architecture/SKILL.md` | Play (compiled) | **Create** via `/create-play prepare-architecture` after intent.yaml is written |
| `core/components/skills/draft-technical-approach/SKILL.md` | Skill | **Modify** — Accept profiles_ref input; architecture decisions informed by NFR + QP profiles |
| New: quality-standards.yaml schema | Schema | **Create** — Schema for the quality standards artifact produced by prepare-architecture |

### Technical Approach

**Strategy:** Phased bottom-up implementation — LTM foundations first, then schema changes, then skill updates, then play intent updates, then play compilation.

**Alternatives Considered:**
- **Embed QP directly in NFR profile** → Rejected because QP measures engineering quality practices (code coverage, testing depth, documentation standards), which are orthogonal to NFR runtime requirements (latency, uptime, encryption). Mixing them violates the single-responsibility of each profile axis.
- **Keep architecture in prepare-implementation** → Rejected because architecture decisions are product-scoped, not epic-scoped. Re-deriving architecture per epic leads to inconsistency and redundant agent work.
- **Create a separate derive-profiles play** → Rejected because profile derivation is tightly coupled to product discovery — the BRD/intent is the input. Splitting it into a separate play would break the single-invocation flow of `discover-product`.

**Risks:**
- **Rebake compatibility** — Rebaking plays after intent.yaml changes may produce SKILL.md with unexpected structure changes → Mitigation: Review each rebaked SKILL.md diff carefully before committing; run `/create-play --rebake` in isolation per play → Severity: medium
- **Schema migration for existing products** — Existing locked `product.yaml` files lack the `profiles` section → Mitigation: Profiles section is additive and optional; downstream plays check for presence and degrade gracefully → Severity: low
- **Circular dependency in profile derivation** — QP depends on NFR which depends on PP → Mitigation: Sequential derivation order (PP → NFR → QP) is explicitly specified in the flow, matching the existing PP → NFR pattern → Severity: low
