# Consolidated findings — PR #486 (issue #467, merge-to-main re-pin), 2026-07-04
# Gate OFF (review-change) → harness-verdict path.
categories:
  harness:
    playbook: review/harness
    layer1: {check: "lint_play.py merge-change (EXECUTED direct)", result: "PASS 0 gaps", finding: null}
    layer2:
      source: design-findings.yaml
      result: "conforms: recompiled from ICE (fingerprint hand-verified); re-pin matches the
        committed 'pinned' definition (never resolves off, changed via ICE); three
        preconditions kept as a machine wall verified before presenting; F7/REC7/SE-7
        updated to the pinned model, no dangling off-path; scope clean (only merge-change +
        its config line)"
      resolved_in_pr:
        - class: P2
          summary: "source-of-truth for the pinned set was out of sync — gate-config.md and
            the #467 ruling still listed only grill/launch/learn and called merge-change off"
          resolution: "FIXED in this PR: gate-config.md pinned-set updated to include
            merge-change's land; config comment already updated in the re-pin; #467
            correction comment posted (issue #467 comment 4882506281)"
  config-map:
    playbook: review/config-maps
    layer1: {check: "YAML valid; merge-change dropped from off-list", result: "PASS", finding: null}
  stm-records:
    playbook: null
    finding: {class: P4, summary: "branch.json unplaybooked — recurring; seed knowledge/review/stm-records.md", basis: "categories.yaml"}
summary: {P1: 0, P2: 0, P3: 0, P4: 1, resolved_in_pr: 1, runnable_suites_red: 0}
