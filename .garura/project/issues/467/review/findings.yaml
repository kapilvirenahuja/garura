# Consolidated findings — PR #487 (issue #467, pin deploy), 2026-07-04
# Gate OFF (review-change) → harness-verdict path.
categories:
  harness:
    playbook: review/harness
    layer1: {check: "lint_play.py deploy (EXECUTED direct)", result: "PASS 0 gaps", finding: null}
    layer2:
      source: design-findings.yaml
      result: "conforms: recompiled from ICE (fingerprint hand-verified); pin matches the
        committed 'pinned' definition; C10 coherent with exactly one F10/REC10/SE-11, old
        off-path text removed, no orphans; scope clean (deploy + gate-config only)"
      resolved_in_pr:
        - class: P2
          summary: "PR cited 'deploy needs HITL per Kapil's ruling' but that ruling was not
            yet in the committed record — #467 comments listed deploy as unruled/off"
          resolution: "FIXED: the deploy ruling is now recorded on #467 (issue comment
            4882556885); the citation is grounded in the committed record"
  stm-records:
    playbook: null
    finding: {class: P4, summary: "branch.json unplaybooked — recurring; seed knowledge/review/stm-records.md", basis: "categories.yaml"}
summary: {P1: 0, P2: 0, P3: 0, P4: 1, resolved_in_pr: 1, runnable_suites_red: 0}
