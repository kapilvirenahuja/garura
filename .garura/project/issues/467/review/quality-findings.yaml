meta:
  generated_at: "2026-07-04T13:51:54Z"
  diff_path: ".garura/project/issues/467/review/pr.diff"
  changed_paths_count: 5
  scope_note: >
    Scoped run per orchestrator instruction — evaluates only the CODE category
    (4 files: check_self_review.py + validate_issue.py, canonical copies under
    play-creator/references/ and their stamped deploy copies under
    propose-change/scripts/ and start-change/scripts/) and the config category
    (.garura/core/config.yaml). The other 17 changed paths in PR #483
    (harness SKILL.md/ice.md prose, test_chain_checks.py, generated STM run
    records) belong to other review categories per categories.yaml and were
    NOT evaluated here.
  scan_coverage: 1.0
  taxonomy_path: "./core/components/memory/standards/rules/pr.md"
  standards_relevant: 12
  standards_with_findings: 0
  artifact_type_note: >
    All 4 python files classify as `garura-prose` (core/components/** wins
    first-match in the Artifact-Type Scoping table, #438) despite being real
    executable scripts — this makes every grep: and grep+path: taxonomy row
    ineligible (no SEC-*/BE-*/ARCH-* row can fire regardless of content).
    .garura/core/config.yaml classifies as `stm-evidence`
    (.garura/core/** in the same table) for the same reason. Only pure
    path: rows remain eligible; those were evaluated for real using ruff
    (default ruleset + format --diff), radon (cyclomatic complexity), and an
    AST-based nesting/line-count pass against the KB thresholds in
    quality/code/complexity-structure.md and quality/code/linting-formatting.md.
  tool_evidence:
    - "ruff check (default ruleset): All checks passed! — 0 warnings on all 4 files"
    - "ruff format --diff: 4 files already formatted — 0 diff"
    - "black --check --diff: 4 files would be left unchanged — 0 diff (cross-check)"
    - "radon cc -s: check_self_review.py main()=6, fail()=1; validate_issue.py main()=10, read_description()=5, fail()=1 — all ≤10 (CODE-09 threshold)"
    - "AST line/depth scan: longest function 38 lines (≤50, CODE-10); max nesting depth 2 (≤4, CODE-11); largest file 88 lines (≤300, CODE-12)"
  dropped_no_violation:
    - {standard_id: CODE-01, reason: "no linter config file (.eslintrc/.pyproject.toml[pylint]/ruff.toml) changed in diff — row does not match any of the 5 scoped paths"}
    - {standard_id: CODE-02, reason: "no formatter config file changed in diff — row does not match"}
    - {standard_id: CODE-03, reason: "ruff check (default ruleset) clean — 0 warnings on all 4 files"}
    - {standard_id: CODE-04, reason: "ruff format --diff and black --check both produce zero diff on all 4 files"}
    - {standard_id: CODE-09, reason: "cyclomatic complexity ≤10 in every function (radon), threshold not breached"}
    - {standard_id: CODE-10, reason: "longest function is 38 lines, threshold ≤50 not breached"}
    - {standard_id: CODE-11, reason: "max nesting depth 2, threshold ≤4 not breached"}
    - {standard_id: CODE-12, reason: "largest file is 88 lines, threshold ≤300 not breached"}
    - {standard_id: CODE-13, reason: "Single Responsibility is a manual-review-gate check (KB) — each script does exactly one job (validate an issue / scan a self-review); no automatable evidence of violation"}
    - {standard_id: CODE-14, reason: "no circular dependencies — the two scripts do not import each other or any local module"}
    - {standard_id: CODE-15, reason: "cognitive-complexity check requires SonarQube (KB tool reference) — tool unavailable in this environment, no fabricated score"}
    - {standard_id: CODE-17, reason: "ruff (default ruleset, which includes E741 ambiguous-name) found no naming violations — identifiers are clear (title, desc, problems, verdict, blockers)"}
    - {standard_id: CODE-19, reason: "no case-convention violations — all constants/identifiers already consistent snake_case"}
    - {standard_id: CODE-20, reason: "catch-all path:**/* row (P4, evidence=file only) fired on all 5 scoped files but carries no computable violation signal; logged for audit, not emitted as a finding"}

findings: []

counts:
  P1: 0
  P2: 0
  P3: 0
  P4: 0
