# Fix-It Delivered — #377

## Run Summary

| Field | Value |
|-------|-------|
| Play | fix-it |
| Issue | #377 — [BUG] repo-orchestrator drops bundled multi-commit dispatches (commits one, then stops) |
| Status | COMPLETE |
| Started | 2026-05-27 (T1 issue validation) |
| Completed | 2026-05-27 (PR #394 merged, branch deleted) |
| Root Cause | repo-orchestrator's "Don't chain" rule was a blanket NEVER with no carve-out for explicitly-requested multi-group iteration; Decision Framework had no loop construct; Intent→Skill Mapping table had no multi-group pattern. The agent therefore committed the first group and returned. |
| Fix Strategy | Three surgical text edits to `core/components/agents/repo-orchestrator.md`: qualified "Don't chain" with a multi-group carve-out, inserted step 8b loop in the Decision Framework, added a multi-group row to the Intent→Skill Mapping table. |
| Regression Test Verdict | PASS (3/3 grep assertions GREEN after fix; all 3 RED before) |

## Pipeline Steps

| Step | Status | Key Output |
|------|--------|------------|
| Validate Issue (T1) | PASS | issue-read.yaml, state=open, bug label confirmed. |
| Create Branch (T2) | PASS | `fix/377-multi-commit-dispatch` created from main, pushed. |
| RCA & Design (T3) | PASS (3 dispatches due to skill-chain drift) | rca.yaml, design.yaml, resolution-trace.yaml, regression-test.yaml (red-verified). |
| Prepare Inline Checkpoint (T4) | PASS | No HTML brief produced — inline render from STM YAML. |
| Approval Gate (T5) | PASS | Tether received; background issue comment dispatched. |
| Implement Fix (T6) | PASS | Three edits applied; implementation-report.yaml records one file modified. |
| Verify Fix (T7) | PASS | quality-auditor regression-test-verdict: pass. All 3 assertions GREEN. |
| Ship (T8) | PASS (3 dispatches, demonstrating the bug live) | First dispatch committed and stopped (the bug). Drove push + create-pr + merge as separate dispatches. Merged. |
| Scenario Validation (T9) | PASS | All 4 SCE green. |
| Write Evidence (T10) | PASS | This file. |

## Artifacts Produced

Under `.garura/project/issues/377/evidence/fix-it/`:
- `issue-read.yaml` — full issue body
- `branch-result.yaml`
- `rca.yaml` — root cause trace
- `resolution-trace.yaml` — core LTM resolution (no product LTM)
- `design.yaml` — strategy + 2 alternatives with rejections + execution plan
- `regression-test.yaml` — 3 grep-based eval-spec assertions
- `regression-test-verdict.yaml` — quality-auditor pass verdict
- `implementation-report.yaml`
- `issue-comment-agent.yaml` — completed, comment URL recorded
- `ship-result.yaml` — commit/PR/merge/branch-delete record
- `scenario-evals.yaml` — SCE-1..4 results
- `20260527-063508.md` — this delivery record

**Delivered to main:**
- Commit: `dde7d64` (pre-merge local) → merge SHA `d35691972df815dec837fe7d95ac6402c90865e8` (squash on main)
- File changed: `core/components/agents/repo-orchestrator.md`
- PR: https://github.com/kapilvirenahuja/garura/pull/394 (merged)
- Branch: `fix/377-multi-commit-dispatch` deleted (local + remote)
- Issue comment: https://github.com/kapilvirenahuja/garura/issues/377#issuecomment-4551972484

## Step Eval Results

SE-1 (validate-issue) PASS · SE-2 (rca root_cause distinct + file-cited) PASS · SE-3 (design alternatives with rejection_reason) PASS · SE-4 (resolution-trace non-empty, core LTM entries) PASS · SE-5 (no HTML brief) PASS · SE-6 (Tether before implement-fix) PASS · SE-7 (issue-comment-agent.yaml dispatched before implement) PASS · SE-8 (files_modified in design.affected_files) PASS · SE-9 (builder contract carried no brief content) PASS · SE-10 (regression-test red-verified before T6) PASS · SE-11 (verdict from quality-auditor, not builder self-report) PASS · SE-12 (builder contract: only paths, no test content) PASS · SE-13 (task_list initialized) PASS · SE-14 (ship evidence has commit_sha + pr_number + pr_state=merged + branch_deleted=true) PASS.

## Scenario Eval Results

SCE-1 PASS · SCE-2 PASS · SCE-3 PASS · SCE-4 PASS.

## Notable observations

The bug being fixed was demonstrated live during the ship phase. The first repo-orchestrator dispatch (instructing commit + push + PR + merge in one bundle) created the commit and stopped — exactly the symptom in the issue body. The remaining steps were driven through two additional single-step dispatches. After this PR merges, `/sync-claude` will deploy the new agent instructions and bundled dispatches should work in a single call.

The tech-designer skill chain also drifted twice — the first dispatch only ran draft-rca; the second only ran draft-fix-design. Each remaining skill was invoked in a focused follow-up dispatch. This is consistent with the existing memory note `feedback_tech_designer_skill_chain_drift` — the dispatch-each-skill-isolated pattern was followed.

## Next Steps

1. Run `/sync-claude` to deploy the updated `repo-orchestrator.md` to `~/.claude/agents/`. Until this runs, the live agent still has the old instructions and bundled dispatches will continue to fail.
2. After the deploy, optionally re-run the regression test against the deployed copy as a verification that the fix is live where it matters.

Evidence file: `.garura/project/issues/377/evidence/fix-it/20260527-063508.md`
