status: completed
tasks_completed: [1, 2, 3, 4, 5]
files_created: []
files_modified:
  - core/components/plays/enhance/reference/intent.yaml
notes: |
  All 5 additive tasks completed successfully. No existing constraints (C1-C18),
  failure conditions (F1-F10), or scenarios (S1-S7) were modified.

  Task 1 — Extend C7 with full risk schema field enumeration:
    Updated C7 rule text to enumerate all required risk fields: description,
    mitigation, type (technical|business|architectural), action (eval-driven|
    approval-required|monitor-only), ltm_reference (string path or null),
    generated_evals (list, populated when action is eval-driven).

  Task 2 — Add C19 (Risk Classification Constraint):
    Inserted C19 after C8 in the Approach Design section. Covers: required
    fields per risk, type taxonomy, action taxonomy, LTM/KB grounding rule,
    eval-driven propagation (MUST, at approach design time), backward
    compatibility default (type:technical, action:monitor-only for historical
    files), and override allowance with justification.

  Task 3 — Add C20 (Auto-triggered Risk Checkpoint):
    Inserted C20 after C10 in the Mid-Checkpoint section. Covers: unconditional
    checkpoint trigger when approval-required risks present (regardless of
    --approve-plan), display scope limited to approval-required risks only,
    Tether/Vanish semantics, and explicit orthogonality statement with C9.

  Task 4 — Add F11 (Missing Approval-required Checkpoint):
    Inserted F11 after F10. F11 fires when approval-required risks present but
    C20 checkpoint was skipped. Explicitly non-configurable (unlike F8) and
    noted as distinct from F8.

  Task 5 — Add S8 (Approval-required Risk Scenario):
    Inserted S8 after S7. Covers the end-to-end flow: type:architectural +
    action:approval-required risk with --approve-plan NOT set triggers focused
    C20 checkpoint. Explicitly distinct from S5 (--approve-plan full plan
    review).

  Evals verified:
    E1: C7 now enumerates type, action, ltm_reference, generated_evals. All
        existing C7 field enumerations intact. PASS.
    E2: C19 defines type taxonomy, action taxonomy, LTM/KB grounding rule,
        generated_evals propagation (MUST), and backward compatibility default.
        PASS.
    E3: C20 fires unconditionally on approval-required risks, shows ONLY those
        risks, Tether/Vanish semantics, orthogonality with C9 stated. PASS.
    E4: F11 is separate from F8, fires regardless of --approve-plan, references
        C20 focused checkpoint not the full plan review. PASS.
    E5: S8 has persona, given (type:architectural, action:approval-required, no
        --approve-plan), then (focused checkpoint, Tether/Vanish). Distinct from
        S5. PASS.
    E6: C19 states propagation as MUST at approach design time for eval-driven
        risks into top-level evals list. PASS.
