# build-arch — Play Analysis (Rebake for Defect 22)

Generated by `/create-play --rebake build-arch` on 2026-04-15.

## Rebake Reason — Defect 22

D22 is a **pure path-fix rebake**. The post-D1 folder layout (ADR 017) split
`.meridian/product/` into stage-centric sub-folders, and the former `ux/` and
`arch/` folders were renamed to `experience/` and `architecture/`. The
build-arch intent.yaml and SKILL.md still carried pre-D1 paths and had to be
corrected to align with the locked product folder whitelist.

**No new constraints. No new failure conditions. No new scenarios. No new
agents. No new skills. No structural workflow change.** The semantic map of
the play is otherwise identical to the prior 2026-04-14 compile.

## Path Migration Summary

The intent.yaml (already updated on disk) and the build-arch skills (already
updated on disk) carry the new layout. This rebake propagates those path
fixes into SKILL.md.

| Old Path (pre-D1) | New Path (post-D1, ADR 017) | Source |
|-------------------|------------------------------|--------|
| `{product_base}product/scope.yaml` | `{product_base}scope/scope.yaml` | C1 + derive-architecture-spec input |
| `{product_base}product/enriched-capabilities.yaml` | `{product_base}scope/enriched-capabilities.yaml` | C1 + derive-architecture-spec input |
| `{product_base}product/epics/` | `{product_base}scope/epics/` | C1 + derive-architecture-spec input |
| `{product_base}product/quality-profile.yaml` | `{product_base}specification/quality-profile.yaml` | C1 + derive-quality-standards input |
| `{product_base}product/project-profile.yaml` | `{product_base}specification/project-profile.yaml` | derive-quality-standards input + config.yaml |
| `{product_base}ux/personas.md` | `{product_base}experience/personas.md` | C2 |
| `{product_base}ux/design-spec.md` | `{product_base}experience/design-spec.md` | C2 + derive-architecture-spec input |
| `{product_base}ux/screens/` | `{product_base}experience/screens/` | C2 + derive-architecture-spec input |
| `{product_base}ux/flows/` | `{product_base}experience/flows/` | C2 |
| `{product_base}arch/architecture.yaml` | `{product_base}architecture/architecture.yaml` | C10 + derive-architecture-spec output |
| `{product_base}arch/quality-standards.yaml` | `{product_base}architecture/quality-standards.yaml` | C10 + derive-quality-standards output |
| `{product_base}arch/validation-architecture.yaml` | `{product_base}architecture/validation-architecture.yaml` | C10 + validate-architecture-spec output |
| `{product_base}arch/validation-full.yaml` | `{product_base}architecture/validation-full.yaml` | C10 + validate-architecture-spec output |
| `{product_base}arch/_checkpoints/build-arch/` | `{product_base}architecture/_checkpoints/build-arch/` | C10 + scriber whitelist |
| `{product_base}arch/_evidence/build-arch/` | `{product_base}architecture/_evidence/build-arch/` | C10 + scriber whitelist |
| `{product_base}arch/_status/build-arch.json` | `{product_base}architecture/_status/build-arch.json` | C10 + scriber whitelist |

Note: There are THREE migration categories, not two:
1. `{product_base}product/` (monolithic) → `scope/` or `specification/` (split by SDLC stage)
2. `{product_base}ux/` → `{product_base}experience/`
3. `{product_base}arch/` → `{product_base}architecture/`

## Semantic Map (Unchanged from 2026-04-14)

### Workflow Structure

**Structure A** — Full checkpoint flow with two human review gates.

```
Pre-flight
  └── Phase: Execution — Architecture Derivation
        Step 1 — derive-architecture-spec (tech-architect)
        Step 2 — validate-architecture-spec stage 1 (tech-architect)
  └── Phase: Checkpoint 1 — Architecture Review
        Step 3 — Tether/Vanish/Orbit on architecture.yaml
  └── Phase: Execution — Quality Standards Derivation
        Step 4 — derive-quality-standards (tech-architect)
        Step 5 — validate-architecture-spec stage 2 (tech-architect, both files)
  └── Phase: Checkpoint 2 — Quality Standards Review
        Step 6 — Tether/Vanish/Orbit on quality-standards.yaml
  └── Phase: Scenario Validation
        SCE-1 through SCE-6 (E2E persona checks)
  └── Phase: Evidence & Close
        Step 7 — scriber writes evidence + repo-orchestrator self-commit
```

### Agent Map

| Agent | Type | Phases | Domain |
|-------|------|--------|--------|
| `tech-architect` | domain | Execution Phase 1, Execution Phase 2 | Architecture derivation, quality-standards derivation, validation orchestration |
| `tech-designer` | domain (allowed alt for derive-quality-standards) | Execution Phase 2 | Same skill, alternate owner — kept available for QP-dimension splits |
| `scriber` | utility (exempt) | Checkpoints, Evidence | Whitelist-validated writes for checkpoint, evidence, status files |
| `repo-orchestrator` | utility (exempt) | Close | Self-commit per ADR 012 |

Domain agent count: 1-2 (tech-architect primary, tech-designer optional). Within ≤5 budget.

### Skill Inventory

| Skill | Used in Step | Status |
|-------|--------------|--------|
| `derive-architecture-spec` | Step 1 | existing — paths updated for D22 |
| `validate-architecture-spec` | Steps 2 + 5 | existing — paths updated for D22 |
| `derive-quality-standards` | Step 4 | existing — paths updated for D22 |
| `validate-kb-extension` | Pre-flight | existing — unchanged (KB catalog gate) |
| `write-evidence` | (via scriber, all phases) | existing — unchanged |

### Constraint → Enforcement Map (Unchanged Classifications)

| ID | Category | Enforcement Mechanism |
|----|----------|----------------------|
| C1 | pre-flight | Pre-flight check (specify-product LOCKED — paths updated to `scope/` + `specification/`) |
| C2 | pre-flight | Pre-flight check (design-exp LOCKED — paths updated to `experience/`) |
| C3 | artifact-verifiable | SE-2 (validate-architecture-spec) |
| C4 | artifact-verifiable | SE-3 |
| C5 | artifact-verifiable | SE-4 |
| C6 | artifact-verifiable | SE-5 |
| C7 | artifact-verifiable | SE-8 |
| C8 | artifact-verifiable | SE-9 |
| C9 | artifact-verifiable | SE-6 |
| C10 | structural + pre-flight | Agent boundary table + pre-flight write-access check (path now `architecture/`) |
| C11 | structural | Agent boundary table |
| C12 | artifact-verifiable | Steps 3, 6 scriber dispatches (checkpoint artifacts) |
| C13 | structural | Play structure — no implementation agents in pool |

### Failure Condition → Eval Map (Unchanged)

| ID | Covered By |
|----|------------|
| F1 | SE-1 (architecture), SE-7 (quality-standards) |
| F2 | SE-2 |
| F3 | SE-3, SCE-1 |
| F4 | SE-4 |
| F5 | SE-5 |
| F6 | SE-8 |
| F7 | SE-9 |
| F8 | SE-6 |
| F9 | Steps 3, 6 scriber dispatches + filesystem check |
| F10 | Agent boundary table + scriber whitelist enforcement |
| F11 | Agent boundary table — no implementation agents |

### Scenario → SCE Map (Unchanged)

| ID | Persona | Covered By |
|----|---------|------------|
| S1 | Technical Architect | SCE-1 |
| S2 | Engineering Manager | SCE-2 |
| S3 | Implementation Lead | SCE-3 |
| S4 | Security Architect | SCE-4 |
| S5 | Product Manager | SCE-5 |
| S6 | DevOps / Platform Engineer | SCE-6 |

## Agent Audit (Reused from 2026-04-14 Compile)

- `tech-architect` — 11/11 PASS. No re-audit required for D22 (no skill pool
  changes, no contract changes — only the path values in the contracts move).
- `tech-designer` — Pre-existing utility for the alternate derive-quality-
  standards owner path. Untouched by D22.
- `scriber` — Whitelist updated in 2026-04-14 to include `experience/` and
  `architecture/` (the post-D1 rename). No further changes.
- `repo-orchestrator` — Untouched by D22.

## Required Sections (G10) — All Present in Target SKILL.md

- Frontmatter
- Header
- Compiled From
- Role + Agent Boundaries
- Pre-flight (with three-way path split: scope, specification, experience, architecture)
- Workflow (with per-step JSON contracts using updated paths)
- Scenario Validation
- Evidence & Close
- Pause and Resume
- Compilation Metadata (with new intent_hash + drift notice for D22)

## Pipeline Step Skips (Justified)

- Step 2 (intent-crafter): SKIP — paths only, no schema/content change.
- Step 3 (skill-creator): SKIP — no skill modifications beyond the path
  corrections already on disk.
- Step 4 (agent audit): SKIP re-audit — agents unchanged. Reuse 2026-04-14
  audit results.
- Step 5 (workflow selection): PRESERVE Structure A — no complexity change.
- Step 6a (constraint classify): REUSE prior classifications. Verified above.
- Step 6b (evals-creator): RE-RUN — required because the play-internal eval
  language might subtly change to match updated artifact paths (paths in
  artifact_pattern fields). Run via Skill tool.
- Step 6c (compile SKILL.md): FULL REWRITE — every JSON contract path
  updated, plus Pre-flight bash split into three categories.
- Step 6d (coverage matrix): REGENERATE — must reflect new paths and the new
  intent_hash.
