iteration: 1
overall_verdict: PASS

evals:
  - id: E1
    description: capture-learning-fast play exists with correct structure
    verdict: PASS
    evidence: |
      core/components/plays/capture-learning-fast/SKILL.md exists with name:
      capture-learning-fast, user-invokable: false. Workflow section includes JSON
      contract invocation of knowledge-extractor with mode: "fast".
      reference/intent.yaml exists with C1, C2, C3, F1, F2, S1, S2 defined.

  - id: E2
    description: Ship SKILL.md has non-blocking Step 4 for capture-learning-fast
    verdict: PASS
    evidence: |
      Ship SKILL.md contains "Phase: Post-merge Learning Capture" with Step 4
      invoking capture-learning-fast. Explicit failure handling: "On any failure
      of capture-learning-fast: Log... Do NOT halt. Continue to Step 5." Old Step 4
      (Scenario Validation) renumbered to Step 5 with SCE-4 added. Old Step 5
      (Evidence & Close) renumbered to Step 6. Status file JSON includes
      "capture-learning-fast": {"status": "pending"} entry.

  - id: E3
    description: knowledge-extractor has FAST mode with correct input contract
    verdict: PASS
    evidence: |
      knowledge-extractor.md contains "### FAST Mode" section at line 293. Input
      contract includes mode: "fast", pr_diff, stm_base, product_base, issue_body.
      No context_base field in FAST mode input (explicitly stated: "No context_base
      required"). Output contract includes no_learnings and proposals_path fields.
      Operating modes table at line 51 includes FAST row.

  - id: E4
    description: Proposals staged to STM, never written to product LTM
    verdict: PASS
    evidence: |
      capture-learning-fast skill SKILL.md Constraint 1: "Never write to product LTM.
      product_base is read-only reference context. All output goes to
      {stm_base}/{issue}/evidence/capture-learning-fast/. Writing to any path under
      product_base is a hard constraint violation." Output path in knowledge-extractor
      FAST mode: {stm_base}/{issue}/evidence/capture-learning-fast/proposals.yaml.
      Intent.yaml C3 reinforces stage-only rule.

  - id: E5
    description: Trivial detection is content-based, not hardcoded
    verdict: PASS
    evidence: |
      knowledge-extractor FAST mode Step 2 describes agent-driven assessment:
      "Use agent judgment to assess the diff topology... Not hardcoded heuristic."
      Skill SKILL.md Constraint 3: "No hardcoded triviality patterns. Triviality
      is assessed by agent judgment on diff semantics, not by file extension lists
      or path prefixes." No regex or extension-based filters present.

  - id: E6
    description: Ship intent.yaml updated with C8 and S4
    verdict: PASS
    evidence: |
      core/components/plays/ship/reference/intent.yaml contains C8 (line 47):
      "After merge-pr completes, ship MUST invoke capture-learning-fast as a
      non-blocking step..." and S4 (line 112): persona Developer, scenario for
      post-merge learning with proposals staged and product LTM unchanged.
      Existing C1-C7 and S1-S3 verified unchanged.

  - id: E7
    description: Ship SKILL.md Compilation Metadata notes the direct-edit deviation
    verdict: PASS
    evidence: |
      Compilation Metadata section includes "Direct-edit deviation note: Step 4
      (capture-learning-fast) and Step 5-6 renumbering were added by enhance/#240
      as a direct edit to this compiled artifact. The intent_hash above reflects
      the pre-#240 intent.yaml and will not match..." sub_plays count updated
      from 4 to 5. scenario_evals updated from 3 to 4.
