recipe: ship
type: structure-c
note: >
  Ship is a Structure C recipe that chains L2 sub-recipes.
  It does not invoke skills directly — all skills are internal to sub-recipes.
sub_recipes:
  - name: commit-code
    path: core/components/recipes/commit-code/SKILL.md
    status: existing (L2 compiled)
  - name: create-pr
    path: core/components/recipes/create-pr/SKILL.md
    status: existing (L2 compiled)
  - name: merge-pr
    path: core/components/recipes/merge-pr/SKILL.md
    status: existing (L2 compiled)
