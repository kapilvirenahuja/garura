play: ship
type: structure-c
note: >
  Ship is a Structure C play that chains L2 sub-plays.
  It does not invoke skills directly — all skills are internal to sub-plays.
sub_plays:
  - name: commit-code
    path: core/components/plays/commit-code/SKILL.md
    status: existing (L2 compiled)
  - name: create-pr
    path: core/components/plays/create-pr/SKILL.md
    status: existing (L2 compiled)
  - name: merge-pr
    path: core/components/plays/merge-pr/SKILL.md
    status: existing (L2 compiled)
