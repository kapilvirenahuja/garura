recipe: discover-product
intent_hash: sha256:0474d47cc799c28e49220cb644bc6c950ecbe294cb261d7536323161f85666bc
generated_by: evals-creator (inline execution)

step_evals:
  - id: SE-1
    covers: F1
    step: 3
    statement: >
      The vision artifact contains at least 3 strategic goals, and a target
      audience is identifiable from the artifact content.
    pass: product.yaml strategic_goals has >=3 entries AND target_users (or equivalent for library type) non-empty.
    fail: >
      The vision artifact contains fewer than 3 strategic goals, or no target
      audience is identifiable from the artifact content.

  - id: SE-2
    covers: F2
    step: 2
    statement: The product domain is resolvable — not rejected by user.
    pass: product.yaml contains an unambiguous domain classification, OR user confirmed one after clarification.
    fail: >
      The product domain is unresolvable — the user rejected all proposed
      domain classifications and provided no alternative.

  - id: SE-3
    covers: F3
    step: 3
    statement: After vision drafting, a vision artifact exists at .meridian/product/discovery/.
    pass: product.yaml exists at .meridian/product/discovery/product.yaml as valid non-empty YAML.
    fail: >
      Vision drafting completed but no vision artifact exists at the
      expected path in .meridian/product/discovery/.

  - id: SE-4
    covers: F5
    step: 6
    statement: Validation completed without blocker-severity issues remaining after cycle-back limit.
    pass: Zero blockers remain, OR all blockers resolved within 1 (product) / 2 (library) cycle-backs.
    fail: >
      Validation completed with blocker-severity issues still present
      after the maximum permitted cycle-back iterations (1 for product
      type, 2 for library type).

  - id: SE-5
    covers: F7
    step: pre-flight
    statement: Recipe halts in pre-flight when product.yaml has status LOCKED.
    pass: Pre-flight detects LOCKED status and halts before any domain dispatch.
    fail: >
      The recipe was invoked for a product slug whose product.yaml
      already has status LOCKED. Re-discovery of an already-locked
      product is not permitted.

  - id: SE-6
    covers: F8
    step: 3
    statement: Type "library" product.yaml contains no fabricated market data.
    pass: When type=library, no invented competitors, no TAM/SAM/SOM, personas match stated consumers.
    fail: >
      A type "library" product.yaml contains fabricated market data —
      invented competitors with no basis in the intent, synthetic
      TAM/SAM/SOM estimates, or persona descriptions that do not match
      the intent's stated consumers.

  - id: SE-7
    covers: F9
    step: 4
    statement: >
      A checkpoint .md artifact is written before presenting results to the
      user, providing an audit trail for the approval flow.
    pass: >
      A checkpoint file exists at
      {product_base}/checkpoints/discover-product/{YYYYMMDD-HHMMSS}.md with
      Status: PENDING_APPROVAL before the user is prompted at Step 4.
    fail: >
      No checkpoint artifact was written before presenting results to
      the user — the approval flow has no audit trail.

  - id: SE-8
    covers: F10
    step: 3
    statement: When opportunity discovery was skipped, product.yaml still contains strategic goals.
    pass: product.yaml.strategic_goals is non-empty even when Step 2 was skipped per C12.
    fail: >
      Opportunity discovery was skipped but the product.yaml has no
      strategic goals — the vision was drafted without sufficient
      structure for downstream planning.

  - id: SE-9
    covers: C6
    step: 3
    statement: >
      All vision and product artifacts are written to .meridian/product/discovery/
      relative to the project root. Product STM is project-scoped.
    pass: product.yaml is at .meridian/product/discovery/product.yaml.
    fail: product.yaml is written outside .meridian/product/discovery/, or to a per-issue or global-home path.

  - id: SE-10
    covers: C7
    step: 3
    statement: >
      The vision artifact uses "Strategic Goals" terminology. OKR/Objectives/
      Key Results terminology is prohibited.
    pass: product.yaml field is strategic_goals and contains no "OKR", "Objectives", or "Key Results" strings.
    fail: product.yaml contains OKR-specific terminology.

  - id: SE-11
    covers: C8
    step: 4
    statement: >
      A checkpoint artifact is written before each user-facing pause with
      PENDING_APPROVAL. The review surface is the product.yaml path.
    pass: >
      For each user pause (vision review at Step 4, and resolution at
      Step 6 when blockers/warnings exist), a file at
      {product_base}/checkpoints/discover-product/{timestamp}.md exists
      with Status: PENDING_APPROVAL, references the product.yaml path,
      and makes no mention of an HTML brief as the review surface.
    fail: >
      No checkpoint artifact was written before a user pause, or the
      status is not PENDING_APPROVAL at checkpoint time.

  - id: SE-12
    covers: F13
    step: 3
    statement: >
      product.yaml at lock time has a profiles section with product_profile,
      nfr_profile, quality_profile, each with 7 dimensions.
    pass: profiles section present with all 3 sub-sections and 7 dimensions each (21 total), all with level 1-5.
    fail: >
      The product.yaml is locked without a profiles section, or the
      profiles section is missing any of the three sub-sections
      (product_profile, nfr_profile, quality_profile), or any profile
      has fewer than 7 dimensions.

  - id: SE-13
    covers: C13
    step: 3
    statement: product.yaml records an explicit type field ("product" or "library").
    pass: product.yaml has type set to "product" or "library" consistent with the intent classification.
    fail: product.yaml has no type field, or type does not match the intent's market-facing/internal classification.

  - id: SE-14
    covers: C14
    step: 6
    statement: >
      Pre-lock resolution gate writes pre-lock-resolutions.yaml before lock
      when blockers, warnings, or placeholder values are present.
    pass: >
      When validation returns issues or product.yaml contains placeholder
      values (TBD/unclear/to be determined/trailing ?), Step 6 presents a
      structured interview and writes pre-lock-resolutions.yaml at
      {product_base}/evidence/discover-product/pre-lock-resolutions.yaml
      before Step 7 (Lock) executes.
    fail: >
      The recipe locks an artifact while validation issues or placeholder
      values remain unresolved, OR no resolution interview is presented,
      OR pre-lock-resolutions.yaml is not written before lock.

  - id: SE-15
    covers: F11
    step: 7
    statement: Lock does not execute while any validation issue or placeholder remains unresolved.
    pass: Step 7 runs only after Step 6 confirms zero unresolved items (Branch A auto or RESOLVED path).
    fail: >
      The recipe locks an artifact (changes status to LOCKED) while
      validation issues of any severity (blocker or warning) remain
      unresolved, or while product.yaml contains placeholder field
      values (TBD, unclear, to be determined, trailing ?).

  - id: SE-16
    covers: C15, F12
    step: 6
    statement: >
      No accept-risk bypass exists in the blocker branch. Only RESOLVED or
      Vanish are offered from blocker detection.
    pass: >
      Step 6 Branch C presents only RESOLVED and Vanish as response options.
      No "accept risk" or "proceed despite blockers" option exists.
    fail: >
      The recipe offers a bypass mechanism that allows the user to skip
      unresolved blockers via Tether — presenting an accept-risk or
      proceed-despite-blockers option that reaches lock without resolving
      each blocker individually.

  - id: SE-17
    covers: C16, F14
    step: 3
    statement: >
      Profiles are derived from canonical LTM definitions in PP -> NFR -> QP cascade.
    pass: >
      Step 3 contract includes config.profile_knowledge_path pointing at
      ~/.meridian/core/memory/knowledge/project-profiling/. product.yaml
      profiles.product_profile keys are pp1_user_sophistication .. pp7_industry_vertical,
      profiles.nfr_profile keys are nfr1_risk .. nfr7_data_sensitivity, and
      profiles.quality_profile keys are qp1_testing_depth .. qp7_security_testing.
      NFR rationale references PP reasoning; QP rationale references PP and/or NFR.
    fail: >
      Profiles were derived without reading LTM dimension definitions —
      the agent invented dimensions or levels that don't match the
      canonical definitions in product-profile.md, nfr-profile.md, or
      quality-profile.md.

  - id: SE-18
    covers: C17
    step: 3b
    statement: Profiles presented to user for validation before vision review checkpoint.
    pass: >
      Status file shows derive-profiles completed before
      checkpoint-vision started; any user adjustments were written back
      to product.yaml before the vision review checkpoint.
    fail: Profiles were not presented for validation before the vision review checkpoint.

  - id: SE-19
    covers: C18, F15
    step: 3
    statement: >
      Every capability the source document (BRD/PRD/intent) explicitly
      defers or excludes appears in product.yaml scope.out_of_scope —
      never in scope.in_scope.
    pass: >
      No capability marked deferred/excluded in the source appears in
      scope.in_scope. Ambiguities are surfaced as decision_points.
    fail: >
      product.yaml scope.in_scope contains a capability that the source
      document (BRD, PRD, or user intent) explicitly defers or excludes
      from the current phase — the vision drafting agent reinterpreted a
      deferral as in-scope without surfacing the conflict.

  - id: SE-20
    covers: C19, F16
    step: 3
    statement: >
      When the agent's reasoning would change a source requirement, the
      conflict is surfaced as a decision_point (original_requirement,
      proposed_alternative, rationale) rather than applied silently.
    pass: >
      Any requirement in product.yaml that materially differs from the
      source has a corresponding entry in product.yaml.decision_points
      with all three fields populated.
    fail: >
      product.yaml contains a requirement that materially differs from the
      source document due to unsurfaced risk-based or strategic reasoning
      by the vision drafting agent — the agent silently changed a source
      requirement without recording a decision_point for user resolution.

  - id: SE-21
    covers: C9
    step: 6
    statement: Cycle-back iteration limits enforced by type (1 product, 2 library).
    pass: status file iteration_count never exceeds 1 (product) or 2 (library); halts with F5 at limit.
    fail: Recipe permits more cycle-backs than the type-dependent limit allows.

  - id: SE-22
    covers: C4
    step: all
    statement: All product-domain work is delegated to a domain agent.
    pass: Recipe executor invokes product-strategist (and judge for validation) for all product-domain tasks; no inline domain work.
    fail: Recipe performs product analysis, vision drafting, or validation directly without an agent dispatch.

  - id: SE-23
    covers: C5
    step: all
    statement: Domain agent dispatches stay within the budget.
    pass: >
      Product-type invocation uses at most 3 base + 2 cycle-back (<=5).
      Library-type invocation uses at most 2 base + 2 cycle-back (<=4).
      repo-orchestrator dispatches are not counted.
    fail: Domain agent dispatches exceed the budget.

  - id: SE-24
    covers: C11
    step: 2
    statement: Ambiguous domain triggers clarification, not silent continuation.
    pass: When product-strategist returns domain_clarification_needed, user is presented with candidates before re-dispatch.
    fail: Product-domain work proceeds with an ambiguous domain classification.

  - id: SE-25
    covers: C12
    step: 1
    statement: Opportunity discovery skipped for library/component intents per three-signal assessment.
    pass: skip_opportunity is true when all three signals match; Step 2 is not dispatched in that case.
    fail: Step 2 runs for an internal-library intent, or Step 2 is skipped for a clearly market-facing intent.

scenario_evals:
  - id: SCE-1
    covers: S1
    statement: >
      The vision artifact (product.yaml) presented at the first checkpoint
      with its file path can be evaluated — including for internal libraries
      where market sections are absent and the YAML contains technical context.
    pass: >
      At the first checkpoint, product.yaml contains strategic goals and
      positioning derived from the intent. For type=product, market
      positioning sections are present; for type=library, market sections
      are absent and technical_context renders instead. The checkpoint
      message references the product.yaml path as the review surface.
      Optionally, /briefs may be used to generate an HTML rendering.
    fail: >
      product.yaml does not render strategic goals or positioning, OR a
      library-type artifact contains market sections, OR the checkpoint
      does not present the product.yaml path for review.

  - id: SCE-2
    covers: S2
    statement: >
      A LOCKED vision artifact can serve as authoritative input for roadmap
      planning regardless of product type.
    pass: >
      product.yaml has status=LOCKED, strategic_goals with IDs (>=3),
      success metrics, and defined scope boundaries — sufficient to
      derive engineering milestones without ambiguity.
    fail: Any of those are missing or ambiguous.

  - id: SCE-3
    covers: S3
    statement: Validation results shown inline enable lock-readiness determination.
    pass: Validation output includes completeness score, checklist with per-item status, and issues categorized by severity.
    fail: Any of those are missing.

  - id: SCE-4
    covers: S4
    statement: >
      A LOCKED library-type product.yaml provides strategic goals with IDs,
      value proposition, and assumptions sufficient for roadmap planning even
      without market sections.
    pass: type=library, status=LOCKED, strategic_goals with IDs (>=3), value_proposition, assumptions.
    fail: Any of those are missing.

  - id: SCE-5
    covers: S5
    statement: >
      Validation blockers trigger a structured resolution interview listing
      each blocker individually, resolutions are recorded in
      pre-lock-resolutions.yaml, re-validation occurs, and lock proceeds
      only when re-validation confirms resolution — with no bypass.
    pass: >
      Blockers listed individually in numbered format; RESOLVED answers
      written to pre-lock-resolutions.yaml with timestamp; judge re-invoked
      after RESOLVED; lock runs only if re-validation has no blockers; no
      bypass option is available.
    fail: Any of the above violated.

  - id: SCE-6
    covers: S6
    statement: >
      A LOCKED product.yaml with a profiles section containing all three
      profiles (PP/NFR/QP) with levels and rationale is sufficient for
      architecture planning.
    pass: profiles section contains 21 dimensions (7 per profile), each with level 1-5 and non-empty rationale.
    fail: The profiles section is missing any sub-section, dimension, level, or rationale.

  - id: SCE-7
    covers: S7
    statement: >
      Derived profiles are presented at the vision review checkpoint with
      adjustment capability, and the cascade (PP -> NFR -> QP) is evident.
    pass: >
      Step 3b profile presentation exists and runs before the Step 4
      checkpoint; the user can adjust any dimension using DIM=level format;
      adjustments are applied to product.yaml before the checkpoint; cascade
      order is reflected in rationale text.
    fail: Profiles not presented before the vision review checkpoint, adjustment mechanism absent, or cascade order not evident.

  - id: SCE-8
    covers: S8
    statement: >
      For a product.yaml produced from a BRD that defers capabilities,
      every deferred capability appears in scope.out_of_scope (not in_scope)
      and any source ambiguities are surfaced as decision_points at the
      vision review checkpoint.
    pass: >
      Scope fidelity verified; any conflicts captured as decision_points
      at the Step 4 checkpoint for explicit resolution by reading
      product.yaml.
    fail: A deferred capability appears in scope.in_scope, or source ambiguities were resolved silently.

  - id: SCE-9
    covers: S9
    statement: >
      decision_points in product.yaml are reviewable at the vision review
      checkpoint — each shows original_requirement, proposed_alternative,
      and rationale for explicit human resolution.
    pass: >
      Each decision_point has all three fields populated; at the Step 4
      vision review checkpoint, the user reads product.yaml and makes an
      explicit choice; the final product.yaml reflects the user's decision.
    fail: >
      Requirements that differ from the source have no decision_point,
      or decision_points are not surfaced at the vision review checkpoint.

counts:
  step_evals: 25
  scenario_evals: 9
