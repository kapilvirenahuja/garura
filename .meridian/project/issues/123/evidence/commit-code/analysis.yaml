branch:
  name: feature/123-briefs-recipe-sync-droids-factory-support
  issue: "123"
  is_hotfix: false

change_summary:
  total_files: 18
  staged: 0
  unstaged: 2
  untracked: 16

groups:
  - id: G1
    issue: "123"
    type: feat
    scope: components
    subject: "add briefs recipe, sync-droids skill, and Factory Droid support"
    confidence: high
    files:
      - path: AGENTS.md
        description: "New Factory Droid agent instructions — mirrors CLAUDE.md for .factory/ deployment context"
      - path: core/config.yaml
        description: "Added factory: and global-factory: config sections for project-mode and global Factory Droid paths"
      - path: .gitignore
        description: "Added .factory/skills/ and .factory/droids/ to gitignore (deployed content, not source)"
      - path: core/components/recipes/briefs/SKILL.md
        description: "New briefs recipe — checksum-driven brief regeneration via doc-builder"
      - path: core/components/recipes/briefs/reference/intent.yaml
        description: "briefs recipe intent — constraints, failure conditions, scenarios"
      - path: core/components/skills/sync-droids/SKILL.md
        description: "New sync-droids skill — syncs core/components/ to .factory/ or ~/.factory/ with Claude Code→Factory Droid transformation"
      - path: core/components/skills/sync-droids/scripts/sync.sh
        description: "sync-droids shell script — handles clean, transform, copy, memory sync"
      - path: core/components/skills/sync-droids/templates/sync-output.md
        description: "sync-droids output template"

  - id: G2
    issue: "123"
    type: chore
    scope: stm
    subject: "record create-recipe evidence and spec for briefs recipe"
    confidence: high
    files:
      - path: .meridian/project/issues/_pending/evidence/create-recipe/briefs/agent-audit-doc-builder.md
        description: "STM evidence — doc-builder agent audit from create-recipe run"
      - path: .meridian/project/issues/_pending/evidence/create-recipe/briefs/coverage-matrix.md
        description: "STM evidence — coverage matrix from create-recipe run"
      - path: .meridian/project/issues/_pending/evidence/create-recipe/briefs/evals.yaml
        description: "STM evidence — evals generated by create-recipe"
      - path: .meridian/project/issues/_pending/evidence/create-recipe/briefs/skill-manifest.yaml
        description: "STM evidence — skill manifest from create-recipe run"
      - path: .meridian/specs/2026-03-24-briefs-recipe-checksum-driven-brief-regeneration.md
        description: "Spec document for briefs recipe — describes behavior and implementation approach"

risks:
  sensitive_files: []
  breaking_changes: []
  ambiguous_types: []
  hotfix_branch: false
  notes:
    - id: R1
      severity: medium
      file_pattern: ".factory/init.sh, .factory/library/*.md, .factory/services.yaml"
      description: >
        Five files in .factory/ are untracked and not covered by .gitignore.
        Current .gitignore only ignores .factory/skills/ and .factory/droids/.
        AGENTS.md states '.factory/ is NOT tracked in the repository — it is gitignored',
        but these root-level files escape that rule. They appear to be project-mode
        Factory scaffolding (init.sh, library/, services.yaml).
        Decision required: either extend .gitignore to cover these paths, or include
        them in a commit as intentional project-mode configuration.

recommendation:
  checkpoint_needed: false
  reason: >
    All changes map to issue #123 with high confidence.
    Two groups identified: G1 (feat — core feature components) and G2 (chore(stm) — operational STM evidence).
    No sensitive files. No breaking changes. Not a hotfix branch.
    One medium-risk ambiguity exists for .factory/ root files (R1) — caller should resolve
    before including or excluding those paths from G1.
