# Ship Evidence — Issue #208

**Date:** 2026-04-13
**Feature branch:** feature/208-review-pr-recipe (deleted)
**PR:** #211 — MERGED
**Merge SHA:** 211c2075c3d84edce366c518a46e2402ecff1b22
**Final state:** on main, up to date with remote, feature branch gone

## Pipeline Steps

| Step | Status | Notes |
|---|---|---|
| commit-code | completed | 2 commits: feat(recipes) 6bb33dd, refactor(standards) 0a40889 |
| commit-code evidence self-commit | completed | d83e22b |
| create-pr | completed | PR #211 |
| config + STM interim commits | completed | a58e0db (config), 72fb8fa (stm) |
| review-pr | partial → bypassed | Ran Steps 0–2; user switched `review-pr.bypass=true` mid-flight for solo-developer workflow |
| merge-pr | completed | merge SHA 211c2075, branch deleted local+remote |

## review-pr First Real Run — Findings

review-pr executed through Step 2 (scoped-quality-eval) before bypass was set:

- **Scan coverage:** 1.0 (67 changed paths, 7 relevant standards)
- **P1:** 0 (SEC-19 matches were false positives — illustrative YAML in finding-schema.md)
- **P2:** 2 (ARCH-07 — github.com URLs in STM evidence YAML; mechanical regex fire, not remediable)
- **P3:** 1 (DOC-04 — ADR 016 addition; positive signal)
- **P4:** 11 (docs/adr path matches + 8 HTML 100%-similarity renames)
- **Eval compliance:** SE-1, SE-2, SE-3 PASS

Observed limitation: `quality-check-scoped` fires on rename destination paths even when the rename is 100% similarity (zero content change). File-move refactors produce noisy P4/P3 findings. Follow-up candidate.

## review-pr Bypass Decision

tech-designer flagged during Step 1 that `fallback_reviewers: []` + single committer would force F3 halt at Step 4 (reviewer-selection excludes PR author). User direction: this repo is solo-developer, skip PR reviews entirely. Config updated in-flight:

```yaml
review-pr:
  bypass: true
```

Committed as a58e0db. Ship pre-flight on future runs will see `bypass=true` and skip Step 2.5 cleanly.

## Scenario Evals

- **SCE-1 (Developer):** PASS — on main, up to date, feature branch absent
- **SCE-2 (Code Reviewer):** PASS — PR #211 MERGED, merge SHA on main, linked to #208, quality checklist in PR body
- **SCE-3 (review-pr gate):** N/A — bypass=true at merge time

## Known Follow-ups

- #209 — formats/ standardization (standardize all user-facing output shapes)
- #210 — standards/agent-lifecycle → standards/rules rename
- review-pr noise on 100% similarity renames (potential issue)
- review-pr solo-developer reviewer-selection (mitigated by bypass for this repo; other repos may need fallback_reviewers)
