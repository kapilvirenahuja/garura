# Phoenix OS Configuration

project:
  name: Phoenix OS
  type: Agentic Framework

platform: github

github:
  repo: https://github.com/kapilvirenahuja/phoenix-os.git

# Source of truth locations
components:
  skills: ./core/components/skills/
  recipes: ./core/components/recipes/
  agents: ./core/components/agents/
  memory: ./core/components/memory/

# Claude Code derived locations (populated by /sync-claude)
claude:
  skills: ./.claude/skills/
  agents: ./.claude/agents/

# Short-Term Memory (STM) — Issue-centric artifacts (ADR 008)
stm:
  base-path: .phoenix-os/
  pending-path: .phoenix-os/_pending/
  structure:
    docs: "{issue-number}/docs/"
    evidence: "{issue-number}/evidence/"
    checkpoint: "{issue-number}/checkpoint/{recipe-name}/{YYYYMMDD-HHMMSS}.md"
