---
name: discover-product
description: Discover product vision, strategic goals, and market positioning — presents the product.yaml path for human review at each checkpoint
user-invocable: true
---

# discover-product

Given a product idea or problem statement, discover the market opportunity, draft a product vision with strategic goals and market positioning, validate the vision for completeness, and lock it when ready. The recipe runs as a single invocation with inline checkpoints. It produces a vision artifact (product.yaml) for human review, validates completeness, and locks the vision — all in one flow. Users may run `/briefs` separately to generate an HTML rendering of the artifact if desired. All product artifacts are stored in project-scoped product STM. Supports both market-facing products and internal libraries.

## Compiled From

This recipe was compiled from `reference/intent.yaml` by `/create-recipe`.
Intent defines constraints (C1, C4-C9, C11-C19), failure conditions (F1-F3, F5, F7-F16), and scenarios (S1-S9).
To modify this recipe, update intent.yaml and re-run `/create-recipe --rebake discover-product`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the workflow. You delegate domain tasks to agents via JSON contracts — never execute domain work directly.

**Forbidden:** Direct product analysis, vision drafting, or validation. Direct `git` commands for commits. All domain work goes through agents.

**Agent boundaries:**

| Agent | Domain | Steps |
|-------|--------|-------|
| `product-strategist` | Opportunity discovery, vision drafting | 2, 3 |
| `judge` | Context-isolated coverage check and vision validation | 3a, 5 |
| `repo-orchestrator` | Evidence self-commit (utility, exempt from C5) | 8 |

**Path resolution:**
All `.meridian/` paths are relative to the project root. NEVER expand to `~/.meridian/`. Product artifacts go to `.meridian/product/discovery/` (C6). Evidence goes to `.meridian/product/evidence/discover-product/`. Checkpoints go to `.meridian/product/checkpoints/discover-product/`.

## Pre-flight

Execute these checks before any domain work.

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| Intent text provided and >5 meaningful words | C1 | Hard halt |
| Derive slug from intent text | — | Hard halt if empty |
| Check if `{product_base}/discovery/product.yaml` exists with status LOCKED | F7 | Hard halt |
| Check for status file for resume | — | Resume if found |

```
product_base = ".meridian/product"
intent = "{user-provided intent text}"

# C1: halt if empty or word count <= 5
word_count = count_words(intent)
if word_count <= 5:
  halt("Intent must contain more than 5 meaningful words")

# Derive slug from intent
slug = slugify(intent)

# F7: halt if already locked
if file_exists("{product_base}/discovery/product.yaml"):
  product = read_yaml("{product_base}/discovery/product.yaml")
  if product.status == "LOCKED":
    halt("Product {slug} is already LOCKED. Re-discovery not permitted. (F7)")

# Resume check
status_path = "{product_base}/status/discover-product.json"
if file_exists(status_path):
  resume from first incomplete step (see Pause and Resume)
```

---

## Workflow

9 steps, single flow. One invocation: assess → discover → draft → coverage-check → derive-profiles → checkpoint → validate → pre-lock-gate → lock → evidence → summary.

### Step 1 — Assess Intent

Owner: recipe (no agent call)
Depends on: pre-flight

Before any agent calls, assess the intent to determine product type (C13) and whether opportunity discovery should be skipped (C12).

**Three-signal assessment (C12):**

Evaluate the intent against three signals:
1. The intent names specific consumers rather than market segments
2. The intent describes a technical interface boundary rather than a user problem
3. No market-facing end users are identifiable

```
product_type = "product"    # default
skip_opportunity = false

if (intent names specific consumers, NOT market segments)
   AND (intent describes technical interface boundary, NOT user problem)
   AND (no market-facing end users identifiable):
  product_type = "library"
  skip_opportunity = true

# C13: Record product_type for product.yaml type field
# C12: Record skip_opportunity for Step 2 conditional
```

**When `skip_opportunity = true`:**
- Skip Step 2 entirely
- Pass `raw_intent` directly to Step 3 (draft-vision) instead of `market_context`
- C5 domain agent budget: 2 base (draft + validate) plus up to 2 cycle-back = 4 max

**When `skip_opportunity = false`:**
- Proceed to Step 2 normally
- C5 domain agent budget: 3 base (discover + draft + validate) plus up to 1 cycle-back = 4 max

---

### Step 2 — Discover Opportunity

Owner: `product-strategist`
Depends on: Step 1
Skill: `discover-product-opportunity`
**Conditional:** Skip when `skip_opportunity = true` per C12.

```json
{
  "intent_path": "core/components/recipes/discover-product/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "stm": {
    "input": {},
    "output": {
      "product_yaml_path": "{product_base}/discovery/product.yaml"
    }
  },
  "config": {
    "problem_statement": "{intent text}",
    "product_type": "{product_type}",
    "slug": "{slug}"
  },
  "task_id": "discover-opportunity",
  "ltm_context": {
    "project_base": ".meridian/product/discovery/",
    "core_base": "~/.meridian/core/memory/",
    "query_domains": ["domain-taxonomy", "standards"],
    "locked_artifacts": []
  }
}
```

Agent invokes `discover-product-opportunity` → returns market context with problem, target users, competitors, market size, differentiators, risks. Writes market context section of `product.yaml` to STM.

**Domain clarification handling (C11):**
If agent returns `domain_clarification_needed`:
1. Present candidate domains to user
2. Parse response, re-invoke with confirmed domain
3. Re-invocation does NOT count against C5 domain agent budget
4. If user rejects all candidates → hard halt (F2)

**Step 2 Evals:**

- **SE-2 (F2):** The product domain is resolvable — the product.yaml contains an unambiguous domain classification that was not rejected by the user. Pass: product.yaml contains a non-empty domain field with a classification value present, OR the user confirmed a domain after clarification. Fail: The product domain is unresolvable — the user rejected all proposed domain classifications and provided no alternative.

- **SE-24 (C11):** Ambiguous domain triggers clarification, not silent continuation. Pass: When product-strategist returns domain_clarification_needed, the user is presented with candidates before re-dispatch. Fail: Product-domain work proceeds with an ambiguous domain classification.

- **SE-25 (C12):** Opportunity discovery skipped for library/component intents per three-signal assessment. Pass: skip_opportunity is true when all three signals match; Step 2 is not dispatched in that case. Fail: Step 2 runs for an internal-library intent, or Step 2 is skipped for a clearly market-facing intent.

---

### Step 3 — Draft Vision

Owner: `product-strategist`
Depends on: Step 2 (or Step 1 if Step 2 skipped)
Skill: `draft-product-vision`

**Contract when Step 2 executed (product type):**

```json
{
  "intent_path": "core/components/recipes/discover-product/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "stm": {
    "input": {
      "market_context": "{market_context output from Step 2 agent}"
    },
    "output": {
      "product_yaml_path": "{product_base}/discovery/product.yaml"
    }
  },
  "config": {
    "product_type": "product",
    "slug": "{slug}",
    "domain": "{confirmed_domain from Step 2}",
    "profile_knowledge_path": "~/.meridian/core/memory/knowledge/project-profiling/"
  },
  "task_id": "draft-vision",
  "ltm_context": {
    "project_base": ".meridian/product/discovery/",
    "core_base": "~/.meridian/core/memory/",
    "query_domains": ["domain-taxonomy", "standards"],
    "locked_artifacts": []
  }
}
```

**Contract when Step 2 skipped (library type, per C12):**

```json
{
  "intent_path": "core/components/recipes/discover-product/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "stm": {
    "input": {},
    "output": {
      "product_yaml_path": "{product_base}/discovery/product.yaml"
    }
  },
  "config": {
    "product_type": "library",
    "raw_intent": "{intent text}",
    "slug": "{slug}",
    "profile_knowledge_path": "~/.meridian/core/memory/knowledge/project-profiling/"
  },
  "task_id": "draft-vision",
  "ltm_context": {
    "project_base": ".meridian/product/discovery/",
    "core_base": "~/.meridian/core/memory/",
    "query_domains": ["domain-taxonomy", "standards"],
    "locked_artifacts": []
  }
}
```

Agent invokes `draft-product-vision` → writes `{product_base}/discovery/product.yaml` (full file — market context + vision sections consolidated if product type, or vision-only sections if library type; status: DRAFT, type: "{product_type}"). Profiles section is populated via the PP → NFR → QP cascade using the canonical LTM definitions (C16).

**Step 3 Evals:**

- **SE-1 (F1):** The vision artifact contains at least 3 strategic goals AND a target audience is identifiable. Pass: product.yaml.strategic_goals has 3+ entries AND target_users (or equivalent for library type) is non-empty. Fail: Fewer than 3 strategic goals, or no identifiable target audience.

- **SE-3 (F3):** After vision drafting, product.yaml exists at the expected path. Pass: `.meridian/product/discovery/product.yaml` exists as valid non-empty YAML. Fail: No vision artifact at the expected path.

- **SE-6 (F8):** A type "library" product.yaml contains no fabricated market data. Pass: When type=library, no invented competitors, no TAM/SAM/SOM, personas match stated consumers. Fail: Fabricated market data present in a library-type artifact.

- **SE-8 (F10):** When opportunity discovery was skipped, product.yaml still contains strategic goals. Pass: strategic_goals non-empty even when Step 2 was skipped per C12. Fail: No strategic goals after a skipped Step 2.

- **SE-9 (C6):** All vision and product artifacts are written to `.meridian/product/discovery/`. Pass: product.yaml at `.meridian/product/discovery/product.yaml`. Fail: product.yaml written outside that directory, or written to a per-issue or global-home path.

- **SE-10 (C7):** Vision artifact uses "Strategic Goals" terminology; OKR terminology prohibited. Pass: product.yaml field is `strategic_goals`; no "OKR", "Objectives", or "Key Results" strings present. Fail: OKR-specific terminology present.

- **SE-12 (F13):** product.yaml has a profiles section with all three sub-sections and 7 dimensions each. Pass: `profiles.product_profile`, `profiles.nfr_profile`, `profiles.quality_profile` present; each has 7 dimensions with level 1-5. Fail: Any sub-section missing, or any profile has fewer than 7 dimensions.

- **SE-13 (C13):** product.yaml records an explicit type field. Pass: type is "product" or "library" consistent with the intent classification. Fail: type field missing or inconsistent with intent.

- **SE-17 (C16, F14):** Profiles derived from canonical LTM definitions in sequential PP → NFR → QP cascade. Pass: Step 3 contract includes `config.profile_knowledge_path` pointing at the canonical `project-profiling/` directory; product.yaml profile dimension keys match the canonical set exactly (`pp1_user_sophistication`..`pp7_industry_vertical`, `nfr1_risk`..`nfr7_data_sensitivity`, `qp1_testing_depth`..`qp7_security_testing`); NFR rationale references PP-derived reasoning; QP rationale references PP and/or NFR-derived reasoning. Fail: Canonical set violated, profile_knowledge_path omitted, or cascade reasoning not evident.

- **SE-19 (C18, F15):** Source-document scope decisions preserved with fidelity. Pass: Every capability the source explicitly defers/excludes appears in `scope.out_of_scope`; none appear in `scope.in_scope`; ambiguities are surfaced as decision_points. Fail: A deferred capability appears in `scope.in_scope`, or ambiguity was resolved silently.

- **SE-20 (C19, F16):** Conflicts between agent reasoning and source requirements surfaced as decision_points. Pass: Any requirement in product.yaml that materially differs from the source has a corresponding `decision_points` entry with original_requirement, proposed_alternative, and rationale. Fail: A requirement differs from source without a corresponding decision_point.

- **SE-22 (C4):** All product-domain work is delegated to a domain agent. Pass: Recipe executor invokes product-strategist (and judge for validation) for all product-domain tasks; no inline domain work. Fail: Recipe performs product analysis/drafting/validation directly without an agent dispatch.

- **SE-23 (C5):** Domain agent dispatches stay within budget. Pass: Product-type invocation uses ≤3 base + 2 cycle-back (≤5); library-type invocation uses ≤2 base + 2 cycle-back (≤4); repo-orchestrator dispatches not counted. Fail: Dispatches exceed the type-dependent budget.

---

### Step 3a — Reverse-Coverage Check (Context-Isolated)

Owner: `judge`
Depends on: Step 3

**Context isolation:** The judge receives ONLY the product.yaml path and the problem_statement (from config). It does NOT receive market context artifacts, product-strategist drafting notes, domain research, or any intermediate reasoning.

```json
{
  "intent_path": "core/components/recipes/discover-product/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "mode": "check-input-output-coverage",
  "artifact_paths": {
    "input_path": "{product_base}/discovery/product.yaml",
    "output_path": "{product_base}/discovery/product.yaml"
  },
  "check_type": "problem-to-vision",
  "config": {
    "problem_statement": "{original intent text from pre-flight}",
    "market_context_path": "{product_base}/discovery/market-context.yaml (if Step 2 ran, null otherwise)"
  },
  "stm": {
    "output": {
      "coverage_check_path": "{product_base}/discovery/coverage-check.yaml"
    }
  },
  "task_id": "check-coverage"
}
```

The judge extracts facets from the problem_statement and market context (target users, risks, differentiators, problem dimensions) and verifies that product.yaml addresses each:
- **covered**: strategic goal, value proposition, or scope element traces to this input facet
- **partial**: facet acknowledged but not reflected in strategic goals or metrics
- **dropped**: input facet has no representation in product.yaml
- **drifted**: product.yaml contains strategic goals or scope that don't trace to any input facet

**On dropped elements:** If any core facet of the problem_statement is completely dropped, log in evidence and flag for the user at the next checkpoint (Step 4) — this is not a hard halt because the vision is still in DRAFT.

Update status file: `check-coverage → completed`.

---

### Step 3b — Derive & Validate Project Profiles

Owner: recipe (no agent call — profiles already written to product.yaml by Step 3)
Depends on: Step 3

After `draft-product-vision` writes product.yaml with the `profiles` section, present all three derived profiles to the user for validation before the vision review checkpoint (C17).

Read product.yaml profiles section and present:

```markdown
## Project Profiles: {slug}

The following profiles were derived from your product vision. Review and adjust any dimension levels.

### Product Profile (PP)
| Dimension | Level | Label | Rationale |
|-----------|-------|-------|-----------|
| PP-1: User Sophistication | {level} | {label} | {rationale} |
| PP-2: UX Maturity | {level} | {label} | {rationale} |
| PP-3: Persona Complexity | {level} | {label} | {rationale} |
| PP-4: Geographic Scope | {level} | {label} | {rationale} |
| PP-5: Integration Density | {level} | {label} | {rationale} |
| PP-6: Delivery Ambition | {level} | {label} | {rationale} |
| PP-7: Industry Vertical | {level} | {label} | {rationale} |

### NFR Profile (NFR)
| Dimension | Level | Label | Rationale |
|-----------|-------|-------|-----------|
| NFR-1: Risk | {level} | {label} | {rationale} |
| NFR-2: Security | {level} | {label} | {rationale} |
| NFR-3: Performance | {level} | {label} | {rationale} |
| NFR-4: Availability | {level} | {label} | {rationale} |
| NFR-5: Compliance | {level} | {label} | {rationale} |
| NFR-6: Scalability | {level} | {label} | {rationale} |
| NFR-7: Data Sensitivity | {level} | {label} | {rationale} |

### Quality Profile (QP)
| Dimension | Level | Label | Rationale |
|-----------|-------|-------|-----------|
| QP-1: Testing Depth | {level} | {label} | {rationale} |
| QP-2: Code Quality Standards | {level} | {label} | {rationale} |
| QP-3: Documentation Level | {level} | {label} | {rationale} |
| QP-4: CI/CD Maturity | {level} | {label} | {rationale} |
| QP-5: Observability Maturity | {level} | {label} | {rationale} |
| QP-6: Accessibility Standard | {level} | {label} | {rationale} |
| QP-7: Security Testing | {level} | {label} | {rationale} |

---
Review the profiles above. Type **Tether** to accept, or provide adjustments in format:
`PP-1=4, NFR-3=2, QP-5=3` (comma-separated dimension=level pairs)
```

Parse response:
- `Tether`/`tether` → profiles confirmed, proceed to Step 4
- Adjustment string → parse `DIM=level` pairs, update product.yaml profiles section, re-present all three tables
- `Vanish`/`vanish` → halt

**Step 3b Evals:**

- **SE-18 (C17):** Profiles presented to user for validation before the vision review checkpoint. Pass: status file shows `derive-profiles` completed before `checkpoint-vision` started; any user adjustments were written back to product.yaml before the checkpoint. Fail: Profiles not presented before the vision review checkpoint, or adjustments were not applied.

---

### Step 4 — Checkpoint: Vision Review

Owner: recipe (no agent call)
Depends on: Step 3b

Write checkpoint artifact to `{product_base}/checkpoints/discover-product/{YYYYMMDD-HHMMSS}.md` with `Status: PENDING_APPROVAL` (C8). The checkpoint artifact is the audit trail required by F9 — write it BEFORE presenting to the user.

```
checkpoint_path = "{product_base}/checkpoints/discover-product/{timestamp}.md"
write_file(checkpoint_path, """
---
checkpoint: vision-review
slug: {slug}
product_yaml: {product_base}/discovery/product.yaml
status: PENDING_APPROVAL
created_at: {timestamp}
---

# Vision Review Checkpoint — {slug}

Review surface: product.yaml at {product_base}/discovery/product.yaml

Profiles confirmed at Step 3b. Coverage-check output at
{product_base}/discovery/coverage-check.yaml.

Any dropped problem facets from Step 3a are listed below for user attention.
""")
```

Present to user:

```markdown
## Product Discovery: {slug}

**Product YAML:** `{product_base}/discovery/product.yaml`
**Checkpoint record:** `{checkpoint_path}`

The review surface for this checkpoint is the product.yaml file at the path above. Open it in your editor to review the vision, strategic goals, scope, and derived profiles. If you would like an HTML rendering, you may run `/briefs` separately after approval.

If the coverage-check flagged any dropped problem facets, or if product.yaml contains any `decision_points`, address them here — either approve as-is or reply with plain-text feedback describing the changes you want.

---

Type **Tether** to approve and proceed to validation, or **Vanish** to halt.
Optionally include plain-text feedback for targeted revisions.
```

**Recipe pauses here.** User reviews product.yaml and returns with Tether/Vanish plus any optional plain-text feedback.

Parse response:
- `Tether`/`tether` → update checkpoint to APPROVED, proceed to Step 5
- `Vanish`/`vanish` → update checkpoint to REJECTED, halt
- If feedback text is provided → store for potential cycle-back at Step 6

**Step 4 Evals:**

- **SE-7 (F9):** A checkpoint .md artifact is written before presenting results to the user, providing an audit trail. Pass: a file exists at `{product_base}/checkpoints/discover-product/{YYYYMMDD-HHMMSS}.md` with `Status: PENDING_APPROVAL` before the user is prompted at Step 4. Fail: No checkpoint artifact was written before the pause.

- **SE-11 (C8):** A checkpoint artifact is written before each user-facing pause; the review surface is the product.yaml path. Pass: for each user pause (Step 4 vision review, and Step 6 resolution when blockers/warnings exist), the checkpoint `.md` exists with `Status: PENDING_APPROVAL`, references the product.yaml path, and makes no mention of an HTML brief as the review surface. Fail: Checkpoint missing or not in PENDING_APPROVAL state at pause time.

---

### Step 5 — Validate Vision (Context-Isolated)

Owner: `judge`
Depends on: Step 4
Skill: `validate-product-vision`

**Context isolation:** The judge receives the required contract scaffold plus ONLY the product artifact path and validation skill name. It does NOT receive market context from Step 2, product-strategist drafting notes, iteration history, or any intermediate reasoning from prior steps.

```json
{
  "intent_path": "core/components/recipes/discover-product/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "mode": "validate-artifact",
  "validation_skill": "validate-product-vision",
  "artifact_paths": {
    "product_yaml_path": "{product_base}/discovery/product.yaml"
  },
  "stm": {
    "output": {
      "validation_result_path": "{product_base}/discovery/validation-result.yaml"
    }
  },
  "task_id": "validate-vision"
}
```

Judge invokes `validate-product-vision` → returns validation_result with `ready_for_lock`, `completeness_score`, issues (blocker/warning), checklist.

**Step 5 Evals:**

- **SE-4 (F5):** Validation completes without blocker-severity issues remaining after cycle-back limit. Pass: Zero blockers remain, OR all blockers resolved within 1 (product) / 2 (library) cycle-backs. Fail: Blockers still present after the permitted iterations.

- **SE-5 (F7):** Pre-flight halts when product.yaml has status LOCKED. Pass: Pre-flight detects LOCKED and halts before any domain dispatch. Fail: Recipe proceeds past pre-flight for a LOCKED product.

---

### Step 6 — Pre-Lock Resolution Gate

Owner: recipe (no agent call)
Depends on: Step 5

Read validation_result from Step 5. Scan product.yaml for placeholder values.

**6a. Collect unresolved items:**

```
validation = read_yaml(validation_result_path)
product = read_yaml("{product_base}/discovery/product.yaml")

blockers = [i for i in validation.issues if i.severity == "blocker"]
warnings = [i for i in validation.issues if i.severity == "warning"]

# C14: Scan for placeholder field values
placeholders = scan_yaml_values(product, pattern=/\bTBD\b|unclear|to be determined|\?$/i)

unresolved_items = blockers + warnings + placeholders
```

**Branch A — Clean path (no unresolved items):**

If `validation.ready_for_lock == true` AND `unresolved_items` is empty:
- Auto-proceed. Show validation summary inline.
- No user pause required. Continue to Step 7.

**Branch B — Warnings and/or placeholders (no blockers):**

If `validation.ready_for_lock == true` AND `blockers` is empty AND (`warnings` OR `placeholders` non-empty):
- Write checkpoint artifact to `{product_base}/checkpoints/discover-product/{YYYYMMDD-HHMMSS}.md` with `Status: PENDING_APPROVAL`.
- Present resolution interview (C14, C15):

```markdown
## Pre-Lock Review Required — {slug}

**Completeness:** {completeness_score}%
**Product YAML:** `{product_base}/discovery/product.yaml`

The following items require your input before locking:

{If warnings non-empty:}
### Warnings ({count})
| # | Issue | Field | Severity |
|---|-------|-------|----------|
| 1 | {message} | {field} | warning |

{If placeholders non-empty:}
### Placeholder Fields ({count})
| # | Field | Current Value |
|---|-------|---------------|
| 1 | {field_path} | "{placeholder_text}" |

---
Provide your decision for each item, then type:

**RESOLVED**
1. {your decision/answer for item 1}
2. {your decision/answer for item 2}
...

Or type **Vanish** to halt.
```

Parse response:
- `RESOLVED` with numbered answers → write `{product_base}/evidence/discover-product/pre-lock-resolutions.yaml`:
  ```yaml
  pre_lock_resolutions:
    slug: "{slug}"
    resolved_at: "{timestamp}"
    items:
      - id: 1
        type: "warning|placeholder"
        item: "{issue message or field path}"
        resolution: "{user answer}"
  ```
  → Update checkpoint to APPROVED, proceed to Step 7
- `Vanish` → halt

**Branch C — Blockers present (hard block, C15 — no bypass):**

If `blockers` non-empty:
- Write checkpoint artifact with `Status: PENDING_APPROVAL`.
- Present resolution interview (C14, C15 — NO accept-risk bypass):

```markdown
## Pre-Lock Blockers — {slug}

**Completeness:** {completeness_score}%
**Product YAML:** `{product_base}/discovery/product.yaml`
**Lock blocked.** The following must be resolved before this artifact can be locked:

### Blockers ({blocker_count})
| # | Issue | Field | Severity |
|---|-------|-------|----------|
| 1 | {message} | {field} | blocker |

{If warnings non-empty:}
### Warnings ({warning_count})
| # | Issue | Field | Severity |
|---|-------|-------|----------|
| ... |

{If placeholders non-empty:}
### Placeholder Fields ({placeholder_count})
| # | Field | Current Value |
|---|-------|---------------|
| ... |

---
Address each item. Once resolved, type:

**RESOLVED**
1. {how you resolved item 1}
...

Or type **Vanish** to cycle back and re-draft.
```

Parse response:
- `RESOLVED` with numbered answers → write pre-lock-resolutions.yaml
  → Re-invoke `judge` with `validate-product-vision` skill to re-validate (same context-isolated contract as Step 5)
  → If now `ready_for_lock: true` with no blockers → proceed to Step 7
  → If still blockers → re-present interview (counts against cycle-back limit C9)
- `Vanish` → cycle-back per C9 (see below)

**Cycle-back logic (C9):**
1. Read iteration_count from status file
2. Check against type-dependent limit:
   - Product type: max 1 cycle-back iteration
   - Library type: max 2 cycle-back iterations
3. If within limit: increment iteration_count, re-invoke `product-strategist` with `draft-product-vision` skill + feedback from validation issues. Return to Step 3 and continue flow from there.
4. If limit reached: halt with F5 — "Maximum cycle-back iterations reached. Re-invoke the recipe to start fresh."

**Step 6 Evals:**

- **SE-14 (C14):** Pre-lock resolution gate writes pre-lock-resolutions.yaml before lock when blockers, warnings, or placeholder values are present. Pass: Step 6 presents a structured interview and writes `{product_base}/evidence/discover-product/pre-lock-resolutions.yaml` before Step 7 (Lock). Fail: Lock runs with unresolved issues, OR no interview, OR missing resolutions file.

- **SE-15 (F11):** Lock does not execute while any validation issue or placeholder remains unresolved. Pass: Step 7 runs only after Step 6 confirms zero unresolved items (Branch A auto, or RESOLVED path). Fail: Lock executes with unresolved items present.

- **SE-16 (C15, F12):** No accept-risk bypass exists in the blocker branch. Pass: Step 6 Branch C presents only RESOLVED and Vanish as response options; no "accept risk" or "proceed despite blockers" option exists. Fail: Any bypass mechanism is present.

- **SE-21 (C9):** Cycle-back iteration limits enforced by type (1 product, 2 library). Pass: iteration_count in the status file never exceeds the type-dependent limit; halts with F5 at the limit. Fail: Recipe permits more cycle-backs than allowed.

---

### Step 7 — Lock

Owner: recipe (no agent call)
Depends on: Step 6

Read `{product_base}/discovery/product.yaml`. Change `status: "DRAFT"` to `status: "LOCKED"`. Update `updated_at` timestamp. Write file.

```
product = read_yaml("{product_base}/discovery/product.yaml")
product.status = "LOCKED"
product.updated_at = current_timestamp()
write_yaml("{product_base}/discovery/product.yaml", product)
```

---

### Step 8 — Write Evidence

Owner: recipe + `repo-orchestrator` (utility, exempt from C5)
Depends on: Step 7

Write evidence to `{product_base}/evidence/discover-product/{YYYYMMDD-HHMMSS}.md`:
- Intent text and resolved slug
- Product type (product or library)
- `product.yaml` path and final status (LOCKED)
- Market context summary (problem, user count, competitor count) — or "skipped per C12" if library type
- Validation summary (completeness score, blocker count)
- Iteration count (cycle-backs used)
- Checkpoint outcomes (paths and Tether/Vanish decisions)

Invoke `repo-orchestrator` for evidence self-commit (ADR 012). Non-blocking on failure.

```json
{
  "intent_path": "core/components/recipes/discover-product/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "stm": {
    "input": {
      "evidence_files": [
        "{product_base}/evidence/discover-product/{timestamp}.md",
        "{product_base}/evidence/discover-product/pre-lock-resolutions.yaml",
        "{product_base}/checkpoints/discover-product/*.md",
        "{product_base}/discovery/product.yaml",
        "{product_base}/discovery/coverage-check.yaml",
        "{product_base}/discovery/validation-result.yaml",
        "{product_base}/status/discover-product.json"
      ]
    },
    "output": {}
  },
  "config": {
    "commit_message": "chore(stm): record discover-product evidence for {slug}",
    "non_blocking": true
  },
  "task_id": "evidence-self-commit"
}
```

---

### Step 9 — Summary

Owner: recipe (no agent call)
Depends on: Step 8

Present locked artifact paths to user:

```markdown
## Discovery Complete: {slug}

**Artifacts:**
- Product YAML: `{product_base}/discovery/product.yaml` (status: LOCKED, type: {product_type})
- Coverage check: `{product_base}/discovery/coverage-check.yaml`
- Validation result: `{product_base}/discovery/validation-result.yaml`

**Validation:** {completeness_score}% complete, {blocker_count} blockers resolved

Want an HTML rendering of the locked vision? Run `/briefs` — briefs are opt-in and not part of this recipe.

**Continue with** `/plan-roadmap` to begin roadmap planning.
```

---

## Scenario Validation

Run after workflow completes (after Step 7, before evidence). These validate end-to-end outcomes across the full recipe flow.

- **SCE-1 (S1):** The vision artifact (product.yaml) presented at the first checkpoint with its file path can be evaluated — including for internal libraries where market sections are absent and the YAML contains technical context instead. Pass: At the Step 4 checkpoint, product.yaml contains strategic goals and positioning derived from the original intent; for type=product, market positioning sections are present; for type=library, market sections are absent and technical_context is present; the checkpoint message presents the product.yaml path as the review surface; /briefs is offered as an optional follow-up. Fail: product.yaml does not render strategic goals or positioning, OR a library-type artifact contains market sections that should be absent, OR the checkpoint does not present the product.yaml path for review.

- **SCE-2 (S2):** A LOCKED vision artifact can serve as authoritative input for roadmap planning regardless of product type. Pass: product.yaml has status LOCKED, contains strategic_goals with IDs (≥3 entries), success metrics, and defined scope boundaries — sufficient to derive engineering milestones without ambiguity. Fail: Any of those are missing or ambiguous.

- **SCE-3 (S3):** The validation results shown inline before lock enable lock-readiness determination. Pass: Validation output includes completeness score, checklist with per-item status, and issues categorized by severity. Fail: Any of those are missing — a Product Manager cannot determine readiness.

- **SCE-4 (S4):** A LOCKED library-type product.yaml provides strategic goals with IDs, value proposition, and assumptions sufficient for roadmap planning even without market sections. Pass: type=library, status=LOCKED, strategic_goals with IDs (≥3), value_proposition, and assumptions present. Fail: Any of those are missing.

- **SCE-5 (S5):** Validation blockers trigger a structured resolution interview listing each blocker individually; resolutions are recorded in pre-lock-resolutions.yaml; re-validation occurs; lock proceeds only when re-validation confirms resolution — with no bypass. Pass: Blockers listed individually in numbered format; RESOLVED answers written to pre-lock-resolutions.yaml with timestamp; judge re-invoked after RESOLVED; lock only proceeds if re-validation shows no blockers; no bypass option. Fail: Any of the above is violated.

- **SCE-6 (S6):** A LOCKED product.yaml with the profiles section containing all three profiles (PP, NFR, QP) with levels and rationale is sufficient for architecture planning. Pass: profiles section contains 21 dimensions (7 per profile), each with level 1-5 and non-empty rationale. Fail: Any sub-section, dimension, level, or rationale is missing.

- **SCE-7 (S7):** Derived profiles are presented at the vision review checkpoint with adjustment capability. Pass: Step 3b profile presentation exists and runs before the Step 4 checkpoint; user can adjust any dimension using `DIM=level` format; adjustments are applied to product.yaml before the checkpoint; cascade order (PP → NFR → QP) is reflected in rationale text. Fail: Profiles not presented before the vision review checkpoint, adjustment mechanism absent, or cascade order not evident.

- **SCE-8 (S8):** A product.yaml produced from a BRD that explicitly defers capabilities preserves those deferrals in out_of_scope. Pass: Every capability the BRD/source explicitly defers appears in `scope.out_of_scope` (not `scope.in_scope`); any ambiguities between in-scope and deferred capabilities are surfaced as decision_points for user resolution at the vision review checkpoint by reading product.yaml. Fail: A deferred capability appears in `scope.in_scope`, or source ambiguities were resolved silently without surfacing as decision_points.

- **SCE-9 (S9):** decision_points in product.yaml where agent reasoning conflicts with source requirements are presented for explicit human resolution. Pass: Each decision_point has original_requirement, proposed_alternative, and rationale populated; at the Step 4 vision review checkpoint, the user reads product.yaml, makes an explicit choice on each decision_point, and the final product.yaml reflects the user's decision. Fail: Requirements differing from the source have no decision_point, or decision_points are not presented at the vision review checkpoint.

## Evidence & Close

Evidence pattern per ADR 012:

1. Recipe writes evidence and checkpoint artifacts to STM
2. Recipe presents summary to user
3. Recipe invokes `repo-orchestrator` with targeted self-commit:
   - Explicit file list (only files this recipe produced)
   - Commit message: `chore(stm): record discover-product evidence for {slug}`
   - No analysis, no grouping — just stage and commit
4. Non-blocking: if self-commit fails, recipe logs warning and exits successfully

## Recovery

When an agent returns structured failure:
1. Read `domain_assessment.responsible_domain`
2. Invoke responsible agent with fix context + original intent
3. Max 2 retries per step. After that, halt with full failure context.

For retries, add to contract:
```json
{
  "retry": {
    "previous_failure": "{what_failed}",
    "fix_applied": "{what was done}",
    "attempt": 2
  }
}
```

**Pre-flight failures (C1, F7) are not recoverable** — hard halt.

## Pause and Resume

Flat 12-task status file. Steps run top to bottom.

**Slug detection:** Derive slug from intent text during pre-flight.

**Status file:** `{product_base}/status/discover-product.json`

```json
{
  "recipe": "discover-product",
  "slug": "{slug}",
  "started_at": "2026-04-13T10:00:00+0530",
  "iteration_count": 0,
  "intent_assessment": {
    "product_type": "product",
    "skip_opportunity": false
  },
  "tasks": {
    "assess-intent": { "status": "completed", "completed_at": "..." },
    "discover-opportunity": { "status": "completed", "completed_at": "..." },
    "draft-vision": { "status": "completed", "completed_at": "..." },
    "check-coverage": { "status": "completed", "completed_at": "..." },
    "derive-profiles": { "status": "completed", "completed_at": "..." },
    "checkpoint-vision": { "status": "pending" },
    "validate-vision": { "status": "pending" },
    "pre-lock-resolution": { "status": "pending" },
    "lock-vision": { "status": "pending" },
    "evidence-self-commit": { "status": "pending" },
    "summary": { "status": "pending" }
  }
}
```

**Executor loop:**
```
derive slug from intent text
check for status file at {product_base}/status/discover-product.json

for each step in compiled order:
  if status file shows step "completed" -> skip
  if status file shows step "in_progress" -> reset to pending
  mark step "in_progress" in status file
  execute step
  mark step "completed" in status file
```

**Fresh start:** No status file → execute all steps, create status file on first step.
**Resume:** Status file exists → skip completed, reset any in_progress to pending, continue from first incomplete step.

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:0474d47cc799c28e49220cb644bc6c950ecbe294cb261d7536323161f85666bc |
| compiled_by | create-recipe |
| compiled_at | 2026-04-13 |
| maturity | L2 |
| workflow_structure | B |
| agents | 1 domain (product-strategist) + 1 validator (judge) + 1 utility (repo-orchestrator) |
| step_evals | 25 (SE-1 through SE-25, covering C4-C9, C11-C19, F1-F3, F5, F7-F16) |
| scenario_evals | 9 (SCE-1 through SCE-9, covering S1-S9) |
