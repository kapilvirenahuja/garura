---
name: prepare-architecture
description: Design project architecture informed by three-axis profiles (PP, NFR, QP), producing architecture.yaml and quality-standards.yaml
user-invocable: true
---

# prepare-architecture

Design the project's technical architecture by reading LTM architecture knowledge, scanning the codebase, and selecting patterns, stacks, platforms, data stores, and operations infrastructure informed by the product's three-axis profiles (PP, NFR, QP). Produces a concrete `architecture.yaml` with named technologies, a structured `decisions[]` decision map with driver-tagged reasoning trails, and a `quality-standards.yaml` defining the project's quality baseline. All artifacts are stored at the product level. Requires a locked `product.yaml` (with profiles section containing all three sub-sections) and a locked `roadmap.yaml` as upstream inputs.

## Compiled From

This recipe was compiled from `reference/intent.yaml` by `/create-recipe`.
Intent defines constraints (C1-C13), failure conditions (F1-F8), and scenarios (S1-S6).
To modify this recipe, update `reference/intent.yaml` and re-run `/create-recipe --rebake prepare-architecture`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the task graph, checkpoints, and evidence.
You delegate domain tasks to agents via the JSON contract — never execute domain work directly.

**Forbidden:** `Bash`, `Grep`, `Glob`, `Edit`, `EnterPlanMode`, `ExitPlanMode`, or any direct file operations beyond `Read`/`Write` for checkpoints and evidence. No direct git commands. No direct skill invocations.

**Agent boundaries:**

| Agent | Domain | Steps |
|-------|--------|-------|
| `tech-designer` | Codebase scan, LTM read, architecture drafting, quality standards | 2, 3, 4 (domain) |
| `judge` | Context-isolated architecture + quality standards validation | 5c |
| `repo-orchestrator` | Evidence self-commit (utility, exempt from C9) | 8 |

**Path resolution:**
All `.meridian/product/` paths are **relative to the project root**. NEVER expand to an absolute path under `~/.`. Product artifacts are product-scoped; `~/.meridian/core/memory/` is for LTM only.

**Brief generation (opt-in, NOT part of this recipe):**
Briefs are not generated by this recipe. The checkpoint review surface is the two YAML artifact file paths. After the recipe completes, users may optionally run `/briefs` separately to generate an HTML rendering for easier review.

## Pre-flight

Execute these checks before any domain work. Orchestrator owns — do not delegate.

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| `--product` argument provided and file exists | — | Hard halt |
| Product has `status: LOCKED` | C1 | Hard halt — cannot design architecture from unlocked product |
| Product has `profiles` section with all 3 sub-sections (`product_profile`, `nfr_profile`, `quality_profile`) | C1, F6 | Hard halt — profiles required for profile-driven architecture |
| `roadmap.yaml` exists at `{product_base}/roadmap/roadmap.yaml` with `status: LOCKED` | C1 | Hard halt — roadmap required as upstream input |
| Derive `slug` from product.yaml | — | Hard halt if slug absent |

```bash
# Resolve product base and slug from product.yaml
product_base = ".meridian/product"
slug = extract_slug(product.yaml)

# C1: product.yaml status must equal LOCKED
# C1, F6: profiles section with all 3 sub-sections (product_profile, nfr_profile, quality_profile) required
# C1: roadmap.yaml status must equal LOCKED
# Hard halt on any failure — pre-flight failures are not recoverable
```

**Resume check:** If `.meridian/product/status/prepare-architecture.json` exists, resume — skip completed steps, reset any `in_progress` to `pending`, continue from first incomplete.

## Workflow

### Phase: Preparation

**Step 1 — Read Upstream Context**
Owner: orchestrator (no agent call)
Depends on: pre-flight

Read `product.yaml` at `.meridian/product/discovery/product.yaml`. Extract:
- `profiles`: `product_profile`, `nfr_profile`, `quality_profile` — all dimensions, levels, and rationale
- `strategic_goals` — list of goal IDs and descriptions
- `slug` — already resolved during pre-flight

Read `roadmap.yaml` at `.meridian/product/roadmap/roadmap.yaml`. Extract:
- `timeline` — horizons with `epic_refs` per horizon
- `feasibility` — per-epic risk levels and blockers
- Summary of epics, sequencing constraints, and risk landscape

Store extracted context in memory for use in Steps 2–4 contracts. Write initial status file at `.meridian/product/status/prepare-architecture.json`.

Update status file: `read-context → completed`.

---

**Step 2 — Scan Codebase & Read LTM**
Owner: `tech-designer`
Depends on: Step 1

Pass the following contract as the ENTIRE agent prompt (JSON only, nothing else):

```json
{
  "intent_path": "core/components/recipes/prepare-architecture/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "slug": "{slug}",
  "task_id": "scan-codebase-ltm",
  "stm": {
    "product_yaml_path": ".meridian/product/discovery/product.yaml",
    "roadmap_yaml_path": ".meridian/product/roadmap/roadmap.yaml",
    "context_report_path": null
  },
  "config": {
    "profiles": "{extracted profiles from Step 1}"
  },
  "ltm_context": {
    "project_base": ".meridian/product/architecture/",
    "core_base": "~/.meridian/core/memory/",
    "query_domains": ["architecture", "patterns", "stacks", "platforms", "data", "operations"],
    "locked_artifacts": ["{product.yaml filename if LOCKED}"]
  }
}
```

Agent scans the codebase for existing technology decisions (ADRs, package manifests, framework choices, infrastructure configs — C10) and reads LTM architecture knowledge from `~/.meridian/core/memory/knowledge/architecture/` before any technology selections (C2). Architecture files' "When to Choose" and "When to Avoid" prose is matched against the project's profile values. Produces a context report at `.meridian/product/architecture/context-report.yaml`.

**Expected return:** Contract with `stm.context_report_path` populated, `step_failure` null.

Check `step_failure` first — if non-null, read `domain_assessment.responsible_domain`, retry once with fix context. After 2 failures, halt with full failure context.
Then check `stm.context_report_path` is non-null.

Update status file: `scan-codebase-ltm → completed`.

**Step 2 Evals:**
- **SE-1 (C10):** Read `stm.context_report_path` — verify at least 1 codebase finding is referenced (ADR, package manifest, framework choice, or infrastructure config), OR the report explicitly states greenfield. PASS if condition met. FAIL if report contains no codebase findings and does not state greenfield. Halt on FAIL.
- **SE-2 (C2, F4):** Read `stm.context_report_path` — verify at least 3 LTM architecture knowledge files are listed as consulted. PASS if count ≥ 3. FAIL if fewer than 3 LTM files referenced. Halt on FAIL.

---

**Step 3 — Draft Architecture**
Owner: `tech-designer`
Depends on: Step 2

Update `task_id` to `draft-architecture` in the enriched contract. Pass the full enriched contract (with `stm.context_report_path` populated) as the ENTIRE agent prompt — no other text. Contract must include:

```json
{
  "intent_path": "core/components/recipes/prepare-architecture/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "slug": "{slug}",
  "task_id": "draft-architecture",
  "stm": {
    "product_yaml_path": ".meridian/product/discovery/product.yaml",
    "roadmap_yaml_path": ".meridian/product/roadmap/roadmap.yaml",
    "context_report_path": "{populated from Step 2}",
    "architecture_yaml_path": null
  },
  "config": {
    "profiles": "{extracted profiles}",
    "output_base": ".meridian/product/architecture/"
  }
}
```

Agent invokes `draft-technical-approach` skill. NFR section requirements are derived from NFR Profile levels in `product.yaml` (C4). Every technology selection must name a specific product, library, or platform and be justified against profiles and LTM findings (C3). The output must include a `decisions[]` array capturing key architectural decisions with structured records: question, answer, drivers, reasons (with driver attribution), picked technologies, and rejected alternatives (C12, C13). Produces `architecture.yaml` conforming to the architecture.yaml schema at `.meridian/product/architecture/architecture.yaml`.

**Expected return:** Contract with `stm.architecture_yaml_path` populated, `step_failure` null.

Check `step_failure` first — if non-null, retry once. After 2 failures, halt.
Then check `stm.architecture_yaml_path` is non-null.

Update status file: `draft-architecture → completed`.

**Step 3 Evals:**
- **SE-3 (C3, F1):** Read `stm.architecture_yaml_path` — verify every entry in `stack[]` has a specific named technology (e.g., "React 19", "Python 3.12", "Claude Sonnet"), not vague references like "a web framework" or "a relational database". PASS if all entries are specific. FAIL if any entry uses a vague reference. Halt on FAIL.
- **SE-4 (C4, F2):** Read `stm.architecture_yaml_path` `nfrs` section — verify every requirement has a rationale traceable to an NFR Profile dimension and level from `product.yaml` (e.g., "NFR-3 level 4 → API response < 100ms at p95"). PASS if all requirements have traceable rationale. FAIL if any requirement lacks NFR Profile traceability. Halt on FAIL.
- **SE-5 (F7):** Read `stm.architecture_yaml_path` — verify no technology selection contradicts the project profiles (e.g., no enterprise-grade infrastructure when PP-6=1 POC, no basic security when PP-7=5 BFSI). PASS if all selections are consistent with profile values. FAIL if any contradiction is found. Halt on FAIL.
- **SE-6 (C12, F8):** Read `stm.architecture_yaml_path` `decisions` field — verify `decisions[]` exists, is non-empty, and each entry contains all required fields: `question` (string), `answer` (string), `drivers` (non-empty array), `reasons` (non-empty array of objects with `driver` and `text` fields), `picked` (non-empty array), and `rejected` (array of objects with `option` and `reason` fields). PASS if all entries have complete structure. FAIL if decisions is absent, empty, or any entry is missing required fields. Halt on FAIL.
- **SE-7 (C13):** Read `stm.architecture_yaml_path` `decisions` field — verify every `reasons[]` entry in every decision has a `driver` field with a value from the allowed set: `budget`, `ltm`, `profile`, `user-decision`. PASS if all reason entries have a valid driver tag. FAIL if any reason lacks a driver or uses an unrecognized driver tag. Halt on FAIL.

---

**Step 4 — Draft Quality Standards**
Owner: `tech-designer`
Depends on: Step 3

Update `task_id` to `draft-quality-standards`. Pass the full enriched contract (with `stm.architecture_yaml_path` populated) as the ENTIRE agent prompt — no other text. Contract must include:

```json
{
  "intent_path": "core/components/recipes/prepare-architecture/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "slug": "{slug}",
  "task_id": "draft-quality-standards",
  "stm": {
    "product_yaml_path": ".meridian/product/discovery/product.yaml",
    "architecture_yaml_path": "{populated from Step 3}",
    "quality_standards_path": null
  },
  "config": {
    "profiles": "{extracted profiles}",
    "output_base": ".meridian/product/architecture/"
  }
}
```

Agent invokes `draft-quality-standards` skill. Produces `quality-standards.yaml` conforming to the quality-standards.yaml schema (C5). Each QP dimension level maps to concrete standards and named tooling — vague references are not acceptable. The `debt_baseline` section is initialized with `current_level: null` for all QP dimensions, establishing the measurement baseline. Produces artifact at `.meridian/product/architecture/quality-standards.yaml`.

**Expected return:** Contract with `stm.quality_standards_path` populated, `step_failure` null.

Check `step_failure` first — if non-null, retry once. After 2 failures, halt.
Then check `stm.quality_standards_path` is non-null.

Update status file: `draft-quality-standards → completed`.

**Step 4 Evals:**
- **SE-8 (C5, F3):** Read `stm.quality_standards_path` `standards` section — verify every standards entry has `qp_level` matching the corresponding QP profile dimension level from `product.yaml`, and concrete tooling is named (not null) for core fields (`linter`, `formatter`, `pipeline`, `logging`). PASS if all entries have matching `qp_level` and named tooling. FAIL if any `qp_level` mismatches or required tooling is null. Halt on FAIL.
- **SE-9 (C5):** Read `stm.quality_standards_path` `standards` section — verify every entry's `qp_level` is non-null and the standards content (strategy, tooling choices, coverage targets) is appropriate for that level (e.g., qp_level 4 should not specify minimal tooling). PASS if all entries have non-null `qp_level` and content consistent with that level. FAIL if any entry has null `qp_level` or content inconsistent with its declared level. Halt on FAIL.

---

### Phase: Checkpoint

**Step 5 — Checkpoint: Artifact Review**
Owner: orchestrator
Depends on: Step 4

This checkpoint is ALWAYS required — C7 mandates explicit human approval before any artifacts are locked. The review surface is the two YAML artifact file paths.

Write checkpoint artifact to `.meridian/product/checkpoints/prepare-architecture/{YYYYMMDD-HHMMSS}.md`.

Update status file: `artifact-review → in_progress`.

Present artifact paths to user:

```markdown
## Architecture Review: {slug}

The following artifacts are ready for review before lock:

**Architecture YAML:** `.meridian/product/architecture/architecture.yaml`
**Quality Standards YAML:** `.meridian/product/architecture/quality-standards.yaml`

Open the two YAML files directly to review technology selections, NFR mappings,
the `decisions[]` reasoning trail, and quality standards tooling.

**Optional:** You may run `/briefs` separately to generate an HTML rendering of
these artifacts for easier review. Briefs are opt-in and NOT required for lock.

---

Type **Tether** to approve and proceed to lock, or **Vanish** to halt.
```

**Feedback loop (max 3 cycles):**
- Text feedback → determine which artifact needs revision (architecture or quality standards), re-invoke `tech-designer` with enriched contract + `"user_feedback": "{feedback text}"` appended for the relevant step. Cycle count ≤ 3.
- **Tether** → proceed to Step 5b (Pre-Lock Resolution Gate).
- **Vanish** → halt. Present: "Artifacts rejected — run `/prepare-architecture --product {path}` to start over."
- After 3 feedback cycles with no Tether/Vanish → present: "Maximum feedback cycles reached. Type **Tether** to approve or **Vanish** to halt."

Update status file: `artifact-review → completed`.

---

### Step 5b — Pre-Lock Resolution Gate

Owner: orchestrator (no agent call)
Depends on: Step 5 (Tether)

After artifact approval, read `architecture.yaml` and `quality-standards.yaml` for unresolved items before locking (C11).

```
architecture = read_yaml(".meridian/product/architecture/architecture.yaml")
quality_standards = read_yaml(".meridian/product/architecture/quality-standards.yaml")

high_risks = [r for r in architecture.technical_risks if r.severity == "high"]
quality_gaps = [s_name for s_name, s in quality_standards.standards.items()
                if any required tooling field is null]
```

**If both arrays are empty:**
- Auto-proceed. Update status file: `pre-lock-resolution → completed`.
- Proceed to Step 5c.

**If unresolved items exist — present structured RESOLVED interview (C11):**

```markdown
## Pre-Lock Resolution Required — Architecture: {slug}

Before the architecture can be locked, the following items must be resolved:

{If high_risks non-empty:}
### High-Severity Technical Risks ({count})
| # | Risk | Affected Features | Mitigation |
|---|------|-------------------|------------|
| 1 | {risk} | {affected_features} | {mitigation} |

{If quality_gaps non-empty:}
### Quality Standard Gaps ({count})
| # | Section | Gap |
|---|---------|-----|
| 1 | {section} | {gap description} |

---
Provide your decision or answer for each item, then type:

**RESOLVED**
1. {your decision for item 1}
2. {your decision for item 2}
...

Or type **Vanish** to halt.
```

Parse response:
- `RESOLVED` with numbered answers → write `.meridian/product/evidence/prepare-architecture/pre-lock-resolutions.yaml`:
  ```yaml
  pre_lock_resolutions:
    slug: "{slug}"
    resolved_at: "{timestamp}"
    high_risks:
      - risk: "{description}"
        resolution: "{user decision}"
    quality_gaps:
      - section: "{section}"
        resolution: "{user answer}"
  ```
  → Update status file: `pre-lock-resolution → completed`.
  → Proceed to Step 5c.

- If RESOLVED has fewer answers than items → re-present unanswered items.
- `Vanish` → halt. Present halt message.

**Step 5b Evals:**
- **SE-10 (C11, F5):** After artifact approval, the recipe reads `architecture.yaml` technical_risks and `quality-standards.yaml` for unresolved gaps. Lock does not proceed while high-severity risks or quality gaps remain without user resolution. Pass: When high-severity risks or quality gaps exist, a structured resolution interview is presented with each item listed individually, resolutions are written to `pre-lock-resolutions.yaml`, and lock only proceeds after all items are resolved. Fail: Lock proceeds while high-severity technical risks or quality standard gaps remain unresolved.

---

### Step 5c — Validate Architecture (Context-Isolated)

Owner: `judge`
Depends on: Step 5b
Skill: `validate-architecture-design`

**Context isolation:** The judge receives ONLY the artifact paths and validation skill name. It does NOT receive context_report_path, tech-designer drafting notes, LTM architecture knowledge references, pre-lock resolution answers, or any intermediate reasoning from Steps 1-5b.

```json
{
  "mode": "validate-artifact",
  "validation_skill": "validate-architecture-design",
  "artifact_paths": {
    "architecture_yaml_path": ".meridian/product/architecture/architecture.yaml",
    "quality_standards_yaml_path": ".meridian/product/architecture/quality-standards.yaml",
    "product_yaml_path": ".meridian/product/discovery/product.yaml"
  },
  "task_id": "validate-architecture"
}
```

Judge invokes `validate-architecture-design` -> returns validation_result with ready_for_lock, per-check results, issues.

**On validation failure (ready_for_lock: false):**
Present blockers to user in the same resolution interview pattern as Step 5b:

```markdown
## Architecture Validation Issues — {slug}

The context-isolated validation found the following issues:

### Issues ({count})
| # | Check | Issue | Severity |
|---|-------|-------|----------|
| 1 | {check_id} | {message} | {severity} |

---
Address each blocker item, then type **RESOLVED** with numbered answers.
Or type **Vanish** to halt.
```

Parse response:
- `RESOLVED` → apply fixes to architecture.yaml/quality-standards.yaml, re-invoke judge for re-validation (max 1 retry). If still failing, halt.
- `Vanish` → halt.

**On validation success (ready_for_lock: true):**
Auto-proceed to Step 6.

Update status file: `validate-architecture → completed`.

**Step 5c Evals:**
- **SE-11 (context isolation):** The judge validation contract contains ONLY artifact_paths (architecture_yaml_path, quality_standards_yaml_path, product_yaml_path) — no context_report_path, tech-designer notes, LTM references, or pre-lock resolution answers. PASS if contract has only artifact_paths and validation_skill. FAIL if any drafting context is present.

---

### Phase: Lock

**Step 6 — Lock**
Owner: orchestrator (no agent call)
Depends on: Step 5c

Lock both artifacts:

```
architecture = read_yaml(".meridian/product/architecture/architecture.yaml")
architecture.status = "LOCKED"
architecture.updated_at = current_timestamp()
write_yaml(".meridian/product/architecture/architecture.yaml", architecture)

quality_standards = read_yaml(".meridian/product/architecture/quality-standards.yaml")
quality_standards.status = "LOCKED"
quality_standards.updated_at = current_timestamp()
write_yaml(".meridian/product/architecture/quality-standards.yaml", quality_standards)
```

Update status file: `lock → completed`.

---

### Phase: Scenario Validation

**Step 7 — Scenario Evals**
Owner: orchestrator
Depends on: Step 6

Run these scenario evals against the final artifacts:

- **SCE-1 (S1 — Technical Architect):** Read `.meridian/product/architecture/architecture.yaml`. Verify `stack[]` — each entry has a named technology and a `rationale` field. Verify `nfrs` — each requirement rationale references an NFR Profile dimension level. PASS if all stack entries are specific and all NFR requirements have profile-traceable rationale. FAIL if any stack entry is vague or any NFR requirement lacks traceability.

- **SCE-2 (S2 — Engineering Lead):** Read `.meridian/product/architecture/quality-standards.yaml`. Verify every `standards` section has named tooling for `linter`, `formatter`, `pipeline`, `logging`, and `monitoring` (or equivalent fields). Verify `debt_baseline.dimensions` has an entry for every QP dimension (QP-1 through QP-7) with `target_level` matching the product.yaml QP profile level and `current_level: null`. PASS if all tooling fields are present and debt baseline is complete. FAIL if any tooling is null or any QP dimension is missing from debt_baseline.

- **SCE-3 (S3 — Product Manager):** Read `.meridian/product/architecture/architecture.yaml` and `.meridian/product/architecture/quality-standards.yaml`. Verify the `profiles_ref` or equivalent profile-linking field is present in architecture.yaml and a high-level summary of technology decisions is readable (stack names and rationale present). PASS if profile linkage and high-level decisions are navigable from the YAML artifacts alone. Note: The PM may optionally run `/briefs` separately to generate an HTML rendering — brief presence is NOT part of this eval.

- **SCE-4 (S4 — Implementation Agent):** Read `.meridian/product/architecture/architecture.yaml`. For every epic referenced in `roadmap.yaml` `timeline[].epic_refs`, verify architecture coverage exists — either a `deployment_units` entry, a `stack` entry, a `platforms` entry with the epic in `features_served`, or an `integrations` entry with the epic in `features_served`. PASS if all roadmap epics have corresponding architecture coverage. FAIL if any epic lacks coverage.

- **SCE-5 (S5 — Quality Lead):** Read `.meridian/product/architecture/quality-standards.yaml`. Verify `debt_baseline.dimensions` covers all 7 QP dimensions (QP-1 through QP-7), each with `target_level`, `current_level: null`, `gap: null`, and `remediation: null`. Verify each `standards` section has a `qp_level` matching the product.yaml `quality_profile` level for that dimension. PASS if all dimensions are present, all `target_level` values match QP profile, and all `debt_baseline` entries are properly initialized. FAIL if any dimension is missing or any `qp_level` mismatches.

- **SCE-6 (S6 — Decision Reviewer):** Read `.meridian/product/architecture/architecture.yaml` `decisions[]`. Verify every decision has a `question`, `answer`, non-empty `drivers[]`, and `reasons[]` where each reason has a `driver` tag from the allowed set (budget, ltm, profile, user-decision). Verify that `rejected[]` entries (when present) have both `option` and `reason` fields. Verify decisions collectively cover at least 3 distinct driver types. PASS if all decisions are fully structured with driver-tagged reasoning and rejected alternatives have stated rationale. FAIL if any decision lacks driver traceability or rejected entries lack rationale.

Record PASS/FAIL for each SCE. If any SCE fails, report to user with details but continue to Evidence — scenario failures are observations, not halts.

---

### Phase: Evidence & Close

**Step 8 — Write Evidence and Report**
Owner: orchestrator + `repo-orchestrator` (utility, exempt from C9)
Depends on: Step 7

Write evidence file to `.meridian/product/evidence/prepare-architecture/{YYYYMMDD-HHMMSS}.md`:

```markdown
# prepare-architecture Evidence

**Product:** {slug}
**Product YAML:** .meridian/product/discovery/product.yaml
**Completed:** {timestamp}

## Artifacts

| Artifact | Path | Status |
|----------|------|--------|
| Context Report | .meridian/product/architecture/context-report.yaml | written |
| Architecture YAML | .meridian/product/architecture/architecture.yaml | LOCKED |
| Quality Standards | .meridian/product/architecture/quality-standards.yaml | LOCKED |

## Checkpoint

Artifact review: {checkpoints[artifact_review].status} | Feedback cycles: {count}
Pre-lock resolution: {pre_lock_resolution_status}

## Scenario Eval Results

| Eval | Persona | Result |
|------|---------|--------|
| SCE-1 | Technical Architect | PASS/FAIL |
| SCE-2 | Engineering Lead | PASS/FAIL |
| SCE-3 | Product Manager | PASS/FAIL |
| SCE-4 | Implementation Agent | PASS/FAIL |
| SCE-5 | Quality Lead | PASS/FAIL |
| SCE-6 | Decision Reviewer | PASS/FAIL |
```

Present final summary to user:

```markdown
## Architecture Complete: {slug}

**Artifacts:**
- Architecture YAML: `.meridian/product/architecture/architecture.yaml` (status: LOCKED)
- Quality Standards: `.meridian/product/architecture/quality-standards.yaml` (status: LOCKED)

**Optional:** Run `/briefs` to generate an HTML rendering of these artifacts.

**Next:** Run `/draft-lld` to create the low-level design, or `/scope-roadmap-epics` to begin implementation planning.
```

Invoke `repo-orchestrator` for evidence self-commit (non-blocking — failure does not halt):

```json
{
  "intent_path": "core/components/recipes/prepare-architecture/reference/intent.yaml",
  "stm_base": ".meridian/product/",
  "task_id": "evidence-report",
  "stm": {
    "input": {},
    "output": {
      "evidence": ".meridian/product/evidence/prepare-architecture/{YYYYMMDD-HHMMSS}.md"
    }
  },
  "config": {
    "commit_message": "chore(stm): record prepare-architecture evidence for {slug}",
    "non_blocking": true
  }
}
```

Update status file: `evidence-report → completed`.

---

## Pause and Resume

Steps execute in compiled order — run top to bottom.

**Product detection:** Slug is derived from `product.yaml` during pre-flight.

**Status file:** `.meridian/product/status/prepare-architecture.json`

```json
{
  "recipe": "prepare-architecture",
  "slug": "{slug}",
  "started_at": "2026-04-13T10:00:00+0530",
  "product_yaml_path": ".meridian/product/discovery/product.yaml",
  "tasks": {
    "read-context":            { "status": "completed", "completed_at": "..." },
    "scan-codebase-ltm":       { "status": "completed", "completed_at": "..." },
    "draft-architecture":      { "status": "completed", "completed_at": "..." },
    "draft-quality-standards": { "status": "completed", "completed_at": "..." },
    "artifact-review":         { "status": "in_progress", "started_at": "..." },
    "pre-lock-resolution":     { "status": "pending" },
    "validate-architecture":   { "status": "pending" },
    "lock":                    { "status": "pending" },
    "scenario-evals":          { "status": "pending" },
    "evidence-report":         { "status": "pending" }
  },
  "contract_snapshot": "{last enriched contract JSON}"
}
```

**Executor loop:**
```
check for status file at .meridian/product/status/prepare-architecture.json

if file exists (resume):
  read contract_snapshot — reconstruct enriched JSON contract
  for each step in compiled order:
    if status == "completed" → skip
    if status == "in_progress" → reset to pending (may not have finished)
    mark step "in_progress" in status file
    execute step
    mark step "completed" in status file

if file absent (fresh start):
  execute all steps
  create status file on Step 1
```

**`/prepare-architecture --resume`** — No `--product` argument needed.

1. Find the status file at `.meridian/product/status/prepare-architecture.json`.
2. Read `contract_snapshot` — reconstruct the enriched JSON contract.
3. Route based on task statuses:
   - `artifact-review` status `in_progress` or `pending` → re-present YAML artifact paths, continue from Step 5 feedback loop.
   - `artifact-review` status `completed` AND `pre-lock-resolution` status `pending` → jump to Step 5b.
   - `pre-lock-resolution` status `completed` AND `validate-architecture` status `pending` → jump to Step 5c.
   - `validate-architecture` status `completed` AND `lock` status `pending` → jump to Step 6.
4. Report: "Resuming prepare-architecture for `{slug}` — {description of what it is doing}."
5. If no status file found → halt: "No prepare-architecture checkpoint found. Start with `/prepare-architecture --product <path>`."

---

## Recovery

When an agent returns `step_failure` (non-null):

1. Read `step_failure.domain_assessment.responsible_domain` — identify which agent can fix it.
2. Invoke the responsible agent with the current contract + retry context appended:
   ```json
   "retry": {
     "previous_failure": "{step_failure.error}",
     "fix_applied": "{what was adjusted}",
     "attempt": 2
   }
   ```
3. Max 2 retries per step. After 2 failures, halt with full failure context presented to user.

**Pre-flight failures are not recoverable** — hard halt with clear error message.

## References

| File | Path | Used For |
|------|------|----------|
| Intent | `reference/intent.yaml` | Source of constraints, failure conditions, scenarios |
| Architecture Schema | `core/components/skills/draft-technical-approach/schemas/architecture.yaml` | architecture.yaml artifact structure |
| Quality Standards Schema | `core/components/skills/draft-quality-standards/schemas/quality-standards.yaml` | quality-standards.yaml artifact structure |

---

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:14397266085aedef858f1676250303dba196b0ba1c882a10b47f4029d1e77597 |
| compiled_by | create-recipe |
| compiled_at | 2026-04-13 |
| maturity | L2 |
| workflow_structure | A (full checkpoint flow) |
| domain_agents | 1 (tech-designer) + 1 validator (judge) |
| utility_agents | 1 (repo-orchestrator) — exempt from C9 |
| step_evals | 11 (SE-1 through SE-11, covering C2-C5, C10-C13, F1-F5, F7, F8, context isolation) |
| scenario_evals | 6 (SCE-1 through SCE-6, covering S1-S6) |
| briefs | NOT generated by this recipe — opt-in via `/briefs` after completion |
