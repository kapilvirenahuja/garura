# Checkpoint: prepare-architecture — {slug}

**Status:** {status}
**Recipe:** prepare-architecture
**Product:** {slug}
**Timestamp:** {timestamp}

## Artifacts
- Architecture YAML: `{architecture_yaml_path}`
- Quality Standards: `{quality_standards_path}`
- Architecture Brief: `{brief_path}`
- Hub: `{hub_path}`
