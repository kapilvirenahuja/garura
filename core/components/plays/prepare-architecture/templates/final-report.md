## Architecture Complete: {slug}

**Artifacts:**
- Architecture YAML: `{architecture_yaml_path}` (status: LOCKED)
- Quality Standards: `{quality_standards_path}` (status: LOCKED)
- Architecture Brief: `{brief_path}`
- Hub: `{hub_path}`

**Profiles Used:**
- Product Profile: {pp_summary}
- NFR Profile: {nfr_summary}
- Quality Profile: {qp_summary}

**Risk Summary:** {high_risk_count} high-severity risks resolved

**Continue with** `/prepare-implementation --epic {epic_id}` to begin epic-scoped implementation planning.
