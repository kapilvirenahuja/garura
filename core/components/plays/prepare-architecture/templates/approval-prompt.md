## Architecture Review: {slug}

**Architecture YAML:** `{architecture_yaml_path}`
**Quality Standards:** `{quality_standards_path}`
**Architecture Brief:** `{brief_path}`
**Hub:** `{hub_path}`

Open the architecture brief in your browser to review the technology selections, quality standards, and profile alignment.
Select any text to add inline comments. Use the Comments tab to export structured feedback JSON.

---

Type **Tether** to approve and proceed to lock, or **Vanish** to halt.
Optionally paste feedback for targeted revisions (max 3 cycles).
