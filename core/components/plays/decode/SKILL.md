---
name: garura:decode
description: "Brownfield behavior extraction with executable verification. For each feature in a codebase's feature catalog, extract business logic, data contracts, integration behaviors, and scenarios the code encodes — as three cross-linked artifact streams (feature specs, user-flow specs, cross-cutting aspect specs) — plus a tiered suite of generated tests that must pass green against the current codebase before capture. Pairs with /codify: codify captures shape, decode captures logic."
user-invocable: true
model: opus
---

# decode

Given an existing codebase whose feature catalog exists (in product LTM post-/enrich, or in STM from a prior /codify run), reverse-engineer the business logic the code encodes as three cross-linked artifact streams (feature specs, user-flow specs, cross-cutting aspect specs) plus a tiered suite of generated tests that must run green against the current codebase before each spec is considered captured. The play is tech-agnostic at compile time; tech-aware extraction skills are synthesized at runtime from KB playbooks per detected stack and cleaned up on final Tether. Every rule, scenario, contract, integration, and step cites source at file:line. Test/code drift surfaces as ambiguities, never silent resolution. Output is staged under `{stm_base}/{issue}/evidence/decode/proposals/` with a master proposals.yaml index. No writes to product LTM — /enrich is the sole promotion gate.

## Compiled From

This play was compiled from `reference/intent.yaml` by `/create-play`.
Intent defines constraints (C0–C33 plus C4a–d), failure conditions (F0–F24), and scenarios (S1–S11).
To modify this play, update `reference/intent.yaml` and re-run `/create-play --build decode`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the workflow. You delegate every domain task to agents via JSON contracts — never execute extraction work directly. You DO perform deterministic infrastructure calls (scan, tech-stack detection, temp-skill synthesis, citation integrity check, aggregation, cleanup) via their skills.

**Forbidden:** Direct source code reading as a primary input; direct behavior/flow/aspect extraction; direct test authoring; direct test execution (test-runner agent is the ONLY path); any write to `{product_base}`, `~/.garura/core/memory/`, `core/components/skills/`, or global `~/.claude/skills/`.

**Agent boundaries:**

| Agent | Type | Domain | Phases |
|-------|------|--------|--------|
| `project-orchestrator` | domain | Issue resolution + creation | Setup |
| `tech-architect` | domain | Single-owner extraction: behaviors + flows + aspects (C13) | Phase 3 — Extraction |
| `test-engineer` | domain | Test-surface mapping + tiered test generation (C14, Tier A/B/C) | Phase 2 — Test surface, Phase 4 — Test generation |
| `test-runner` | domain | Context-isolated test execution (C15) | Phase 5 — Baseline-green verification |
| `repo-orchestrator` | utility | Evidence self-commit | Evidence & Close |

Domain agents: 3 (tech-architect, test-engineer, test-runner). Utility: 1 (repo-orchestrator, budget-exempt). Within ≤5 domain budget.

## Arguments

```
/decode [--issue <issue-number>] [--feature FEAT-ID ...] [--capability CAP-ID ...] [--domain DOMAIN-ID ...] [--all]
        [--features-from <path>] [--loc-soft-cap N] [--workspace <path>] [--install-if-missing] [--force-cleanup]
```

- `--issue` — optional. Anchor STM output paths. Resolved from arg, branch `decode/{issue}-*` or `feat/{issue}-decode-*`, else project-orchestrator creates one.
- `--feature` / `--capability` / `--domain` / `--all` — at least ONE is required (C1). Feature-scope input is mandatory; no unbounded invocations.
- `--features-from` — optional override for features.yaml location (C2 path b). Used when /codify STM features.yaml is the input before /enrich promotion.
- `--loc-soft-cap` — per-unit LOC pre-check soft cap (default 5000). C11.
- `--workspace` — optional workspace root when the base repo lacks installed deps (C26 fallback).
- `--install-if-missing` — allow test-runner to invoke harness install command. Default false.
- `--force-cleanup` — remove temp-skills regardless of play state (C32 force trigger).

## Pre-flight

Execute these checks before any domain work:

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| Resolve `stm_base` from `.garura/core/config.yaml` | C3 | Hard halt — config is required |
| Resolve `product_base` from `.garura/core/config.yaml` | C3 | Hard halt — config is required |
| Resolve `core_base` (`~/.garura/core/memory/`) | C21 | Hard halt — LTM Resolution Protocol requires it |
| Resolve issue number per C0 | C0, F0 | Delegate to project-orchestrator; if both fail, hard halt |
| Confirm at least one feature selector supplied | C1, F3 | Hard halt — feature scope is mandatory |
| Resolve features.yaml per C2 order | C2, F2 | Hard halt — /codify + /enrich prerequisite |
| Verify physical-architecture.yaml exists for tech-skill synthesis | C29 | Hard halt — decode cannot dispatch without detected stacks |
| Verify every detected stack has a playbook at `core/components/memory/knowledge/tech/{stack}.md` | C30, F15 | Hard halt — prompt user to author the missing playbook |
| Detect TEST_HARNESS (framework + runner command + runnability) | C26, F18 | Hard halt — specs that cannot be verified green are not captured |

```bash
stm_base=$(grep -E '^\s*base-path:' .garura/core/config.yaml | head -1 | awk '{print $2}')
product_base=$(grep -A1 '^product:' .garura/core/config.yaml | grep 'base-path' | awk '{print $2}')
core_base="$HOME/.garura/core/memory/"

# Issue resolution per C0
branch=$(git branch --show-current)
issue=${ISSUE_ARG}
if [ -z "$issue" ]; then
  issue=$(echo "$branch" | grep -oE '/(decode/|feat/)[0-9]+' | grep -oE '[0-9]+')
fi
```

**Resume check:** If `{stm_base}/{issue}/status/decode.json` exists, resume — skip completed units/steps, reset any `in_progress` to pending, continue from first incomplete.

## Task DAG

Before Step 1, the orchestrator creates tasks for every compiled step with dependencies encoded in `blockedBy`.

```
TaskCreate [T1] Resolve or create issue                — owner: play
TaskCreate [T2] Validate invocation arguments          — blockedBy [T1]
TaskCreate [T3] Resolve features.yaml                  — blockedBy [T2]
TaskCreate [T4] Run codebase scan                      — blockedBy [T3]
TaskCreate [T5] Detect tech stacks                     — blockedBy [T4]
TaskCreate [T6] Synthesize temp tech skills            — blockedBy [T5]
TaskCreate [T7] Detect test harness                    — blockedBy [T4]
TaskCreate [T8] Enumerate target units                 — blockedBy [T3]
TaskCreate [T9] LOC pre-check per unit                 — blockedBy [T8]
TaskCreate [T10] Feature extraction loop               — blockedBy [T6, T7, T9]
TaskCreate [T11] Flow extraction loop                  — blockedBy [T6, T7, T9]
TaskCreate [T12] Aspect extraction loop                — blockedBy [T6, T7, T9]
TaskCreate [T13] Citation integrity verification       — blockedBy [T10, T11, T12]
TaskCreate [T14] Test surface mapping per unit         — blockedBy [T8]
TaskCreate [T15] Generate contract tests (Tier A)      — blockedBy [T10, T14]
TaskCreate [T16] Generate flow tests (Tier B)          — blockedBy [T11, T14]
TaskCreate [T17] Generate unit-pure tests (Tier C)     — blockedBy [T10, T14]
TaskCreate [T18] Baseline-green verification           — blockedBy [T15, T16, T17, T13]
TaskCreate [T19] Aggregate proposals.yaml              — blockedBy [T18]
TaskCreate [T20] Checkpoint Phase 1 (1-by-1 flagged)   — blockedBy [T19]
TaskCreate [T21] Checkpoint Phase 2 (summary)          — blockedBy [T20]
TaskCreate [T22] Cleanup temp skills                   — blockedBy [T21]
TaskCreate [T23] Evidence self-commit                  — blockedBy [T22]
```

Every workflow step includes `TaskUpdate [Tn] → in_progress` before dispatch and `TaskUpdate [Tn] → completed` after eval passes. Per-unit loops (T10, T11, T12, T14–T17) use sub-task structure: one status entry per unit in `status/decode.json`, resume-capable.

---

## Workflow

### Phase: Setup — Issue Resolution + Input Validation

**Step 1 — Resolve or Create Issue (C0, F0)**
Owner: `project-orchestrator`
Depends on: pre-flight

```json
{
  "intent_path": "core/components/plays/decode/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "issue_info": "{stm_base}/{issue}/evidence/decode/issue-info.yaml"
    }
  },
  "task_id": "resolve-issue",
  "issue_arg": "{issue-or-null}",
  "branch_name": "{current-branch}",
  "invocation_args": "{feature selectors + flags for traceability}"
}
```

Record issue_number in `{stm_base}/{issue}/status/decode.json` before Step 2.

**Step 1 Eval — SE-1 (F0):** Status file exists and contains issue_number before Step 2 begins.

**Step 2 — Validate Invocation Arguments (C1, F3)**
Owner: play (inline)
Depends on: Step 1

Confirm at least one of `--feature`, `--capability`, `--domain`, `--all` supplied. Record selectors in status file.

**Step 2 Eval — SE-4 (F3):** invocation.feature_selectors is non-empty in status file.

**Step 3 — Resolve features.yaml (C2, F2)**
Owner: play (inline)
Depends on: Step 2

Resolve features.yaml per C2 order: (a) `{product_base}/scope/features.yaml`, (b) `--features-from <path>`, else hard halt. Record at `{stm_base}/{issue}/evidence/decode/features-resolution.yaml` with {source, features_path, resolution_status}.

**Step 3 Eval — SE-3 (F2):** features-resolution.yaml exists with source in {product_base, features_from} and features_path points to a readable YAML.

---

### Phase: Scan & Detection

**Step 4 — Codebase Scan (C26 dependency, F18 dependency)**
Owner: play (inline — invokes `scan-codebase` skill)
Depends on: Step 3

```json
{
  "repo_roots": ["{codebase_root}"],
  "output_path": "{stm_base}/{issue}/evidence/decode/scan-index.json",
  "size_limit_mb": 10,
  "time_limit_seconds": 600
}
```

When /decode runs back-to-back with /codify, the codify scan-index is reused if compatible.

**Step 4 Eval:** scan-index.json exists with scan_status in {complete, budget_exhausted}. If budget_exhausted, present checkpoint before proceeding.

**Step 5 — Detect Tech Stacks (C29, C30, F15)**
Owner: play (inline — invokes `detect-tech-stacks-from-physical-architecture` skill)
Depends on: Step 4

```json
{
  "physical_architecture_path": "{product_base}/architecture/physical-architecture.yaml",
  "output_path": "{stm_base}/{issue}/evidence/decode/stacks-detected.yaml",
  "kb_tech_root": "core/components/memory/knowledge/tech/"
}
```

If any detected stack has `detection_status: playbook_missing` or `unknown`, hard halt with structured error naming the missing playbook path and the detected product.

**Step 5 Eval — SE-18 (F15):** stacks-detected.yaml summary shows playbook_missing = 0 and unknown = 0 OR play halted.

**Step 6 — Synthesize Temp Tech Skills (C29, C31, F14, F16)**
Owner: play (inline — invokes `synthesize-tech-skill-from-playbook` once per detected stack)
Depends on: Step 5

For every entry in `stacks-detected.yaml/stacks` with detection_status=resolved:

```json
{
  "playbook_path": "core/components/memory/knowledge/tech/{kb_playbook}",
  "stack_record": {...single stack from stacks-detected.yaml...},
  "output_skill_dir": "{stm_base}/{issue}/evidence/decode/temp-skills/extract-from-{stack_id}/",
  "claude_skills_link_dir": ".claude/skills/decode-temp-extract-from-{stack_id}/"
}
```

**Step 6 Evals:**
- **SE-17 (F14):** temp-skills/ contains at least one synthesized skill before Step 10 dispatches.
- **SE-19 (F16):** Every synthesis-record.yaml shows output_skill_path under `{stm_base}/{issue}/evidence/decode/temp-skills/` and no write occurred to `core/components/skills/` or global `~/.claude/skills/`.

**Step 7 — Detect Test Harness (C26, F18)**
Owner: play (inline — invokes `detect-test-harness` skill)
Depends on: Step 4

```json
{
  "scan_index_path": "{stm_base}/{issue}/evidence/decode/scan-index.json",
  "codebase_root": "{codebase_root}",
  "output_path": "{stm_base}/{issue}/evidence/decode/test-harness.yaml",
  "workspace_override": "{--workspace or null}"
}
```

`runnability_summary: not-detectable` → hard halt with prerequisites error. Specs that cannot be verified green are not captured (C25).

**Step 7 Eval — SE-21 (F18):** test-harness.yaml runnability_summary in {runnable, prereq-missing} with install_command OR play halted.

---

### Phase: Unit Enumeration & Pre-checks

**Step 8 — Enumerate Target Units (C1, C4)**
Owner: play (inline)
Depends on: Step 3

Expand feature selectors against features.yaml to produce the target feature set. Discovery for flows and aspects:
- Flows: sourced from `{product_base}/experience/flows/*.md` (post-/enrich) OR inferred from E2E tests OR explicit `--flow` args (future).
- Aspects: discovered via scan-index decorator/middleware registration patterns + aspect-kind catalog.

Emit `{stm_base}/{issue}/evidence/decode/target-units.yaml` with feature_ids[], flow_ids[], aspect_ids[]. Zero-unit case (total_units=0) is valid per C16.

**Step 8 Eval — SE-35 (C16):** target-units.yaml present; when all counts zero, proceed to Phase 6 skipping extraction.

**Step 9 — Per-Unit LOC Pre-check (C11, F9)**
Owner: play (inline — uses scan-index file_surface data)
Depends on: Step 8

For every target unit, measure file_surface LOC. Units exceeding `--loc-soft-cap` (default 5000) are surfaced to user for scoping guidance before dispatch. Record at `{stm_base}/{issue}/evidence/decode/loc-precheck-reports/{unit-id}.yaml`.

**Step 9 Eval — SE-33 (C11):** Every unit has a loc-precheck report; units exceeding cap have user_decision populated before extraction dispatches.

---

### Phase: Extraction — Three-Stream, Tech-Aware (C4, C13)

**Step 10 — Feature Behavior Extraction (loop per feature)**
Owner: `tech-architect`
Depends on: Step 6 (temp skills), Step 7 (harness), Step 9 (LOC pre-check passed)

For every feature in `target-units.yaml/feature_ids`:

```json
{
  "intent_path": "core/components/plays/decode/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "feature_id": "{feature-id}",
      "feature_ref": "{features.yaml entry}",
      "file_surface": ["..."],
      "test_surface_path": "{stm_base}/{issue}/evidence/decode/test-surfaces/{feature-id}.yaml",
      "stacks_detected_path": "{stm_base}/{issue}/evidence/decode/stacks-detected.yaml",
      "temp_skills_dir": "{stm_base}/{issue}/evidence/decode/temp-skills/"
    },
    "output": {
      "spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/behaviors/{feature-id}.yaml",
      "dispatch_plan_path": "{stm_base}/{issue}/evidence/decode/dispatch-plans/{feature-id}.yaml"
    }
  },
  "ltm_context": {
    "product_base": "{product_base}",
    "core_base": "~/.garura/core/memory/",
    "query_domains": ["product", "domain", "{feature.domain}"]
  },
  "skills_to_invoke": ["extract-feature-behavior-spec"],
  "task_id": "extract-feature-{feature-id}"
}
```

Per-feature status tracked in `status/decode.json` with states: pending | in_progress | completed | deferred | failed.

**Step 10 Evals:**
- **SE-5 (F4):** Every rule/scenario/contract/integration/edge_case carries non-empty cited_locations[].
- **SE-9 (F6):** Test-code drift records ambiguities[] with both citations; never silently resolved.
- **SE-23 (F20):** Resolution-trace.yaml produced for each dispatch.
- **SE-29 (C4a):** Feature-spec has all 9 mandatory top-level sections.
- **SE-32 (C7):** When test_surface non-empty, at least one scenario has source = test_extracted or both.
- **SE-39 (C33):** Every file in file_surface appears in dispatch-plans/{feature-id}.yaml with either temp_skill or unmapped_stack.

**Step 11 — Flow Extraction (loop per flow)**
Owner: `tech-architect`
Depends on: Step 6, Step 7, Step 9

For every flow in `target-units.yaml/flow_ids`:

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "flow_id": "{flow-id}",
      "flow_metadata": {...},
      "participating_feature_ids": ["..."],
      "file_surface": ["..."],
      "stacks_detected_path": "...",
      "temp_skills_dir": "..."
    },
    "output": {
      "spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/flows/{flow-id}.yaml"
    }
  },
  "ltm_context": {...},
  "skills_to_invoke": ["extract-flow-spec"],
  "task_id": "extract-flow-{flow-id}"
}
```

**Step 11 Evals:**
- **SE-6 (F4):** Every step/trigger/success_criterion/failure_path has non-empty cited_locations[].
- **SE-30 (C4b):** Flow-spec has all 10 mandatory top-level sections.

**Step 12 — Aspect Extraction (loop per aspect)**
Owner: `tech-architect`
Depends on: Step 6, Step 7, Step 9

For every aspect in `target-units.yaml/aspect_ids`:

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "aspect_id": "{aspect-id}",
      "aspect_kind": "{auth | rate_limit | ...}",
      "aspect_metadata": {...},
      "registration_sites": ["..."],
      "stacks_detected_path": "...",
      "temp_skills_dir": "..."
    },
    "output": {
      "spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/aspects/{aspect-id}.yaml"
    }
  },
  "ltm_context": {...},
  "skills_to_invoke": ["extract-aspect-spec"],
  "task_id": "extract-aspect-{aspect-id}"
}
```

**Step 12 Evals:**
- **SE-7 (F4):** Every policy field and bypass condition has non-empty cited_locations[].
- **SE-31 (C4c):** Aspect-spec has all 8 mandatory top-level sections.

---

### Phase: Citation Integrity

**Step 13 — Verify Citation Integrity (C8, F7)**
Owner: play (inline — invokes `verify-citation-integrity` per unit)
Depends on: Steps 10, 11, 12

For every spec emitted in Steps 10–12:

```json
{
  "spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/{stream}/{unit-id}.yaml",
  "codebase_root": "{codebase_root}",
  "output_path": "{stm_base}/{issue}/evidence/decode/citation-integrity-reports/{unit-id}.yaml",
  "normalization": "trim-equivalent"
}
```

Citation-integrity failure halts the unit; the spec is NOT marked captured.

**Step 13 Eval — SE-10 (F7):** citation-integrity-report.summary.integrity_status = pass OR unit not emitted.

---

### Phase: Test Surface Mapping + Test Generation

**Step 14 — Test Surface Mapping (per unit, C14)**
Owner: `test-engineer`
Depends on: Step 8

For every feature unit (and flow unit, where tests exist at the flow boundary):

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "project_root": "{codebase_root}",
      "scope_filter": {"feature_files": ["..."]}
    },
    "output": {
      "test_surface_path": "{stm_base}/{issue}/evidence/decode/test-surfaces/{unit-id}.yaml"
    }
  },
  "ltm_context": {...},
  "skills_to_invoke": ["map-test-surface"],
  "task_id": "map-test-surface-{unit-id}"
}
```

**Step 14 Eval:** Every unit has a test-surface record (may be empty for no-tests case — flagged confidence: low per C7).

**Step 15 — Generate Contract Tests (C24 Tier A)**
Owner: `test-engineer`
Depends on: Step 10 (feature specs), Step 14 (test surface)

For every feature-spec with inbound API endpoints or outbound integrations:

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "behavior_spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/behaviors/{feature-id}.yaml",
      "test_harness_path": "{stm_base}/{issue}/evidence/decode/test-harness.yaml",
      "codebase_root": "{codebase_root}"
    },
    "output": {
      "output_dir": "{stm_base}/{issue}/evidence/decode/proposals/generated-tests/contract/"
    }
  },
  "ltm_context": {...},
  "skills_to_invoke": ["generate-contract-tests"],
  "task_id": "gen-contract-{feature-id}"
}
```

**Step 15 Eval — SE-36 (C24):** For every inbound endpoint and every outbound integration, a contract test exists.

**Step 16 — Generate Flow Tests (C24 Tier B)**
Owner: `test-engineer`
Depends on: Step 11 (flow specs), Step 14

For every flow-spec:

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "flow_spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/flows/{flow-id}.yaml",
      "test_harness_path": "{stm_base}/{issue}/evidence/decode/test-harness.yaml",
      "codebase_root": "{codebase_root}"
    },
    "output": {
      "output_dir": "{stm_base}/{issue}/evidence/decode/proposals/generated-tests/flow/"
    }
  },
  "ltm_context": {...},
  "skills_to_invoke": ["generate-flow-tests"],
  "task_id": "gen-flow-{flow-id}"
}
```

**Step 16 Eval — SE-37 (C24):** Every flow-spec has one success-path test plus one per failure_paths entry.

**Step 17 — Generate Unit-Pure Tests (C24 Tier C, F11)**
Owner: `test-engineer`
Depends on: Step 10, Step 14

For every feature-spec with pure-classifiable rules:

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "behavior_spec_path": "{stm_base}/{issue}/evidence/decode/proposals/scope/behaviors/{feature-id}.yaml",
      "test_harness_path": "{stm_base}/{issue}/evidence/decode/test-harness.yaml",
      "codebase_root": "{codebase_root}"
    },
    "output": {
      "output_dir": "{stm_base}/{issue}/evidence/decode/proposals/generated-tests/unit-pure/"
    }
  },
  "ltm_context": {...},
  "skills_to_invoke": ["generate-unit-pure-tests"],
  "task_id": "gen-unit-pure-{feature-id}"
}
```

**Step 17 Evals:**
- **SE-14 (F11):** Stack-coupled rules (Tier D) have NO generated test files; only cited_existing entries.
- **SE-38 (C24):** Every cited-specs sidecar lists pure test files for pure rules and cited_existing for stack_coupled rules.

---

### Phase: Baseline-Green Verification (C25 — ABSOLUTE GATE)

**Step 18 — Run Generated Tests Against Current Code (C15, C25, F10, F12)**
Owner: `test-runner`
Depends on: Steps 15, 16, 17, 13

For every unit, dispatch test-runner:

```json
{
  "intent_path": "...",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "test_files": ["..."],
      "test_harness_path": "{stm_base}/{issue}/evidence/decode/test-harness.yaml",
      "codebase_root": "{codebase_root}"
    },
    "output": {
      "test_run_report_path": "{stm_base}/{issue}/evidence/decode/test-run-reports/{unit-id}-{timestamp}.yaml"
    }
  },
  "task_id": "baseline-green-{unit-id}",
  "install_if_missing": "{--install-if-missing or false}"
}
```

**Critical C15 discipline:** The JSON contract passed to test-runner MUST NOT include spec paths, extraction-context paths, or any path outside (test_files, test_harness_path, codebase_root). Violating contract → test-runner returns structured failure `isolation_boundary_violated`.

Units failing baseline-green (any test red) are NOT marked captured. They route to:
- **Revise spec** → re-dispatch tech-architect Step 10/11/12 with the failure context
- **Revise test** → re-dispatch test-engineer Step 15/16/17
- **Drop rule + move to ambiguity** → update spec's ambiguities[]
- **Defer** → move to deferred per C12

Max 2 revise cycles per unit. After that, defer with reason `baseline_green_unresolvable`.

**Step 18 Evals:**
- **SE-13 (F10):** Every captured proposal has baseline_green = true.
- **SE-15 (F12):** test-runner dispatch contracts contain only (test_files, test_harness_path, codebase_root).

---

### Phase: Aggregation + Checkpoint

**Step 19 — Aggregate proposals.yaml (C20, F23, F24)**
Owner: play (inline — invokes `aggregate-decode-proposals` skill)
Depends on: Step 18

```json
{
  "proposals_root": "{stm_base}/{issue}/evidence/decode/proposals/",
  "test_run_reports_dir": "{stm_base}/{issue}/evidence/decode/test-run-reports/",
  "citation_integrity_reports_dir": "{stm_base}/{issue}/evidence/decode/citation-integrity-reports/",
  "features_yaml_path": "{resolved features.yaml}",
  "product_base": "{product_base}",
  "output_path": "{stm_base}/{issue}/evidence/decode/proposals.yaml",
  "skipped_units": ["..."]
}
```

**Step 19 Evals:**
- **SE-2 (F1):** Every artifact_path rooted under `{stm_base}/{issue}/evidence/decode/`.
- **SE-11 (F8):** Every proposal's spec has all mandatory sections.
- **SE-12 (F9):** Every proposal has generated_tests_refs[] OR cited_existing references.
- **SE-16 (F13):** Cross-stream linking populated.
- **SE-22 (F19):** Every proposal has learning_category = product and sub_category = null.
- **SE-26 (F23):** proposals.yaml exists; zero-unit runs have alignment_confirmed=true.
- **SE-27 (F24):** Every completed unit has spec file on disk, generated tests on disk, baseline_green = true.
- **SE-28 (C4):** Three-stream output counts present; zero-stream is explained.
- **SE-34 (C12):** Deferred units appear in skipped_units with partial output under proposals/deferred/.

**Step 20 — Checkpoint Phase 1: 1-by-1 Surfacing (C17, C18, F22)**
Owner: play
Depends on: Step 19

Filter proposals meeting ANY flag: `overall_confidence: "low"` | non-empty `ambiguities[]` | `citation_integrity_status: fail` | deferred status | no-tests fallback | LOC over-budget proceed-anyway.

For each flagged unit, present:

```markdown
## Flagged Proposal {n}/{total-flagged} — Issue #{issue}

**Stream:** {behaviors | flows | aspects}
**Unit:** {unit-id}
**Spec:** {spec_path}
**Flags:** {list}
**Confidence:** {overall_confidence}
**Ambiguities:** {count} — {if present, show first 2 with both citations}
**Baseline green:** {true | false | deferred}

### Summary
{from spec meta block}

---
Type **Tether** (accept), **Vanish** (reject), or **Orbit** (defer).
```

Record each decision in `{stm_base}/{issue}/evidence/decode/checkpoint-decisions.yaml`. If zero flagged units, skip this phase entirely.

**Step 20 Eval — SE-25 (F22):** checkpoint-decisions.yaml has an entry per flagged unit.

**Step 21 — Checkpoint Phase 2: Summary (C17, F21)**
Owner: play
Depends on: Step 20

Present aggregate summary:

```markdown
## Decode Summary — Issue #{issue}

**Total units:** {total}
- Features: {behaviors_count}
- Flows: {flows_count}
- Aspects: {aspects_count}

**Confidence:** high: {h} | medium: {m} | low: {l}

**Tests generated:**
- Tier A contract: {contract_count}
- Tier B flow: {flow_count}
- Tier C unit-pure: {unit_pure_count}
- Cited existing (stack-coupled): {cited_existing_count}

**Baseline green:** pass: {pass} | fail: {fail} | deferred: {deferred}

**Skipped phases:** {skipped_phases}
**Knowledge gaps flagged:** {gap_count}

{if total_units == 0: **Alignment confirmed** — no units matched selectors, zero-unit exit clean.}

**Proposals location:** `{stm_base}/{issue}/evidence/decode/proposals/`
**Master index:** `{stm_base}/{issue}/evidence/decode/proposals.yaml`

---
Type **Tether** to commit evidence AND cleanup temp skills, or **Vanish** to halt without committing (temp skills preserved for resume).
```

**Step 21 Eval — SE-24 (F21):** Both checkpoint phases completed before evidence-commit transitions to in_progress.

---

### Phase: Cleanup + Evidence Close

**Step 22 — Cleanup Temp Skills (C32, F17)**
Owner: play (inline — invokes `cleanup-temp-skills` skill)
Depends on: Step 21 (Tether)

On final Tether:

```json
{
  "stm_temp_skills_dir": "{stm_base}/{issue}/evidence/decode/temp-skills/",
  "claude_skills_link_root": ".claude/skills/",
  "trigger": "tether",
  "output_path": "{stm_base}/{issue}/evidence/decode/cleanup-report.yaml"
}
```

On Vanish, this step is skipped (temp skills preserved for resume). On `--force-cleanup`, trigger = `force`.

**Step 22 Eval — SE-20 (F17):** cleanup-report.yaml status = success after Tether; temp-skills/ removed.

**Step 23 — Evidence Self-Commit (C18)**
Owner: `repo-orchestrator`
Depends on: Step 22

```json
{
  "intent_path": "core/components/plays/decode/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "proposals_index": "{stm_base}/{issue}/evidence/decode/proposals.yaml"
    },
    "output": {}
  },
  "task_id": "evidence-commit",
  "commit_message": "chore(stm): record decode evidence for #{issue}",
  "stage_paths": [
    "{stm_base}/{issue}/evidence/decode/proposals/",
    "{stm_base}/{issue}/evidence/decode/proposals.yaml",
    "{stm_base}/{issue}/evidence/decode/citation-integrity-reports/",
    "{stm_base}/{issue}/evidence/decode/test-run-reports/",
    "{stm_base}/{issue}/evidence/decode/checkpoint-decisions.yaml",
    "{stm_base}/{issue}/evidence/decode/cleanup-report.yaml",
    "{stm_base}/{issue}/status/decode.json"
  ]
}
```

Non-blocking per C18. Commit failure logged; play exits cleanly.

---

## Scenario Validation

At play end, assert the 11 scenarios hold against evidence:

- **SCE-1 (S1 — Single-feature single-stack):** See evals.yaml SCE-1. Feature proposal captured, temp skill synthesized + cleaned up, evidence committed.
- **SCE-2 (S2 — Multi-stack capability):** See evals.yaml SCE-2. 3 temp skills synthesized, resume works, cleanup after final Tether.
- **SCE-3 (S3 — Test/code drift):** See evals.yaml SCE-3. Ambiguity recorded with both citations, user decides before test generated.
- **SCE-4 (S4 — Horizontal aspect):** See evals.yaml SCE-4. Full rate-limit policy captured, applies_to populated, one representative verification test.
- **SCE-5 (S5 — Vertical flow crossing features):** See evals.yaml SCE-5. Ordered participating_features, cross-links on feature-specs, E2E test green.
- **SCE-6 (S6 — Missing playbook):** See evals.yaml SCE-6. Hard halt with missing playbook path, no extraction.
- **SCE-7 (S7 — Generated test fails on current code):** See evals.yaml SCE-7. Baseline-green halts unit, options presented, ambiguity after drop-rule.
- **SCE-8 (S8 — Vanish + resume):** See evals.yaml SCE-8. Temp skills preserved on Vanish, resume skips completed, cleanup only on Tether.
- **SCE-9 (S9 — Flow with no existing E2E):** See evals.yaml SCE-9. Generated Playwright test passes, flow captured.
- **SCE-10 (S10 — /decode after /codify before /enrich):** See evals.yaml SCE-10. --features-from accepted, all units Tier 3.
- **SCE-11 (S11 — Non-runnable environment):** See evals.yaml SCE-11. Hard halt with prerequisites error, no specs emitted.

---

## Recovery

When an agent returns structured failure:
- For extraction failures (F4-F9): tech-architect is responsible; max 2 retries with fix context; defer on persistent failure.
- For test-generation failures (F11): test-engineer is responsible.
- For test-runner failures (F10, F12): isolation boundary violations → play-level contract repair; other failures → re-dispatch after spec/test revise.
- For synthesis failures (F14, F15, F16): play-level; surface to user.
- For cleanup failures (F17): log warning; non-blocking per C18.

Max 2 retry cycles per agent per unit. After that, defer with reason and surface at Phase 1.

## Pause and Resume

Steps run top-to-bottom with per-unit resumability in Steps 10–18.

**Status file:** `{stm_base}/{issue}/status/decode.json`

```json
{
  "play": "decode",
  "issue_number": "{issue}",
  "started_at": "{ISO}",
  "invocation": {
    "feature_selectors": ["..."],
    "features_source": "product_base | features_from",
    "loc_soft_cap": 5000
  },
  "tasks": {
    "resolve-issue": {"status": "completed"},
    "validate-invocation": {"status": "completed"},
    "resolve-features-yaml": {"status": "completed"},
    "codebase-scan": {"status": "completed"},
    "detect-tech-stacks": {"status": "completed"},
    "synthesize-temp-skills": {"status": "completed"},
    "detect-test-harness": {"status": "completed"},
    "enumerate-units": {"status": "completed"},
    "loc-precheck": {"status": "completed"},
    "extract-features": {"status": "in_progress", "progress": {"completed": 5, "total": 12, "current_unit": "MEM-F006"}},
    "extract-flows": {"status": "pending"},
    "extract-aspects": {"status": "pending"},
    "citation-integrity": {"status": "pending"},
    "map-test-surfaces": {"status": "completed"},
    "gen-contract-tests": {"status": "pending"},
    "gen-flow-tests": {"status": "pending"},
    "gen-unit-pure-tests": {"status": "pending"},
    "baseline-green": {"status": "pending"},
    "aggregate": {"status": "pending"},
    "checkpoint-phase-1": {"status": "pending"},
    "checkpoint-phase-2": {"status": "pending"},
    "cleanup-temp-skills": {"status": "pending"},
    "evidence-commit": {"status": "pending"}
  },
  "units": {
    "MEM-F001": {"status": "completed", "spec_path": "...", "baseline_green": true, "completed_at": "..."},
    "MEM-F002": {"status": "completed", "spec_path": "...", "baseline_green": true, "completed_at": "..."},
    "MEM-F006": {"status": "in_progress", "started_at": "..."},
    "MEM-F007": {"status": "pending"}
  }
}
```

**Executor loop:**
```
resolve issue per C0
check status file

for each step in compiled order:
  if status "completed" → skip
  if status "in_progress" → reset to pending (abandoned)
  mark "in_progress"
  for each unit (if looping step):
    if unit.status == "completed" → skip
    if unit.status == "in_progress" → reset to pending
    dispatch unit
    mark unit completed on success
  mark step completed on all units complete
```

**Fresh start:** No status → execute all, create file on Step 1.
**Resume:** Skip completed, continue from first incomplete unit.

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:a92996ba25964fa33c26b4290b0ffa8bc68fcdc681589318193e90e9667f2311 |
| compiled_by | create-play |
| compiled_at | 2026-04-23 |
| workflow_structure | A (full checkpoint flow — 2-phase checkpoint, conditional synthesis phase, non-blocking evidence commit, per-unit resumability) |
| domain_agents | 3 (tech-architect, test-engineer, test-runner) |
| utility_agents | 1 (repo-orchestrator) |
| step_evals | 39 (SE-1 through SE-39) |
| scenario_evals | 11 (SCE-1 through SCE-11) |
| constraints_covered | C0–C33 (38 constraints including C4a–d) |
| failure_conditions_covered | F0–F24 (25 failure conditions) |
| new_skills | 13 (detect-tech-stacks-from-physical-architecture, synthesize-tech-skill-from-playbook, cleanup-temp-skills, detect-test-harness, run-generated-tests-isolated, verify-citation-integrity, aggregate-decode-proposals, extract-feature-behavior-spec, extract-flow-spec, extract-aspect-spec, generate-contract-tests, generate-flow-tests, generate-unit-pure-tests) |
| new_agents | 1 (test-runner) |
| reused_skills | 3 (scan-codebase, map-test-surface, validate-abstraction-layer) |
| prerequisite | Tech playbooks at `core/components/memory/knowledge/tech/{stack}.md` for every stack this play encounters. None ship by default — playbooks are authored as needed. |
