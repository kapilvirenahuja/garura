---
name: prepare-epic
description: "Produce implementation-ready design artifacts for an epic. Deep codebase understanding within locked architecture, blast radius analysis, interface-level LLD, 3-tier scenarios, and task DAG. Requires locked upstream artifacts from specify-product, design-exp, and build-arch. Outputs: tech.yaml (LLD), scenarios.yaml, plan.yaml — all locked."
user-invocable: true
model: opus
---

# prepare-epic

Given an epic, produce a complete set of locked, implementation-ready design artifacts. Upstream locked artifacts from specify-product (epics, scope, quality profile), design-exp (design-spec, screens), and build-arch (5 architecture artifacts) are REQUIRED inputs — hard halt if absent. Before generating any design, the play deeply understands the codebase through architecture inference (reading locked architecture as input), test surface mapping, dependency graph construction, and git history analysis — all in parallel. It then computes the blast radius via test impact analysis, identifies coverage gaps, and specifies baseline tests for uncharacterized code paths. Only after the blast radius is fully characterized and the user approves does LLD generation begin. The LLD (tech.yaml) defines interface-level design: API contracts, internal interfaces, frontend specs, backend specs, data specs, communication channels, and mock strategy — NOT file-level before/after code. Scenarios.yaml provides 3-tier verification scenarios. Plan.yaml provides the task DAG. All artifacts locked after human review. All work is issue-mapped. All intermediate data is stored on disk in STM.

## Compiled From

This play was compiled from `reference/intent.yaml` by `/create-play`.
Intent defines constraints (C1-C40), failure conditions (F1-F29), and scenarios (S1-S13).
To modify this play, update `reference/intent.yaml` and re-run `/create-play --build prepare-epic`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the workflow. You delegate domain tasks to agents via JSON contracts — never execute domain work directly.

**Forbidden:** Direct codebase analysis, direct architecture inference, direct test surface mapping, direct scenario authoring. All domain work goes through agents.

**Agent Boundaries:**

| Agent | Type | Domain | Phases |
|-------|------|--------|--------|
| `tech-architect` | domain | Architecture inference (reads locked arch as input), dependency graph, git history, LTM consultation, context assembly, change surface, LLD (tech.yaml + tech.md), plan.yaml + plan.md | Context Resolution (Steps 2, 4, 5, 6, 7), Blast Radius (Step 8), DRAFT LLD (Step 11), DRAFT Plan (Step 14) |
| `test-engineer` | domain | Test surface mapping, blast radius computation, baseline test specification, three-tier scenarios | Context Resolution (Step 3), Blast Radius (Steps 9, 10), DRAFT Scenarios (Step 13) |
| `feature-steward` | domain | Cross-validation against upstream locked epics | VALIDATE (Step 16) |
| `doc-builder` | utility | HTML briefs for each checkpoint | LLD Brief (Step 12), Scenarios + Plan Brief (Step 15) |
| `repo-orchestrator` | utility | Evidence self-commit | Evidence & Close (Step 20) |

## Pre-flight

Execute these checks before any domain work:

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| Resolve `stm_base` from `.garura/core/config.yaml` | — | Hard halt — config required |
| Resolve `product_base` from `.garura/core/config.yaml` | — | Hard halt — config required |
| Validate ALL upstream locked artifacts exist | C1, C34 | **Hard halt** — no discovery mode, no fallback |
| Resolve issue from branch name OR caller input | C29 | Continue to issue creation |
| Working tree clean check | C30 | Warn or stash |

### Upstream Artifact Validation (HARD HALT)

**ALL of the following must exist. Missing any one is a hard halt (C1, C34, F24):**

```bash
# Resolve paths from config
stm_base=$(grep 'base-path' .garura/core/config.yaml | head -1 | awk '{print $2}')
product_base=$(grep 'base-path' .garura/core/config.yaml | tail -1 | awk '{print $2}')

# specify-product artifacts
[ -f "${product_base}/scope/epics/${epic_id}.yaml" ] || HALT "Missing: locked epic"
[ -f "${product_base}/scope/scope.yaml" ] || HALT "Missing: scope"
[ -f "${product_base}/specification/quality-profile.yaml" ] || HALT "Missing: quality profile"

# design-exp artifacts
[ -f "${product_base}/experience/design-spec.md" ] || HALT "Missing: design spec"
# screens at ${product_base}/experience/screens/*.md

# build-arch 5-artifact output
[ -f "${product_base}/architecture/logical-architecture.yaml" ] || HALT "Missing: logical architecture"
[ -f "${product_base}/architecture/physical-architecture.yaml" ] || HALT "Missing: physical architecture"
[ -f "${product_base}/architecture/nfr-spec.yaml" ] || HALT "Missing: NFR spec"
[ -f "${product_base}/architecture/quality-vision.yaml" ] || HALT "Missing: quality vision"
[ -f "${product_base}/architecture/design-patterns.yaml" ] || HALT "Missing: design patterns"
```

**No discovery mode. No fallback hierarchy. No degraded execution.**

**SE-1 (C1, C34, F24):** Every upstream artifact path is checked. If any artifact is missing, the play halts with a clear error identifying the missing artifact(s). No fallback to discovery mode. No degraded execution. The play does not proceed to context resolution without all upstream artifacts confirmed present.

### Context Paths

```
{stm_base}/{issue}/context/
├── understanding/     # Phase 1: codebase understanding
├── blast-radius/      # Phase 2: impact analysis
├── design/            # Phase 3: implementation artifacts (tech.yaml, scenarios.yaml, plan.yaml)
└── briefs/            # Checkpoint HTML briefs
```

These paths are referred to as:
- `{context_base}` = `{stm_base}/{issue}/context/`
- `{understanding}` = `{context_base}/understanding/`
- `{blast_radius}` = `{context_base}/blast-radius/`
- `{design}` = `{context_base}/design/`
- `{briefs}` = `{context_base}/briefs/`

Context is sacred — it's the input to implement-epic, not a record of play execution.

**Evidence path (play execution records):** `{stm_base}/{issue}/evidence/prepare-epic/`
Eval results, checkpoint timestamps, and evidence files live here — separate from context.

**Resume check:** If `{stm_base}/{issue}/status/prepare-epic.json` exists, resume — skip completed steps, reset any `in_progress` to pending, continue from first incomplete.

---

## Workflow

### Phase 0: Setup

Setup is idempotent — safe to run when start-feature has already executed.

---

**Step 0a — Resolve or Create Issue (C29, F21)**
Owner: `project-orchestrator`
Depends on: pre-flight

If an issue number was resolved from the branch name or caller input, validate it exists. If no issue can be resolved, delegate to `project-orchestrator` to create a new issue from the work description. The play MUST NOT proceed to Context Resolution without a resolved issue number.

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "issue_resolution": "{stm_base}/_pending/prepare-epic/issue-resolution.yaml"
    }
  },
  "task_id": "resolve-issue"
}
```

**Step 0a Eval:** An issue number is resolved before Step 0b begins. `issue_resolution.yaml` contains a valid issue number. FAIL (F21): play proceeds to context resolution without resolved issue.

---

**Step 0b — Create Feature Branch (C30, F22)**
Owner: `repo-orchestrator`
Depends on: Step 0a

If already on a feature branch matching `{type}/{issue}-{slug}` convention, reuse it. Otherwise, create and switch to the branch. Push to origin.

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_resolution": "{stm_base}/_pending/prepare-epic/issue-resolution.yaml"
    },
    "output": {
      "branch_result": "{stm_base}/{issue}/evidence/prepare-epic/branch-result.yaml"
    }
  },
  "task_id": "create-branch"
}
```

**Step 0b Eval:** A feature branch exists and is checked out before any artifact is written. FAIL (F22): artifacts locked without feature branch matching type/issue-slug.

---

**Step 0c — Initialize STM Directory (C31, F23)**
Owner: play (inline — mkdir -p is infrastructure, not domain work)
Depends on: Step 0b

```bash
mkdir -p "${stm_base}/${issue}/context/understanding"
mkdir -p "${stm_base}/${issue}/context/blast-radius"
mkdir -p "${stm_base}/${issue}/context/design"
mkdir -p "${stm_base}/${issue}/context/briefs"
mkdir -p "${stm_base}/${issue}/evidence/prepare-epic"
mkdir -p "${stm_base}/${issue}/status"
```

Once issue number is known, relocate pending artifacts:
`mv {stm_base}/_pending/prepare-epic/* → {stm_base}/{issue}/evidence/prepare-epic/`

**Step 0c Eval:** STM directory at `{stm_base}/{issue}/` exists with all required subdirectories before Step 0d begins. FAIL (F23): any design artifact written before STM init.

---

**Step 0d — Read Upstream Locked Artifacts (C1, C34, F24)**
Owner: play (inline)
Depends on: Step 0c

Read all upstream locked artifacts. Record paths and status. ALL must be present — confirmed in pre-flight. No discovery mode. No fallback hierarchy.

```yaml
# Record upstream artifact inventory
upstream_artifacts:
  specify_product:
    epic: { path: "{product_base}/scope/epics/{epic_id}.yaml", status: "locked" }
    scope: { path: "{product_base}/scope/scope.yaml", status: "locked" }
    quality_profile: { path: "{product_base}/specification/quality-profile.yaml", status: "locked" }
  design_exp:
    design_spec: { path: "{product_base}/experience/design-spec.md", status: "locked" }
    screens: { path: "{product_base}/experience/screens/*.md", status: "locked" }
  build_arch:
    logical_architecture: { path: "{product_base}/architecture/logical-architecture.yaml", status: "locked" }
    physical_architecture: { path: "{product_base}/architecture/physical-architecture.yaml", status: "locked" }
    nfr_spec: { path: "{product_base}/architecture/nfr-spec.yaml", status: "locked" }
    quality_vision: { path: "{product_base}/architecture/quality-vision.yaml", status: "locked" }
    design_patterns: { path: "{product_base}/architecture/design-patterns.yaml", status: "locked" }
```

**Step 0d Eval (SE-1):** Every upstream artifact path is checked and confirmed present. All statuses "locked". FAIL (F24): any upstream locked artifact not found at expected path.

---

### Phase 1: Context Resolution

**Parallel execution:** Steps 2, 3, 4, 5, and 6 are independent data collection tasks. Dispatch all five in parallel. Step 7 (context assembly) depends on all five completing first.

---

**Step 2 — Architecture Inference**
Owner: `tech-architect`
Depends on: Step 0d
Runs in parallel with Steps 3, 4, 5, 6

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_path": "{stm_base}/{issue}/issue.md",
      "logical_architecture_path": "{product_base}/architecture/logical-architecture.yaml",
      "physical_architecture_path": "{product_base}/architecture/physical-architecture.yaml",
      "design_patterns_path": "{product_base}/architecture/design-patterns.yaml",
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml"
    },
    "output": {
      "architecture_inference_yaml": "{understanding}/architecture-inference.yaml",
      "architecture_inference_md": "{understanding}/architecture-inference.md"
    }
  },
  "task_id": "1A-architecture-inference"
}
```

Agent reads the locked architecture as INPUT (not deriving from scratch). Infers issue-scoped impact: which modules, services, data flows, and design patterns from the locked architecture are relevant to this epic. Identifies: module/package structure, service boundaries, data flow, external dependencies, design patterns in use (from locked design-patterns.yaml), framework conventions, logical architecture mapping.

Produces dual format (C27):
- `architecture-inference.yaml` — machine-readable contract fields for downstream agents
- `architecture-inference.md` — rich markdown with mermaid/ASCII diagrams, pattern explanations, logical architecture narrative showing how the epic maps onto the locked architecture

**Step 2 Evals (SE-20):**
- architecture-inference.yaml and .md both exist
- yaml contains: module_structure[], design_patterns[] (>=1), framework_conventions[], logical_architecture
- .md contains diagrams showing how epic maps onto locked architecture
- No field contains "see codebase", "follow existing patterns", or "use available port" — all content explicit (F12)

---

**Step 3 — Test Surface Mapping**
Owner: `test-engineer`
Depends on: Step 0d
Runs in parallel with Steps 2, 4, 5, 6

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "test_surface_path": "{understanding}/test-surface.yaml"
    }
  },
  "task_id": "1B-test-surface-mapping"
}
```

Agent discovers all test files, reads each to map subjects (production files exercised), specific functions tested, assertions made, fixtures and mocks used, test type (unit, integration, e2e). Framework detected from package.json, pyproject.toml, jest.config.js, etc. — never assumed.

**Step 3 Evals:**
- test-surface.yaml exists
- Contains summary{total_test_files, total_test_cases, frameworks[], types{unit, integration, e2e}}
- Each test entry has: file, type, framework, subjects[], fixtures[]
- Agent was test-engineer (task_id "1B-test-surface-mapping") — confirms C28 agent separation
- Greenfield: empty tests[] is valid when no test files exist in the codebase

---

**Step 4 — Dependency Graph**
Owner: `tech-architect`
Depends on: Step 0d
Runs in parallel with Steps 2, 3, 5, 6

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_path": "{stm_base}/{issue}/issue.md"
    },
    "output": {
      "dependency_graph_yaml": "{understanding}/dependency-graph.yaml",
      "dependency_graph_md": "{understanding}/dependency-graph.md"
    }
  },
  "task_id": "1C-dependency-graph"
}
```

Agent builds dependency graph: file imports (static, dynamic), module API calls (which module calls which module's exported functions), service messaging (message queues, event buses, HTTP calls). Computes transitive closure. Identifies fan-in hotspots (files imported by many = high blast radius risk) and coupling clusters (dense mutual dependency groups).

Produces dual format (C27):
- `dependency-graph.yaml` — structured nodes[] and edges[]
- `dependency-graph.md` — mermaid graph or ASCII visualization, coupling cluster explanations

**Step 4 Evals:**
- Both dependency-graph.yaml and .md exist
- yaml contains nodes[] and edges[] (may be empty for greenfield with no imports)
- .md contains at least one visual representation (mermaid or ASCII diagram)

---

**Step 5 — Git History Analysis**
Owner: `tech-architect`
Depends on: Step 0d
Runs in parallel with Steps 2, 3, 4, 6

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_path": "{stm_base}/{issue}/issue.md"
    },
    "output": {
      "commit_history_yaml": "{understanding}/commit-history-analysis.yaml",
      "commit_history_md": "{understanding}/commit-history-analysis.md"
    }
  },
  "task_id": "1D-git-history"
}
```

Agent analyzes last 90 days of git history, filtered for relevance to the current issue. Extracts: relevant commits (messages and file lists), co-change patterns (files that consistently change together — frequency count), recent activity areas (directories with most recent changes), commit intent from message analysis, and knowledge distribution by contributor.

Co-change patterns reveal coupling that the static dependency graph misses. Files that always change together in commits should be treated as coupled even if they have no import relationship (C25).

Produces dual format (C27):
- `commit-history-analysis.yaml` — structured co-change data, relevant_commits[], recent_activity[]
- `commit-history-analysis.md` — narrative context, co-change pattern explanations, timeline visualization

**Step 5 Evals:**
- Both commit-history-analysis.yaml and .md exist
- yaml contains co_change_patterns[] (may be empty if no history) and relevant_commits[]
- .md contains narrative explanation of patterns found

---

**Step 6 — LTM Consultation**
Owner: `tech-architect`
Depends on: Step 0d
Runs in parallel with Steps 2, 3, 4, 5

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_path": "{stm_base}/{issue}/issue.md"
    },
    "output": {
      "ltm_findings_path": "{understanding}/ltm-findings.yaml"
    }
  },
  "task_id": "1E-ltm-consultation",
  "ltm_context": {
    "core_base": "~/.garura/core/memory/",
    "query_domains": ["architecture", "implementation-patterns", "quality-standards", "testing-patterns"]
  }
}
```

Agent reads `~/.garura/core/memory/` for architectural standards, implementation patterns, technology decisions, prior learnings, and quality patterns. Documents both what was found AND what was not found in LTM (gaps). LTM gaps tell the implementation agent what organizational knowledge is missing — they are as important as findings (C15).

**Step 6 Evals:**
- ltm-findings.yaml exists
- Contains both findings[] and gaps[] sections
- gaps[] explicitly documents what LTM did NOT cover (even if empty — must state "consulted, nothing applicable found")
- FAIL (F13): if ltm-findings.yaml absent or contains only findings without gap documentation

---

**Step 7 — Context Assembly**
Owner: `tech-architect`
Depends on: Steps 2, 3, 4, 5, 6 (all must complete)

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "architecture_inference_yaml": "{understanding}/architecture-inference.yaml",
      "test_surface_path": "{understanding}/test-surface.yaml",
      "dependency_graph_yaml": "{understanding}/dependency-graph.yaml",
      "commit_history_yaml": "{understanding}/commit-history-analysis.yaml",
      "ltm_findings_path": "{understanding}/ltm-findings.yaml",
      "issue_path": "{stm_base}/{issue}/issue.md",
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml",
      "logical_architecture_path": "{product_base}/architecture/logical-architecture.yaml",
      "physical_architecture_path": "{product_base}/architecture/physical-architecture.yaml",
      "nfr_spec_path": "{product_base}/architecture/nfr-spec.yaml",
      "quality_vision_path": "{product_base}/architecture/quality-vision.yaml",
      "design_patterns_path": "{product_base}/architecture/design-patterns.yaml",
      "design_spec_path": "{product_base}/experience/design-spec.md"
    },
    "output": {
      "context_assembly_path": "{understanding}/context-assembly.yaml"
    }
  },
  "task_id": "context-assembly"
}
```

Agent assembles all Phase 1 outputs into a unified context document. Context resolution follows a hierarchy with two levels (C19):
- **locked** — from upstream plays (specify-product, design-exp, build-arch)
- **issue-scoped** — from codebase analysis (architecture inference, test surface mapping, dependency graph, git history)

Each source has an authority level. The play documents which source provided each piece of context.

```yaml
# context-assembly.yaml structure (simplified — only 2 source types)
upstream_context:
  specify_product:
    status: locked
    artifacts:
      epic: { path: "...", confidence: authoritative }
      scope: { path: "...", confidence: authoritative }
      quality_profile: { path: "...", confidence: authoritative }
  design_exp:
    status: locked
    artifacts:
      design_spec: { path: "...", confidence: authoritative }
      screens: { path: "...", confidence: authoritative }
  build_arch:
    status: locked
    artifacts:
      logical_architecture: { path: "...", confidence: authoritative }
      physical_architecture: { path: "...", confidence: authoritative }
      nfr_spec: { path: "...", confidence: authoritative }
      quality_vision: { path: "...", confidence: authoritative }
      design_patterns: { path: "...", confidence: authoritative }

issue_scoped_context:
  architecture_inference: { path: "...", confidence: derived }
  test_surface: { path: "...", confidence: derived }
  dependency_graph: { path: "...", confidence: derived }
  commit_history: { path: "...", confidence: derived }
  ltm_findings: { path: "...", confidence: advisory }
```

**Step 7 Evals (SE-20):**
- context-assembly.yaml exists
- Every upstream artifact has a documented entry with path and confidence
- All upstream statuses are "locked"
- Issue-scoped entries all present
- LTM findings and gaps both documented (C15, F13)
- Git history co-change patterns included (C25)

---

### Checkpoint 0: Context Assembly Approval

Owner: play
Depends on: Step 7

Read context-assembly.yaml, architecture-inference.md, and test-surface.yaml from STM. Present to user:

```markdown
## Context Assembly — #{issue}: {title}

**Upstream artifacts:** ALL locked (specify-product: 3, design-exp: 2, build-arch: 5)

### How This Epic Maps Onto The Locked Architecture
{Summary from architecture-inference.md — which modules, services, patterns are relevant}

**Design patterns in scope:**
{For each relevant pattern from locked design-patterns.yaml: - {pattern}: {location} ({relevance to epic})}

**Framework conventions:**
{For each framework_convention: - {framework}: {conventions}}

### LTM Findings
**Relevant standards found:** {count} — {brief descriptions}
**LTM gaps (no coverage):** {count} — {gap descriptions if any}

### Test Surface Summary
**Test files:** {total_test_files}
**Test cases:** {total_test_cases}
**Frameworks:** {frameworks}
**Types:** {unit} unit, {integration} integration, {e2e} e2e

### Co-Change Patterns
{Key co-change patterns from git history relevant to this epic}

---
Type **Tether** to approve and proceed to blast radius analysis.
Type **Vanish** to halt.
Type **Orbit** with feedback to revise context understanding before proceeding.
```

**Orbit handling:** Address feedback inline (update context-assembly.yaml or re-dispatch relevant Step 2-6 agent), then re-present Checkpoint 0.

**SE-20 (C10, C14, C15, C16, C19, C25, F10, F11, F13):** User must type "Tether" before Step 8 executes. Context assembly shows how epic maps onto locked architecture. LTM findings and gaps documented. Git history included. FAIL if Step 8 began without "Tether" at this checkpoint.

---

### Phase 2: Blast Radius Analysis

---

**Step 8 — Change Surface Identification**
Owner: `tech-architect`
Depends on: Checkpoint 0

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_path": "{stm_base}/{issue}/issue.md",
      "architecture_inference_yaml": "{understanding}/architecture-inference.yaml",
      "dependency_graph_yaml": "{understanding}/dependency-graph.yaml",
      "commit_history_yaml": "{understanding}/commit-history-analysis.yaml",
      "ltm_findings_path": "{understanding}/ltm-findings.yaml",
      "context_assembly_path": "{understanding}/context-assembly.yaml",
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml"
    },
    "output": {
      "change_surface_path": "{blast_radius}/change-surface.yaml"
    }
  },
  "task_id": "2A-change-surface"
}
```

Agent identifies every file that must change: directly stated in the epic (high confidence), implied by dependency graph (medium confidence), or suggested by co-change patterns in git history (medium/low confidence). Classifies each as CREATE, MODIFY, or DELETE. Documents what changes within each file, interface changes, and data model changes.

Co-change patterns from Step 5 are a required input — files that always change together should be included even if not directly referenced in the epic description.

**Step 8 Evals:**
- change-surface.yaml exists
- files[] contains at least one entry with: path, action (CREATE|MODIFY|DELETE), what_changes, confidence, source
- interfaces_changed[] and data_models_changed[] present (may be empty arrays)

---

**Step 9 — Blast Radius Computation**
Owner: `test-engineer`
Depends on: Step 8

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "test_surface_path": "{understanding}/test-surface.yaml",
      "change_surface_path": "{blast_radius}/change-surface.yaml",
      "dependency_graph_path": "{understanding}/dependency-graph.yaml"
    },
    "output": {
      "blast_radius_path": "{blast_radius}/blast-radius.yaml"
    }
  },
  "task_id": "2B-blast-radius"
}
```

Agent intersects the change surface with the test surface using the dependency graph. Produces:
- `directly_impacted[]`: tests that exercise files in the change surface directly. would_break assessed.
- `transitively_impacted[]`: tests that exercise files depending on changed files. Full dependency_chain documented.
- `coverage_gaps[]`: files in the change surface with no test coverage. baseline_test_needed flag set.
- `regression_surface[]`: tests that touch the change surface but would NOT break — regression safety net.
- `summary{}`: integer counts of all categories.

**Step 9 Evals:**
- blast-radius.yaml exists
- Contains: directly_impacted[], transitively_impacted[], coverage_gaps[], regression_surface[], summary{}
- summary.total_tests_impacted is an integer (0 valid for greenfield)
- Each coverage_gaps entry has baseline_test_needed: true | false
- Agent was test-engineer (task_id "2B-blast-radius") — confirms C28 separation from tech-architect

---

**Step 10 — Baseline Test Specification**
Owner: `test-engineer`
Depends on: Step 9

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "blast_radius_path": "{blast_radius}/blast-radius.yaml",
      "test_surface_path": "{understanding}/test-surface.yaml"
    },
    "output": {
      "baseline_tests_path": "{blast_radius}/baseline-tests.yaml"
    }
  },
  "task_id": "2C-baseline-tests"
}
```

Agent specifies a baseline test for every coverage gap where `baseline_test_needed: true`. Baseline tests describe CURRENT behavior — what the unmodified codebase does RIGHT NOW (C21). They must pass before any changes are applied. They are the contract that proves the existing behavior is understood before it is changed.

Agent reads the actual production code for each coverage gap before specifying the test. Never guesses behavior from file names or comments.

Framework matches the existing codebase test framework (detected from test-surface.yaml).

**Greenfield exception:** When test-surface.yaml is empty and all change-surface.yaml actions are CREATE, baseline-tests.yaml may be an empty list.

**Completeness signal (C20, C21):** When every `coverage_gaps[]` entry with `baseline_test_needed: true` has a corresponding BT-* in baseline-tests.yaml, the blast radius is fully characterized. Design artifacts may now be generated.

**Step 10 Evals (SE-21):**
- baseline-tests.yaml exists
- For EVERY blast-radius.yaml/coverage_gaps entry with baseline_test_needed=true: a corresponding BT-* entry exists (F16, F19)
- Each BT-* contains: coverage_gap_ref, target{file, area}, current_behavior, test_spec{type, framework, setup, action, assert}, priority, reason
- assert field describes CURRENT behavior — not the future target state after changes (C21)

---

### Checkpoint 1: Blast Radius Approval

Owner: play
Depends on: Step 10

Read blast-radius.yaml and baseline-tests.yaml from STM. Present to user:

```markdown
## Blast Radius — #{issue}: {title}

**Change surface:** {N files} — {CREATE_count} create, {MODIFY_count} modify, {DELETE_count} delete
**Tests directly impacted:** {directly_impacted count}
**Tests transitively impacted:** {transitively_impacted count}
**Tests that would break:** {tests_that_would_break}
**Coverage gaps:** {coverage_gaps count} ({baseline_tests_needed} baseline tests specified)
**Regression risk:** {low|medium|high}

### Tests That Would Break
| Test File | What it tests | Why it breaks | Proposed action |
|-----------|--------------|---------------|-----------------|
{rows from directly_impacted where would_break=true}

### Coverage Gaps — Baseline Tests Specified
| Gap Area | File | Baseline Test | Priority |
|----------|------|--------------|----------|
{rows from coverage_gaps with baseline test IDs}

### Regression Safety Net
| Test File | Risk | Reason |
|-----------|------|--------|
{rows from regression_surface}

---
Type **Tether** to approve blast radius and proceed to LLD design.
Type **Vanish** to halt.
Type **Orbit** with feedback to revise the change surface and recompute.
```

**Orbit handling:** If Orbit: address the feedback by updating change-surface.yaml (play inline or re-dispatch tech-architect Step 8 with feedback as additional input). Then re-run Step 9 (blast radius) and Step 10 (baseline tests) with the updated change surface. Re-present Checkpoint 1 with revised results.

**SE-21 (C10, C16, C20, C21, F10, F15, F16, F19):** User must type "Tether" before Step 11 executes. Blast radius fully characterized. Every coverage gap has baseline test. FAIL if Step 11 began without approval at this checkpoint.

---

### Phase 3: DRAFT — LLD (the CENTERPIECE)

---

**Step 11 — Draft LLD (tech.yaml + tech.md)**
Owner: `tech-architect`
Depends on: Checkpoint 1
This is the **centerpiece** of prepare-epic — an engineering-manager-level Low-Level Design.

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml",
      "change_surface_path": "{blast_radius}/change-surface.yaml",
      "architecture_inference_yaml": "{understanding}/architecture-inference.yaml",
      "dependency_graph_yaml": "{understanding}/dependency-graph.yaml",
      "context_assembly_path": "{understanding}/context-assembly.yaml",
      "ltm_findings_path": "{understanding}/ltm-findings.yaml",
      "blast_radius_path": "{blast_radius}/blast-radius.yaml",
      "logical_architecture_path": "{product_base}/architecture/logical-architecture.yaml",
      "physical_architecture_path": "{product_base}/architecture/physical-architecture.yaml",
      "design_patterns_path": "{product_base}/architecture/design-patterns.yaml",
      "nfr_spec_path": "{product_base}/architecture/nfr-spec.yaml",
      "design_spec_path": "{product_base}/experience/design-spec.md"
    },
    "output": {
      "tech_yaml_path": "{design}/tech.yaml",
      "tech_md_path": "{design}/tech.md"
    }
  },
  "task_id": "3-tech-lld"
}
```

Agent produces tech.yaml (interface-level LLD) and tech.md (rich narrative with diagrams).

**tech.yaml structure (C23, C33, C35):**

```yaml
status: DRAFT

interfaces:
  api_contracts:
    - id: API-001
      method: POST
      path: /api/v1/resource
      request_schema: { ... }
      response_schema: { ... }
      error_codes: [400, 401, 403, 500]
      auth: required
      rate_limit: "100/min"

  internal_interfaces:
    - id: INT-001
      module: service-name
      function: functionName
      signature: "(params: Type) => Promise<Result>"
      consumers: [consumer-a, consumer-b]

  service_contracts:
    - id: SVC-001
      from: service-a
      to: service-b
      protocol: event
      event_name: domain.event
      payload_schema: { ... }

frontend_spec:
  components:
    - id: FE-001
      name: ComponentName
      screen_ref: "SCR-001"    # from design-exp
      props: { ... }
      state: { ... }
      data_flow: "description"

  routes:
    - path: /route/path
      component: PageComponent
      auth_required: true

  state_management:
    approach: "description"
    stores:
      - name: storeName
        shape: { ... }

backend_spec:
  services:
    - id: BE-001
      name: ServiceName
      responsibility: "description"
      dependencies: [Dep1, Dep2]
      interfaces_implemented: [INT-001]

  repositories:
    - id: REPO-001
      name: RepositoryName
      entity: EntityName
      operations: [op1, op2]

  middleware:
    - name: middlewareName
      applies_to: [API-001]
      behavior: "description"

data_spec:
  schema_changes:
    - id: SCHEMA-001
      table: table_name
      action: CREATE | MODIFY | DELETE
      columns: { ... }
      indexes: [...]
      migration_order: 1

  data_flow:
    - from: API-001
      through: [Service, Repository]
      to: target
      caching: "strategy"

communication_channels:
  events:
    - name: event.name
      producer: ServiceName
      consumers: [Consumer1, Consumer2]
      payload_schema: { ... }

  webhooks: []

  realtime:
    - protocol: SSE | WebSocket
      endpoint: /path
      data_shape: { ... }

mock_strategy:
  external_services:
    - service: service-name
      mock_approach: "description"
      mock_shape: { ... }

  test_doubles:
    - interface: InterfaceName
      mock_type: "mock framework approach"
      when: "usage context"

file_changes:
  - path: src/path/to/file.ext
    action: CREATE | MODIFY | DELETE
    implements: [BE-001]     # for CREATE
    interfaces: [INT-001]    # for CREATE
    changes: [...]           # for MODIFY: contract-level interface changes
    reason: "..."            # for DELETE: justification

project_structure:
  conventions: { ... }
  naming_patterns: { ... }
```

**tech.md contains (C27):**
- Architecture diagrams (mermaid) showing component interactions
- Data flow diagrams
- Interface diff visualization (for MODIFY changes)
- Design decision rationale
- Narrative that adds value beyond what YAML fields can express

**Alignment requirements:**
- tech.yaml must align with the established architecture from build-arch output — logical-architecture.yaml, physical-architecture.yaml, design-patterns.yaml (C4, F2)
- tech.yaml must NOT introduce technologies, frameworks, or patterns that contradict the locked architecture
- tech.yaml interface definitions must cover every API endpoint and module interface that appears in the change surface (C35, F25)
- mock_strategy must be non-empty when external dependencies exist in the change surface (C33, F26)

**Step 11 Evals (SE-2, SE-3, SE-4, SE-24):**
- SE-2: tech.yaml contains all required interface-level sections: api_contracts, internal_interfaces, service_contracts, frontend_spec, backend_spec, data_spec, communication_channels, mock_strategy. For MODIFY files: contract-level design. For CREATE files: interface specifications. For DELETE files: justification. (C23, F18)
- SE-3: mock_strategy non-empty when external dependencies in change surface. (C33, F26)
- SE-4: Every API endpoint and module interface in change surface has corresponding tech.yaml definition. Zero gaps. (C35, F25)
- SE-24 (C40, F29): tech.yaml api_contracts, internal_interfaces, and service_contracts are thorough enough for independent authorship: every contract entry specifies request/response field shapes (field names, types, required vs optional), error conditions with corresponding error response shapes, behavior contracts (preconditions, postconditions, invariants that hold across calls), and edge cases with observable behavioral differences. A test-writer can write comprehensive behavioral tests from contracts alone. A code-builder can implement fully from contracts alone. Neither party needs to inspect the other's output or read the codebase to fill in missing contract detail.
- tech.yaml aligns with build-arch output — no contradictions (C4, F2)
- tech.md contains diagrams and narrative (C27)
- No "see codebase", "follow existing patterns", or "use available port" in any field (F12)

---

**Step 12 — Generate LLD Brief**
Owner: `doc-builder`
Depends on: Step 11

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "artifact_base": "{stm_base}/{issue}/",
  "slug": "{issue}",
  "briefs_requested": ["tech"],
  "stm": {
    "input": {
      "tech_path": "{design}/tech.yaml",
      "architecture_inference_path": "{understanding}/architecture-inference.yaml"
    }
  },
  "task_id": "brief-lld",
  "config": {
    "product_slug": "{issue}",
    "phase": "DRAFT"
  }
}
```

Agent generates tech-brief.html and regenerates hub.html.

---

### Checkpoint 2: LLD Review

Owner: play
Depends on: Step 12

```markdown
## LLD Review — #{issue}: {title}

Review the LLD brief at: {briefs}/tech-brief.html

**Interfaces defined:**
- API contracts: {count}
- Internal interfaces: {count}
- Service contracts: {count}

**Frontend:** {component_count} components, {route_count} routes
**Backend:** {service_count} services, {repo_count} repositories
**Data:** {schema_change_count} schema changes
**Communication:** {event_count} events, {webhook_count} webhooks, {realtime_count} realtime
**Mock strategy:** {external_service_count} external services, {test_double_count} test doubles
**File changes:** {CREATE_count} create, {MODIFY_count} modify, {DELETE_count} delete

**Architecture alignment:** Aligned with locked {logical_architecture, physical_architecture, design_patterns}

---
Type **Tether** to approve LLD and proceed to scenarios + plan.
Type **Vanish** to halt.
Type **Orbit** with feedback to revise the LLD before proceeding.
```

**Orbit handling:** Re-dispatch tech-architect Step 11 with feedback as additional input, regenerate brief, re-present Checkpoint 2.

**SE-22 (C10, F10):** User must type "Tether" before Steps 13 or 14 execute. LLD content is interface-level design, not code-level diffs. FAIL if scenarios/plan generation began without "Tether" at this checkpoint.

---

### Phase 4: DRAFT — Scenarios + Plan

**Parallel execution:** Steps 13 and 14 are independent after Checkpoint 2. Dispatch both simultaneously.

---

**Step 13 — Draft Scenarios**
Owner: `test-engineer`
Depends on: Checkpoint 2
Runs in parallel with Step 14

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "blast_radius_path": "{blast_radius}/blast-radius.yaml",
      "baseline_tests_path": "{blast_radius}/baseline-tests.yaml",
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml",
      "tech_yaml_path": "{design}/tech.yaml"
    },
    "output": {
      "scenarios_path": "{design}/scenarios.yaml"
    }
  },
  "task_id": "3-scenarios"
}
```

Agent authors three-tier scenarios (C24):

**Tier 1 — baseline_scenarios[]:** One BS-* per BT-* in baseline-tests.yaml. Verifies CURRENT behavior. Must PASS on the unmodified codebase before any changes begin.

**Tier 2 — new_scenarios[]:** One or more NS-* per epic behavior from the upstream locked epic. Verifies the target behavior after the change. Expected to FAIL on the unmodified codebase (they describe new behavior to be created).

**Tier 3 — regression_scenarios[]:** One RS-* per entry in blast-radius.regression_surface. Verifies existing behavior is preserved after the change. Must still PASS after the change.

**feature_gates{}:** Maps each feature/behavior from the locked epic to the baseline[], new[], and regression[] scenario IDs required. Plus a `total` integer count.

Every scenario (all tiers) must contain: id, description, expected_behavior, pass_criteria, automation (automated|hybrid). Hybrid scenarios must include manual_element field documenting what requires human judgment (C7).

**Compartmentalization (C9):** Scenarios go to scenarios.yaml ONLY. The plan.yaml step receives scenario IDs only — never descriptions, expected_behavior, or pass_criteria.

**Greenfield exception:** When test-surface.yaml is empty and all change-surface actions are CREATE: omit Tier 1 and Tier 3. Produce only Tier 2 new scenarios. feature_gates may have empty baseline[] and regression[] arrays.

**Step 13 Evals (SE-5, SE-6, SE-7, SE-8, SE-9):**
- SE-5: Every scenario has description, expected_behavior, pass_criteria, automation (C7, F6)
- SE-6: Hybrid scenarios have manual_element field (C7)
- SE-7: Every product behavior in locked epic covered by at least one scenario (C8, F7)
- SE-8: Scenario audience is validators only — no implementation details (C11)
- SE-9: Three tiers present: baseline, new, regression. Feature gates reference all three tiers. (C24)

---

**Step 14 — Draft Plan**
Owner: `tech-architect`
Depends on: Checkpoint 2
Runs in parallel with Step 13

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "tech_yaml_path": "{design}/tech.yaml",
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml",
      "baseline_tests_path": "{blast_radius}/baseline-tests.yaml",
      "blast_radius_path": "{blast_radius}/blast-radius.yaml",
      "scenarios_path": "{design}/scenarios.yaml"
    },
    "output": {
      "plan_yaml_path": "{design}/plan.yaml",
      "plan_md_path": "{design}/plan.md"
    }
  },
  "task_id": "3-plan"
}
```

**Compartmentalization notice (C9, F8):** The tech-architect reads scenarios.yaml for scenario IDs and counts ONLY. The agent must NOT read or use scenario descriptions, expected_behavior fields, or pass_criteria fields from scenarios.yaml. Plan.yaml references scenario IDs and integer counts — never scenario content.

Agent produces plan.yaml + plan.md.

**plan.yaml must contain (C5, C6, C12, C13, C22, C37, C38, C39):**

```yaml
status: DRAFT

task_dag:
  - id: T-000
    name: "Phase 0: Prerequisites and Baseline Tests"
    tasks:
      - id: T-000-1
        name: "Implement baseline test BT-001"
        file_changes:
          - path: "path/to/baseline-test.ext"
            action: CREATE
        exit_gate: "Test file exists and passes on unmodified codebase"
        depends_on: []
      # ... one task per BT-* ...
      - id: T-000-GATE
        name: "Baseline tests gate"
        file_changes: []
        exit_gate: "All BT-* tests green on unmodified codebase — blast radius characterized"
        depends_on: [T-000-1, ...]
        scenario_gate:
          ids: [BS-001, ...]    # IDs only, never descriptions
          count: N

  - id: T-001
    milestone_id: T-001
    name: "Feature 1: {Feature Name}"
    depends_on: []    # first feature milestone — acceptance-based; no prior milestone to be ACCEPTED
    delivers: "User can {one-sentence testable end-to-end capability for this milestone}"
    scenario_gate:
      ids: [NS-001, NS-002, RS-001]    # actual IDs — validate-epic uses these to select scenarios
      count: 3
    tasks:
      - id: T-001-1
        name: "{Task name}"
        file_changes:
          - path: "src/path/to/file.ext"
            action: MODIFY
        exit_gate: "Observable outcome — e.g., endpoint returns 200 with correct payload"
        depends_on: [T-000-GATE]    # task-level dep on prerequisites gate
        spec_ref: [API-001, INT-001]    # references to tech.yaml interface IDs
      # ...
      - id: T-001-GATE
        name: "Feature 1 complete"
        file_changes: []
        exit_gate: "All feature 1 scenarios pass and all regression tests still pass"
        depends_on: [T-001-last-task]
        scenario_gate:
          ids: [NS-001, NS-002, RS-001]    # IDs only
          count: 3

  - id: T-002
    milestone_id: T-002
    name: "Feature 2: {Feature Name}"
    depends_on: [T-001]    # T-001 must be ACCEPTED by validate-epic — acceptance-based, not task-completion
    delivers: "User can {one-sentence testable end-to-end capability for this milestone}"
    scenario_gate:
      ids: [NS-003, NS-004, RS-002]    # actual IDs — validate-epic uses these to select scenarios
      count: 3
    tasks: [...]

summary:
  - milestone_id: T-001
    feature_ref: "T-001"
    delivers: "What this feature slice delivers as a working capability"
    scope_items: 3
    scenario_count: 3        # integer — per-feature count, compartmentalization preserved (C12)
    cumulative_scenarios: [BS-001, NS-001, NS-002, RS-001]    # running union ID list (C38) — validate-epic regression suite
  - milestone_id: T-002
    feature_ref: "T-002"
    delivers: "..."
    scope_items: 2
    scenario_count: 4
    cumulative_scenarios: [BS-001, NS-001, NS-002, RS-001, NS-003, NS-004, RS-002, RS-003]    # running union through T-002
```

**plan.md contains (C27):** mermaid DAG diagram of task dependencies, phase flow description, dependency chain narrative.

**Plan structure requirements:**
- Phase T-000: Prerequisites — one task per BT-* in baseline-tests.yaml
- At least 2 feature phases beyond Phase 0 (T-001, T-002, ...) (F9)
- Each feature phase delivers end-to-end working capability — NOT layer-only work (C6, F3)
  - Valid: "User can submit form and see confirmation" (frontend + backend + storage)
  - INVALID: "Database schema migrated" (only data layer, no user-facing path)
  - Exception: Phase 0 prerequisites cover scaffolding and baseline tests
- Each feature phase (T-001, T-002, ...) is a milestone — must include milestone_id, scenario_gate.ids[] (actual IDs), depends_on (empty for first, acceptance-based for subsequent), and delivers (C37, F27)
- Feature phase depends_on enforces acceptance-based sequencing: T-002 depends on T-001 being ACCEPTED by validate-epic, not just T-001 tasks completed (C39)
- plan.yaml summary cumulative_scenarios must be an explicit ID list (not integer) per milestone row — the running union of all scenario IDs from this and all prior milestones (C38, F28)
- References upstream locked epic for feature scope (C3) — NOT self-generated features.yaml

**Step 14 Evals (SE-10, SE-11, SE-12, SE-13, SE-14, SE-15, SE-16, SE-25, SE-26, SE-27):**
- SE-10: Every task has file_changes[] with at least one entry — exception: GATE tasks (C5, F4)
- SE-11: Every feature slice delivers end-to-end capability — exception: Phase 0 (C6, F3)
- SE-12: plan.yaml contains zero scenario descriptions/content — only IDs in scenario_gate (C9, F8)
- SE-13: summary contains per-milestone rows with milestone_id, feature_ref, delivers, scope_items; scenario_count as integer (per-feature, compartmentalization preserved); cumulative_scenarios as explicit ID list — the running union of all scenario IDs from this and all prior milestones (not an integer count). (C12, C38, F28)
- SE-14: Every task has non-null exit_gate with observable outcomes, not process steps (C13, F5)
- SE-15: At least 2 feature slices beyond Phase 0 (F9)
- SE-16: Task DAG has explicit depends_on chains, valid DAG (no cycles), tasks reference tech.yaml spec IDs (C22, F17)
- SE-25 (C37, F27): Every feature phase (T-001, T-002, ...) in task_dag includes all four milestone fields: milestone_id (matching the phase ID), scenario_gate.ids[] as actual scenario IDs (not count only), depends_on (empty list or absent for first milestone; references prior milestone ID for subsequent milestones), and delivers (one-sentence testable end-to-end capability description). A feature phase missing any of these four fields is structurally incomplete.
- SE-26 (C38, F28): plan.yaml summary cumulative_scenarios is an explicit ID list (not an integer) per milestone row — the running union of all scenario IDs from this and all prior milestones. The final milestone's cumulative_scenarios list contains ALL scenario IDs (all baseline + all new + all regression) from scenarios.yaml with no omissions.
- SE-27 (C39): Feature phase depends_on (for T-002 and beyond) references the prior milestone ID (e.g., [T-001]), representing acceptance-based sequencing — T-001 must be ACCEPTED by validate-epic, not merely have its tasks marked done. First feature milestone (T-001) has empty or absent depends_on at the phase level. The dependency chain is readable from plan-milestone-summary.yaml without parsing the full task DAG.

---

**Step 15 — Generate Scenarios + Plan Briefs**
Owner: `doc-builder`
Depends on: Steps 13 and 14 (both must complete before dispatching)

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "artifact_base": "{stm_base}/{issue}/",
  "slug": "{issue}",
  "briefs_requested": ["scenarios", "plan"],
  "stm": {
    "input": {
      "scenarios_path": "{design}/scenarios.yaml",
      "plan_path": "{design}/plan.yaml"
    }
  },
  "task_id": "brief-scenarios-plan",
  "config": {
    "product_slug": "{issue}",
    "phase": "DRAFT"
  }
}
```

Agent generates scenarios-brief.html, plan-brief.html, and regenerates hub.html.

---

### Checkpoint 3: Scenarios + Plan Review

Owner: play
Depends on: Step 15

```markdown
## Scenarios + Plan Review — #{issue}: {title}

Review the scenarios brief at: {briefs}/scenarios-brief.html
Review the plan brief at: {briefs}/plan-brief.html

**Scenarios:** {baseline_scenarios count} baseline + {new_scenarios count} new + {regression_scenarios count} regression = {total} total
**Plan:** Phase 0 (prerequisites) + {feature_phase_count} feature phases = {total_task_count} tasks

### Implementation Sequence
| Feature | Delivers | Scope Items | Scenarios | Cumulative |
|---------|---------|-------------|-----------|------------|
{plan.yaml/summary rows}

---
Type **Tether** to approve scenarios + plan and proceed to validation.
Type **Vanish** to halt.
Type **Orbit** with feedback to revise scenarios, plan, or both.
```

**Orbit handling (targeted):**
- If feedback targets scenarios: re-dispatch test-engineer Step 13 with feedback, regenerate scenarios brief
- If feedback targets plan: re-dispatch tech-architect Step 14 with feedback, regenerate plan brief
- If feedback targets both: re-dispatch both agents, then re-dispatch doc-builder Step 15
- Re-present Checkpoint 3 with revised content

**SE-23 (C10, F10):** User must type "Tether" before Step 16 executes. FAIL if validation began without approval at this checkpoint.

---

### Phase 5: VALIDATE

---

**Step 16 — Cross-Validate**
Owner: `feature-steward`
Depends on: Checkpoint 3
Skill: `validate-implementation-design`

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "epic_path": "{product_base}/scope/epics/{epic_id}.yaml",
      "tech_yaml_path": "{design}/tech.yaml",
      "scenarios_path": "{design}/scenarios.yaml",
      "plan_yaml_path": "{design}/plan.yaml",
      "context_assembly_path": "{understanding}/context-assembly.yaml",
      "logical_architecture_path": "{product_base}/architecture/logical-architecture.yaml",
      "physical_architecture_path": "{product_base}/architecture/physical-architecture.yaml",
      "design_patterns_path": "{product_base}/architecture/design-patterns.yaml"
    },
    "output": {
      "validation_path": "{stm_base}/{issue}/evidence/prepare-epic/validation-report.yaml"
    }
  },
  "task_id": "validate"
}
```

Agent invokes `validate-implementation-design` skill. Cross-validates across all three design artifacts and upstream locked epic:
- Epic behavior-to-scenario traceability (every behavior in locked epic covered by at least one NS-*)
- Audience separation (locked epic clean of tech, plan.yaml clean of scenario content)
- Architecture alignment (tech.yaml aligns with build-arch 5-artifact output)
- Compartmentalization integrity (plan.yaml scenario_gate contains IDs only)
- Coverage completeness (all coverage gaps addressed by baseline tests)

**SE-17:** All validation checks executed with PASS/FAIL status. ready_for_lock is false if any check fails. Issues array contains one entry per violation. Checks cover: locked epic behaviors ↔ scenario coverage, tech.yaml ↔ build-arch alignment, plan.yaml tasks ↔ tech.yaml specs, audience separation, compartmentalization.

---

**Step 17 — Pre-Lock Resolution Gate (play inline)**
Owner: play
Depends on: Step 16

Scan all three design artifacts for unresolved items (C18):

```bash
# Scan for open questions in each artifact:
# tech.yaml: open_questions[], risks[] entries with severity="high"
# scenarios.yaml: open_questions[]
# plan.yaml: open_questions[]
```

If any `open_questions[]` or high-severity `risks[]` entries found, present structured resolution interview:

```markdown
## Pre-Lock Resolution Required — #{issue}

The following items must be resolved before artifacts can be locked.
Provide your resolution for each item. Mark resolved items with **RESOLVED:** prefix.

### Open Questions

{For each open_questions[] entry across all three artifacts:}
**[{artifact_name}] {question_id}:** {question text}
*Source artifact:* {path}

### High-Severity Risks

{For each risk with severity="high" in tech.yaml:}
**[tech.yaml] {risk_id}:** {risk description}
*Current mitigation:* {mitigation if any}

---
After resolving all items above, type **Tether** to proceed to lock.
```

After user resolves all items, write `{stm_base}/{issue}/evidence/prepare-epic/pre-lock-resolutions.yaml`:

```yaml
pre_lock_resolutions:
  resolved_at: "{ISO-8601 timestamp}"
  items:
    - item_id: "{question or risk id}"
      source_artifact: "{artifact path}"
      original_text: "{original question or risk text}"
      resolution: "{user-provided resolution}"
      resolved_by: "user"
      status: RESOLVED
  summary:
    total_items: N
    all_resolved: true
```

If no items found:
```yaml
pre_lock_resolutions:
  resolved_at: "{timestamp}"
  items: []
  summary:
    total_items: 0
    all_resolved: true
    note: "No open questions or high-severity risks found in design artifacts."
```

**SE-18 (C18, F14):** pre-lock-resolutions.yaml must exist before Step 18. All items must have status RESOLVED. FAIL if auto-lock executed while any item remained unresolved.

---

**Step 17b — Scenario Validation (13 SCEs)**
Owner: play
Depends on: Step 17

Run all 13 scenario evals from evals.yaml. Any failure halts here — present failure to user, identify the source artifact, route back to appropriate draft step for correction.

- **SCE-1 (S1):** Every behavior in locked epic has at least one NS-* scenario with complete pass criteria and feature_ref back-link.
- **SCE-2 (S2):** tech.yaml changes are consistent with established architecture from build-arch. No contradiction of documented patterns.
- **SCE-3 (S3):** Any single plan.yaml task is self-contained — assignable to an implementer using only plan.yaml + tech.yaml without codebase access.
- **SCE-4 (S4):** Quality Lead can construct a feature validation plan using only feature_gates IDs — no exposure to scenario descriptions.
- **SCE-5 (S5):** plan.yaml/summary cumulative_scenarios at the final milestone is an explicit ID list containing ALL scenario IDs from scenarios.yaml (all baseline + all new + all regression), with no omissions. plan-milestone-summary.yaml cumulative_scenarios for each milestone is the running union of all scenario IDs from that milestone and all prior milestones in execution order.
- **SCE-6 (S6):** context-assembly.yaml documents all upstream artifact sources. blast-radius.yaml coverage fully characterized: every coverage gap has a baseline test.
- **SCE-7 (S7):** Implementation agent can answer "what file, what interface, what exit gate" for any task using only plan.yaml + tech.yaml — no other access required.
- **SCE-8 (S8):** pre-lock-resolutions.yaml has RESOLVED entry for every open question and high-severity risk, OR documents "none found."
- **SCE-9 (S9):** Blast radius fully characterizes impact zone: every impacted test identified, every coverage gap has baseline test, co-change patterns considered.
- **SCE-10 (S10):** Task DAG dependency chains produce valid topological order. Each exit_gate verifiable given only its depends_on chain. Interface specs from tech.yaml provide sufficient detail.
- **SCE-11 (S11):** Three tier types present: baseline (current behavior), new (target behavior from epic), regression (preserved behavior from blast radius). Feature gates reference all three.
- **SCE-12 (S12):** architecture-inference.md and tech.md contain diagrams and narrative that add value beyond YAML fields alone.
- **SCE-13 (S13):** Cold start — play creates issue, branch, STM, and produces fully locked artifacts without requiring prior start-feature.

Any SCE failure: present specific failure to user with the failing artifact path and the SCE that failed. Route back to the responsible draft step for correction, then re-run validation from Step 16.

---

### Phase 6: Auto-Lock

---

**Step 18 — Lock All Artifacts (play inline)**
Owner: play
Depends on: Step 17b (all 13 SCEs pass)

```bash
# Lock all three design artifacts by setting status: LOCKED
# Artifact path: {stm_base}/{issue}/context/design/

for artifact in tech.yaml scenarios.yaml plan.yaml; do
  path="${stm_base}/${issue}/context/design/${artifact}"
  if [ -f "$path" ]; then
    # Read the YAML, update status field to LOCKED, write back
    # Preserve all other content unchanged
    echo "Locked: $path"
  fi
done
```

**3 artifacts locked (C32):**
1. `{stm_base}/{issue}/context/design/tech.yaml` — status: LOCKED
2. `{stm_base}/{issue}/context/design/scenarios.yaml` — status: LOCKED
3. `{stm_base}/{issue}/context/design/plan.yaml` — status: LOCKED

**SE-19 (C2, C32):** Post-lock verification:
- All 3 artifacts contain `status: "LOCKED"`
- Diff between DRAFT and LOCKED versions shows only the status field change — zero content modifications
- Artifacts reside at `{stm_base}/{issue}/context/design/`
- tech.yaml: no contradiction of build-arch architecture (C4, F2)
- scenarios.yaml: no audience-violating content (C11)
- plan.yaml: zero scenario content beyond IDs in scenario_gate (C9, F8)

---

**Step 18b — Produce Self-Contained Context Package (play inline) (C36)**
Owner: play
Depends on: Step 18 (auto-lock)

After locking design artifacts, produce 5 additional context artifacts that make STM self-contained for downstream plays. No downstream play (implement-epic, validate-epic) reads KB or LTM directly — everything they need is in STM.

1. **quality-gates.yaml** at `{context_base}/design/quality-gates.yaml`:
   - Read quality-vision.yaml from `{product_base}architecture/`
   - Translate QP dimension levels to concrete thresholds (QP-1→min_coverage, QP-2→max_cyclomatic, QP-7→security scans)
   - Detect project toolchain and populate gate commands
   - Write enriched gates with both quality gate commands AND qp_certification thresholds

2. **epic-spec.yaml** at `{context_base}/design/epic-spec.yaml`:
   - Copy the locked epic from `{product_base}scope/epics/{epic_id}.yaml`
   - Include: all behaviors, constraints, success_scenarios, failure_conditions
   - This is the complete product specification for this epic — downstream plays never read the original

3. **design-context.yaml** at `{context_base}/design/design-context.yaml`:
   - Extract screen references relevant to this epic from `{product_base}experience/screens/*.md`
   - Extract user flows relevant to this epic from `{product_base}experience/flows/*.md` (if exist)
   - Extract relevant portions of design-spec.md
   - Scoped to this epic only — not the full product design

4. **architecture-context.yaml** at `{context_base}/design/architecture-context.yaml`:
   - Extract architecture decisions relevant to this epic from build-arch 5 artifacts
   - Include: relevant bounded contexts, components, technology stack, design patterns, NFR thresholds
   - Scoped to this epic's change surface — not the full product architecture

5. **plan-milestone-summary.yaml** at `{context_base}/design/plan-milestone-summary.yaml`:
   - Extract the milestone summary from plan.yaml into a standalone artifact
   - Per-milestone rows containing: milestone_id, delivers, scenario_gate.ids[] (actual IDs), cumulative_scenarios (running union of all scenario IDs from this milestone and all prior milestones in execution order), depends_on references (acceptance-based — empty for first milestone, prior milestone ID for subsequent)
   - This is the contract implement-epic uses to determine which milestone it is working on, which scenarios validate-epic must run for that milestone, and which scenarios constitute the full regression suite up to that point — without reading scenarios.yaml or parsing the full task DAG

**Step 18b Eval:** All 5 artifacts exist at `{context_base}/design/`. quality-gates.yaml has concrete thresholds (not just dimension names). epic-spec.yaml contains all behaviors from the locked epic. design-context.yaml references screens relevant to this epic. architecture-context.yaml includes technology stack and pattern decisions. plan-milestone-summary.yaml contains all milestones with explicit cumulative_scenarios ID lists and acceptance-based depends_on references. FAIL: any downstream play (implement-epic, validate-epic) reads KB or LTM directly.

---

### Phase 7: Evidence & Close

---

**Step 19 — Write Evidence (play inline)**
Owner: play
Depends on: Step 18

Verify all STM artifacts exist on disk (C26):

```bash
evidence_base="${stm_base}/${issue}/evidence/prepare-epic"
required=(
  "${understanding}/architecture-inference.yaml"
  "${understanding}/architecture-inference.md"
  "${understanding}/test-surface.yaml"
  "${understanding}/dependency-graph.yaml"
  "${understanding}/dependency-graph.md"
  "${understanding}/commit-history-analysis.yaml"
  "${understanding}/commit-history-analysis.md"
  "${understanding}/ltm-findings.yaml"
  "${understanding}/context-assembly.yaml"
  "${blast_radius}/change-surface.yaml"
  "${blast_radius}/blast-radius.yaml"
  "${blast_radius}/baseline-tests.yaml"
  "${evidence_base}/validation-report.yaml"
  "${evidence_base}/pre-lock-resolutions.yaml"
)
for path in "${required[@]}"; do
  [ -f "$path" ] || echo "WARNING: Missing expected artifact: $path"
done
```

Write evidence to `{evidence_base}/{YYYYMMDD-HHMMSS}.md`:

```markdown
# prepare-epic Evidence — Issue #{issue}

**Timestamp:** {YYYYMMDD-HHMMSS}
**Issue:** #{issue}
**Branch:** {branch_name}
**Epic:** {epic_id}

## Upstream Artifacts (ALL locked)

| Source | Artifact | Path | Status |
|--------|----------|------|--------|
| specify-product | Epic | {path} | locked |
| specify-product | Scope | {path} | locked |
| specify-product | Quality Profile | {path} | locked |
| design-exp | Design Spec | {path} | locked |
| build-arch | Logical Architecture | {path} | locked |
| build-arch | Physical Architecture | {path} | locked |
| build-arch | NFR Spec | {path} | locked |
| build-arch | Quality Vision | {path} | locked |
| build-arch | Design Patterns | {path} | locked |

## Context Resolution Summary

**Context sources:** locked (upstream) + issue-scoped (codebase analysis)
**LTM findings:** {count} | **LTM gaps:** {count}

## Blast Radius Summary

**Change surface:** {N} files — {CREATE} create, {MODIFY} modify, {DELETE} delete
**Tests directly impacted:** {N}
**Tests that would break:** {N}
**Coverage gaps:** {N} — {N} baseline tests specified
**Regression risk:** {low|medium|high}

## Artifacts Locked

| Artifact | Path | Status |
|----------|------|--------|
| tech.yaml | {stm_base}/{issue}/context/design/tech.yaml | LOCKED |
| scenarios.yaml | {stm_base}/{issue}/context/design/scenarios.yaml | LOCKED |
| plan.yaml | {stm_base}/{issue}/context/design/plan.yaml | LOCKED |

**Total scenarios:** {baseline} baseline + {new} new + {regression} regression = {total}
**Plan:** Phase 0 prerequisites + {N} feature phases = {task_count} tasks

## Pre-Lock Resolutions

{List each resolved item, or "No open questions or high-severity risks found."}

## Step Eval Results

{SE-1 through SE-27: PASS|FAIL per eval, with note on any FAIL}

## Scenario Eval Results

{SCE-1 through SCE-13: PASS|FAIL per eval, with note on any FAIL}
```

Present summary to user.

---

**Step 20 — Self-Commit Evidence (non-blocking)**
Owner: `repo-orchestrator`
Depends on: Step 19

```json
{
  "intent_path": "core/components/plays/prepare-epic/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "commit_record": "{stm_base}/{issue}/evidence/prepare-epic/commit.yaml"
    }
  },
  "task_id": "self-commit-evidence"
}
```

Agent invokes `create-commit` to stage and commit:
- Evidence file: `{evidence_base}/{YYYYMMDD-HHMMSS}.md`
- Pre-lock resolutions: `{evidence_base}/pre-lock-resolutions.yaml`
- Locked artifacts: `{design}/tech.yaml`, `{design}/scenarios.yaml`, `{design}/plan.yaml`

Commit message: `chore(stm): lock prepare-epic artifacts for #{issue}`

**Non-blocking:** If `repo-orchestrator` returns failure or `committed: false`, log as warning — do NOT halt. All artifacts are already locked and written to disk. A missing evidence commit is not fatal.

---

## Pause and Resume

Steps are in execution order — run top to bottom.

**Status file:** `{stm_base}/{issue}/status/prepare-epic.json`

```json
{
  "play": "prepare-epic",
  "issue": "{issue_number}",
  "started_at": "{ISO-8601 timestamp}",
  "tasks": {
    "pre-flight": { "status": "completed", "completed_at": "..." },
    "resolve-issue": { "status": "completed", "completed_at": "..." },
    "create-branch": { "status": "completed", "completed_at": "..." },
    "stm-init": { "status": "completed", "completed_at": "..." },
    "read-upstream": { "status": "completed", "completed_at": "..." },
    "1A-architecture-inference": { "status": "completed", "completed_at": "..." },
    "1B-test-surface-mapping": { "status": "completed", "completed_at": "..." },
    "1C-dependency-graph": { "status": "completed", "completed_at": "..." },
    "1D-git-history": { "status": "completed", "completed_at": "..." },
    "1E-ltm-consultation": { "status": "in_progress", "started_at": "..." },
    "context-assembly": { "status": "pending" },
    "checkpoint-0": { "status": "pending" },
    "2A-change-surface": { "status": "pending" },
    "2B-blast-radius": { "status": "pending" },
    "2C-baseline-tests": { "status": "pending" },
    "checkpoint-1": { "status": "pending" },
    "3-tech-lld": { "status": "pending" },
    "brief-lld": { "status": "pending" },
    "checkpoint-2": { "status": "pending" },
    "3-scenarios": { "status": "pending" },
    "3-plan": { "status": "pending" },
    "brief-scenarios-plan": { "status": "pending" },
    "checkpoint-3": { "status": "pending" },
    "validate": { "status": "pending" },
    "pre-lock-resolution": { "status": "pending" },
    "scenario-validation": { "status": "pending" },
    "auto-lock": { "status": "pending" },
    "write-evidence": { "status": "pending" },
    "self-commit-evidence": { "status": "pending" }
  }
}
```

**Executor loop:**
```
parse issue number from branch or input
check for status file at {stm_base}/{issue}/status/prepare-epic.json

for each step in compiled order:
  if status shows "completed" or "skipped" → skip
  if status shows "in_progress" → reset to pending (may not have finished cleanly)
  mark "in_progress" in status file
  execute step
  mark "completed" (or "skipped" for conditional steps) in status file
```

**Parallel step handling:**
- Steps 2-6: Mark all five "in_progress" simultaneously. Mark each "completed" as it finishes. Step 7 begins when all five show "completed".
- Steps 13-14: Mark both "in_progress" simultaneously. Step 15 begins when both show "completed".

**Fresh start:** No status file → execute all steps, create status file on first step.
**Resume:** Status file exists → skip completed, continue from first incomplete.

---

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:79c9cc4f01eac9d8c7c17d1e4921a2eadb2f1d049235610ac17a3192dff86e9e |
| compiled_by | create-play |
| compiled_at | 2026-04-15 |
| workflow_structure | A — Full checkpoint flow (4 checkpoints) |
| domain_agents | 3 (tech-architect, test-engineer, feature-steward) |
| utility_agents | 2 (doc-builder, repo-orchestrator) |
| agent_budget | exempt — no budget limit. Rationale: prepare-epic is the make-or-break context pass for all downstream implementation work. If context is wrong, implement-epic executes the wrong plan. Thoroughness is the primary value of this play. Every agent dispatch contributes directly to artifact quality. Imposing a budget would degrade completeness, which defeats the play's core purpose. |
| parallel_phases | Steps 2-6 (Phase 1 parallel context collection), Steps 13-14 (Phase 4 parallel scenarios + plan) |
| checkpoints | 4 — (0) context assembly, (1) blast radius, (2) LLD review, (3) scenarios + plan |
| step_evals | 27 (SE-1 through SE-27) |
| scenario_evals | 13 (SCE-1 through SCE-13) |
| design_artifacts_locked | tech.yaml, scenarios.yaml, plan.yaml |
| stm_context_artifacts | quality-gates.yaml, epic-spec.yaml, design-context.yaml, architecture-context.yaml, plan-milestone-summary.yaml |
| dual_format_artifacts | architecture-inference, dependency-graph, commit-history-analysis, tech, plan |
| stm_evidence_artifacts | test-surface, context-assembly, change-surface, blast-radius, baseline-tests, validation-report, pre-lock-resolutions |
