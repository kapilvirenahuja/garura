---
name: capture-learning
description: "Archive completed issue STM, then extract and synthesize reusable patterns from resolution traces and evidence. Presents knowledge candidates for human approval before promoting to LTM. Triggers: 'capture learning', 'archive and learn', 'extract patterns'."
user-invocable: true
model: opus
---

# capture-learning

Given a completed issue number, archive the issue's STM directory to `_archive/{YYYY-MM}/{issue}/` to keep the active workspace clean, then extract and synthesize reusable patterns from resolution traces, present knowledge candidates for human approval, and promote approved candidates to the appropriate LTM layer. The issue must be in a closed state before archival proceeds.

## Compiled From

This play was compiled from `reference/intent.yaml` by `/create-play`.
Intent defines constraints (C1-C4 archival, C5-C7/C9-C15 extraction), failure conditions (F1-F5 archival, F6-F11 extraction), and scenarios (S1-S2 archival, S3-S6 extraction).
To modify this play, update intent.yaml and re-run `/create-play --rebake capture-learning`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the workflow. You delegate domain tasks to agents via JSON contracts — never execute domain work directly.

**Forbidden:** Direct `git` commands for repository operations. Direct `gh` commands for issue operations. Direct file reads of resolution traces for extraction. All domain work goes through agents.

**Agent boundaries:**

| Agent | Type | Domain | Phases |
|-------|------|--------|--------|
| `project-orchestrator` | domain | Issues: verify issue is closed, fetch close date | Execution (archival) |
| `knowledge-extractor` | domain | Knowledge: extract patterns from traces, write approved to LTM | Execution (extraction), Execution (write) |
| `repo-orchestrator` | utility | Git: archive STM directory, commit evidence | Execution (archival), Evidence |

## Pre-flight

Execute these checks before any domain work:

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| Resolve `stm_base` from `core/config.yaml` | — | Hard halt — config is required |
| Resolve `stm_archive` from `core/config.yaml` | — | Hard halt — config is required |
| Issue number provided as input | — | Hard halt — play requires an issue number |
| STM directory exists at `{stm_base}/{issue}/` | — | Hard halt — nothing to archive |

```bash
# Resolve paths from config
stm_base=$(grep '^\s*base-path:' core/config.yaml | head -1 | awk '{print $2}')
stm_archive=$(grep '^\s*archive-path:' core/config.yaml | head -1 | awk '{print $2}')

# Parse issue number from input (supports "42" or "#42")
issue=$(echo "{input}" | grep -oE '[0-9]+' | head -1)
# Halt if empty

# Verify STM directory exists
test -d "${stm_base}/${issue}/"
```

**Resume check:** If `{stm_base}/{issue}/status/capture-learning.json` exists, resume — skip completed steps, reset any `in_progress` to pending, continue from first incomplete. If the archival step already completed, check the archive location instead: `{stm_archive}/{YYYY-MM}/{issue}/status/capture-learning.json`.

## Workflow

### Phase: Execution (Archival)

**Step 1 — Verify Issue Closed**
Owner: `project-orchestrator`
Depends on: pre-flight
Skill: `manage-issue` (action: read)

```json
{
  "intent_path": "core/components/plays/capture-learning/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "issue_state": "{stm_base}/{issue}/evidence/capture-learning/issue-state.yaml"
    }
  },
  "task_id": "verify-issue-closed"
}
```

Agent invokes `manage-issue` (action: read) for issue `{issue}` → writes issue state to STM. Play reads the output and extracts `state` and `closedAt`.

**Step 1 Eval (SE-5 / F5):**
- Issue state is `CLOSED` as reported by manage-issue output
- If state is not `CLOSED` → hard halt, do not proceed to archival (C1)

**Step 1 Post-processing (play inline):**
- Read `{stm_base}/{issue}/evidence/capture-learning/issue-state.yaml`
- Extract `closedAt` date → derive `{YYYY-MM}` for archive bucket
- Store `close_date` and `archive_bucket` in play state for Step 2

---

**Step 2 — Archive STM Directory**
Owner: `repo-orchestrator`
Depends on: Step 1
Skill: `archive-issue-stm`

```json
{
  "intent_path": "core/components/plays/capture-learning/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "issue_state": "{stm_base}/{issue}/evidence/capture-learning/issue-state.yaml"
    },
    "output": {
      "archive_result": "{stm_base}/{issue}/evidence/capture-learning/archive-result.yaml"
    }
  },
  "task_id": "archive-stm"
}
```

Agent invokes `archive-issue-stm` skill with `issue_number` and `close_date` extracted from STM input → moves `{stm_base}/{issue}/` to `{stm_archive}/{YYYY-MM}/{issue}/` → writes archive result to STM.

**Step 2 Evals:**

- **SE-1 (F1/C2):** Archive directory exists at `{stm_archive}/{YYYY-MM}/{issue}/` and is non-empty
- **SE-2 (F2/C3):** Original STM directory at `{stm_base}/{issue}/` no longer exists
- **SE-3 (F3/C4):** Archive is complete — same file count and directory structure as original; no files renamed or restructured
- **SE-4 (F4):** `{YYYY-MM}` in archive path matches the issue's closedAt date

**Note:** After Step 2, all STM paths for this issue shift to the archive location: `{stm_archive}/{YYYY-MM}/{issue}/`.

---

### Phase: Execution (Extraction)

**Step 3 — Locate Evidence Artifacts**
Owner: play
Depends on: Step 2

Scan the archived STM directory for all recognized evidence artifact types (C12):

```bash
find {stm_archive}/{YYYY-MM}/{issue}/evidence/ \( \
  -name "resolution-trace.yaml" -o \
  -name "judge-report*.yaml" -o \
  -name "rca.yaml" -o \
  -name "design.yaml" -o \
  -name "remediation-*.md" -o \
  -name "remediation-*.yaml" -o \
  -name "quality-report.yaml" -o \
  -name "drift-report.md" \
\) -type f
```

Collect all artifact paths into a list. If no recognized evidence artifacts exist, set `evidence_paths` to empty list — Step 4 will handle the zero-candidate case (C15).

---

**Step 4 — Extract Knowledge Candidates**
Owner: `knowledge-extractor`
Depends on: Step 3
Mode: `extract`

```json
{
  "intent_path": "core/components/plays/capture-learning/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "task_id": "extract-candidates",
  "mode": "extract",
  "ltm_context": {
    "project_base": "{product_base}",
    "core_base": "~/.meridian/core/memory/"
  },
  "stm": {
    "input": {
      "evidence_paths": ["{list of evidence artifact paths from Step 3}"]
    },
    "output": {
      "candidates_path": "{stm_archive}/{YYYY-MM}/{issue}/evidence/capture-learning/candidates.yaml"
    }
  }
}
```

Resolve `{product_base}` from `core/config.yaml` → `product.base-path`.

Agent scans all recognized evidence artifact types (C12) — resolution traces (primary, `resolved_from: "llm"` entries), judge reports (failure conditions, remediation strategies), RCA artifacts (root cause patterns), design artifacts (chosen strategies, alternatives), remediation records (fix approaches), quality reports (recurring issues), and drift reports (drift patterns). Synthesizes related decisions into patterns (C13), classifies as project/core with reasoning (C10), deduplicates against existing LTM (C6), and writes `candidates.yaml` to STM.

**Zero-candidate case (C15):** If no extractable signals exist across all recognized artifact types, agent writes `candidates.yaml` with empty list and `summary.unique_candidates: 0`. Play skips checkpoint and write phases, proceeds directly to evidence.

**Step 4 Evals:**

- **SE-7 (F7/C6):** Every candidate has `dedup_status` set; duplicates excluded from approval queue
- **SE-10 (F10/C10):** Every candidate has `proposed_scope` AND `proposed_scope_rationale` (non-empty)
- **SE-11 (F11/C13):** Candidates are synthesized patterns with When to Choose, When to Avoid, Key Components, and Tradeoffs; no more than 7 candidates from a single issue
- **SE-12 (C15):** When no extractable signals exist across all recognized artifact types, candidates.yaml contains empty list with `summary.unique_candidates: 0`

---

### Phase: Checkpoint (skip when: zero candidates extracted)

**Step 5 — Human Reviews Candidates**
Owner: play
Depends on: Step 4

Read `candidates.yaml` from STM. Present candidates to user for review (C11 — mandatory, no LTM write without approval):

```markdown
## Knowledge Candidates — Issue #{issue}

**Artifacts scanned:** {summary.artifacts_scanned}
**LLM fallbacks found:** {summary.total_llm_fallbacks}
**Unique candidates:** {summary.unique_candidates}
**Duplicates excluded:** {summary.duplicate_candidates}

{For each candidate with dedup_status != "duplicate":}

### {id}: {title}

**Type:** {type} | **Scope:** {proposed_scope} ({proposed_scope_rationale})
**Synthesized from:** {synthesized_from}
**Dedup status:** {dedup_status} {dedup_conflict_path if near-duplicate}

**Domain question:** {domain_question}
**Answer:** {llm_answer_summary}

---

For each candidate, type:
- **A** (approve) — write to LTM
- **R** (reject) — skip
- **E** (edit scope) — change project↔core classification

When done, type **Tether** to proceed with approved candidates or **Vanish** to cancel all writes.
```

Update `approval_status` in `candidates.yaml` based on user input. Write updated file back to same STM path.

---

### Phase: Execution (Write)

**Step 6 — Write Approved Candidates to LTM**
Owner: `knowledge-extractor`
Depends on: Step 5
Mode: `write`

```json
{
  "intent_path": "core/components/plays/capture-learning/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "task_id": "write-approved-candidates",
  "mode": "write",
  "ltm_context": {
    "project_base": "{product_base}",
    "core_base": "~/.meridian/core/memory/"
  },
  "stm": {
    "input": {
      "candidates_path": "{stm_archive}/{YYYY-MM}/{issue}/evidence/capture-learning/candidates.yaml"
    },
    "output": {
      "write_result": "{stm_archive}/{YYYY-MM}/{issue}/evidence/capture-learning/write-result.yaml"
    }
  }
}
```

Agent reads approved candidates from STM, writes knowledge files to LTM in canonical knowledge-file-template.md format (C7), updates `_index.md` for each file (C14). Only writes candidates with `approval_status: "approved"` (C11).

**Step 6 Evals:**

- **SE-6 (F6):** Every written knowledge file traces to a candidate with `approval_status: "approved"` in candidates.yaml
- **SE-8 (F8/C7):** Each written file passes its type's validation checklist from knowledge-file-template.md
- **SE-9 (F9/C14):** Each written knowledge file has a corresponding entry in the appropriate `_index.md`

---

### Phase: Scenario Validation

**Step 7 — Scenario Evals**
Owner: play
Depends on: Step 6 (or Step 4 if zero candidates)

- **SCE-1 (S1 — Engineering lead):** Archived directory at `{stm_archive}/{YYYY-MM}/{issue}/` is browsable, preserves original structure, contains all files. Nothing lost during archival.
- **SCE-2 (S2 — Developer):** Active STM workspace at `{stm_base}/` does not contain a directory for the archived issue number. Workspace shows only active issues.
- **SCE-3 (S3 — Tech Designer):** Can review a staged list of synthesized knowledge candidates, each classified with scope reasoning. After approving, can find them in LTM via `_index.md` search with correct metadata.
- **SCE-4 (S4 — Developer):** When a candidate matches an existing LTM file by search pattern overlap, it is marked as near-duplicate with the existing file path. On approval, the existing file is updated — no duplicate created.
- **SCE-5 (S5 — Operator):** Archival completes normally. Extraction phase exits cleanly with zero candidates. No files written to LTM. Evidence records the zero-candidate outcome.
- **SCE-6 (S6 — Quality Lead):** Approved core-scoped knowledge file passes full validation checklist from knowledge-file-template.md. `_index.md` in target directory is updated with the new entry.

---

### Phase: Evidence & Close

**Step 8 — Write Evidence**
Owner: play
Depends on: Step 7

Write evidence to the **archive location**: `{stm_archive}/{YYYY-MM}/{issue}/evidence/capture-learning/{YYYYMMDD-HHMMSS}.md`.

Evidence content includes:
- Issue number and close date
- Archive source and destination paths
- Extraction summary (traces scanned, candidates extracted, approved, written)
- Step eval results (SE-1 through SE-12)
- Scenario eval results (SCE-1 through SCE-6)
- List of knowledge files written to LTM (if any)
- Timestamp

Present summary to user.

**Step 9 — Self-Commit Evidence (non-blocking)**
Owner: `repo-orchestrator`
Depends on: Step 8

```json
{
  "intent_path": "core/components/plays/capture-learning/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "commit_record": "{stm_archive}/{YYYY-MM}/{issue}/evidence/capture-learning/commit.yaml"
    }
  },
  "task_id": "self-commit-evidence"
}
```

Agent invokes `create-commit` to stage and commit the evidence file and any new LTM knowledge files with message: `chore(stm): record capture-learning evidence and knowledge artifacts for #{issue}`.

**Non-blocking:** If `repo-orchestrator` returns failure or `committed: false`, log as warning — do NOT halt. Archival and extraction already succeeded; a missing evidence commit is not fatal.

See [ADR 012: Evidence Self-Commit](../../../docs/adr/012-evidence-self-commit.md) for the architectural rationale.

## Pause and Resume

Steps are in execution order — run top to bottom.

**Issue detection:** Parse issue number from user input during pre-flight (e.g., `42` or `#42`).

**Status file:** `{stm_base}/{issue}/status/capture-learning.json`

```json
{
  "play": "capture-learning",
  "issue": 42,
  "started_at": "2026-03-06T10:00:00+0530",
  "tasks": {
    "verify-issue-closed": { "status": "completed", "completed_at": "..." },
    "archive-stm": { "status": "in_progress", "started_at": "..." },
    "locate-evidence": { "status": "pending" },
    "extract-candidates": { "status": "pending" },
    "human-review": { "status": "pending" },
    "write-approved-candidates": { "status": "pending" },
    "scenario-validation": { "status": "pending" },
    "write-evidence": { "status": "pending" },
    "self-commit-evidence": { "status": "pending" }
  }
}
```

**Executor loop:**
```
parse issue number from input
check for status file at {stm_base}/{issue}/status/capture-learning.json

for each step in compiled order:
  if status file shows step "completed" -> skip
  if status file shows step "in_progress" -> reset to pending (may not have finished)
  mark step "in_progress" in status file
  execute step
  mark step "completed" in status file
```

**Fresh start:** No status file exists → execute all steps, create status file on first step.
**Resume:** Status file exists → skip completed, continue from first incomplete.
**Pause:** User terminates session. Status file reflects last completed step. Next invocation resumes.

**Note on archival and status file:** The status file lives at `{stm_base}/{issue}/`. Step 2 moves this directory to archive. If resuming after Step 2 completed, check the archive location for the status file: `{stm_archive}/{YYYY-MM}/{issue}/status/capture-learning.json`.

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:c6bdbdabe3a2e847daeb48e219f25e09010560eced69134ab949ea1a06aa010f |
| compiled_by | create-play |
| compiled_at | 2026-04-12T00:00:00Z |
| maturity | L2 |
| workflow_structure | A |
| domain_agents | 2 (project-orchestrator, knowledge-extractor) |
| utility_agents | 1 (repo-orchestrator) |
| step_evals | 12 (SE-1 through SE-12, after Steps 1, 2, 4, 6) |
| scenario_evals | 6 (SCE-1 through SCE-6) |
