# Compiled Play Example — commit-code

This is the target output format for `/create-play`. It shows what a clean compiled play looks like. The play compiler should produce output that follows this structure.

**Source:** Derived from `commit-code` intent.yaml (C1-C7, F1-F4, S1-S2) using Structure A (full checkpoint flow).

---

```markdown
---
name: commit-code
description: Commit changes grouped by concern with conventional messages and issue references. Use when you have uncommitted changes.
user-invokable: true
---

# commit-code

Commit all changed files grouped by concern, using conventional commit format with issue references, and push to remote. Approval is required only when confidence is low.

## Compiled From

This play was compiled from `reference/intent.yaml` by `/create-play`.
Intent defines constraints (C1-C7), failure conditions (F1-F4), and scenarios (S1-S2).
To modify this play, update intent.yaml and re-run `/create-play`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the workflow. You delegate domain tasks to agents via JSON contracts — never execute domain work directly.

**Forbidden:** Direct `git` commands for analysis or commits. Direct `gh` commands for issue operations. All domain work goes through agents.

**Agent boundaries:**

| Agent | Domain | Phases |
|-------|--------|--------|
| `repo-orchestrator` | Git: analyze changes, create commits, push branch, draft brief | Preparation, Execution |
| `project-orchestrator` | Issues: fetch open issues, resolve issue mappings | Preparation |

## Pre-flight

Execute these checks before any domain work:

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| Resolve `stm_base` from `.garura/core/config.yaml` | — | Hard halt |
| Branch guard | C1 | Hard halt |
| Changes exist | C2 | Graceful exit |
| Sensitive file scan | C4 | Hard block |
| Open issues exist | C3 | Hard halt |

```bash
stm_base=$(grep '^\s*base-path:' .garura/core/config.yaml | awk '{print $2}')
branch=$(git branch --show-current)
default_branch=$(git remote show origin | grep 'HEAD branch' | awk '{print $NF}')
# C1: halt if branch == default_branch
# C2: git status --porcelain — halt if empty
# C4: scan against analyze-changes/reference/risks.md patterns
# C3: verify open issues exist

# Extract issue number from branch name (e.g., feature/95-some-slug → 95)
issue=$(echo "$branch" | grep -oE '/[0-9]+' | tr -d '/')
```

**Resume check:** If `{stm_base}/{issue}/status/commit-code.json` exists, resume — skip completed steps, reset any `in_progress` to pending, continue from first incomplete.

## Workflow

### Phase: Preparation

**Step 1 — Analyze Changes**
Owner: `repo-orchestrator`
Depends on: pre-flight

```json
{
  "intent_path": "reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {},
    "output": {
      "analysis": "{stm_base}/{issue}/evidence/commit-code/analysis.yaml"
    }
  },
  "task_id": "analyze-changes"
}
```

Agent invokes `analyze-changes` skill → writes changeset analysis to STM.

**Step 1 Eval:**
- Groups are non-empty and each file appears in exactly one group (F1)
- No sensitive files staged without approval (C4)

---

**Step 2 — Resolve Issues**
Owner: `project-orchestrator`
Depends on: Step 1

```json
{
  "intent_path": "reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "analysis": "{stm_base}/{issue}/evidence/commit-code/analysis.yaml"
    },
    "output": {
      "issue_mappings": "{stm_base}/{issue}/evidence/commit-code/issue-mappings.yaml"
    }
  },
  "task_id": "resolve-issues"
}
```

Agent invokes `resolve-issues` skill → writes issue mappings to STM.

**Step 2 Eval:**
- Every group maps to an open issue with confidence score (C3, F4)
- All groups mapped to the same branch issue or explicitly justified (F4)

---

### Phase: Checkpoint (skip when: all issue mappings are high confidence AND no sensitive files)

**Step 3 — Draft Brief**
Owner: `repo-orchestrator`
Depends on: Step 1, Step 2

Agent reads STM artifacts (analysis + issue mappings), produces human-readable brief.
Play renders `templates/checkpoint.md` with brief content.

**Step 4 — Human Review**
Owner: play
Depends on: Step 3

Present brief → **Tether** to proceed | **Vanish** to halt.

---

### Phase: Execution

**Step 5 — Create Commits**
Owner: `repo-orchestrator`
Depends on: Step 4 (or Step 2 if checkpoint skipped)

```json
{
  "intent_path": "reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "analysis": "{stm_base}/{issue}/evidence/commit-code/analysis.yaml",
      "issue_mappings": "{stm_base}/{issue}/evidence/commit-code/issue-mappings.yaml"
    },
    "output": {
      "commit_record": "{stm_base}/{issue}/evidence/commit-code/commits.yaml"
    }
  },
  "task_id": "create-commits"
}
```

**Critical: Agent reads STM data from Steps 1-2. NEVER reads the brief from Step 3.**

Agent invokes `create-commit` skill → creates commits → writes commit record to STM.

**Step 5 Eval:**
- Every commit uses conventional format `type(scope): subject (#issue)` (C5, C6, F1, F2, F3)

---

**Step 6 — Push Branch**
Owner: `repo-orchestrator`
Depends on: Step 5

Agent pushes branch to remote.

**Step 6 Eval:**
- Local HEAD matches remote HEAD after push (C7)
- Non-blocking: push failure logs warning, does not halt

---

### Phase: Scenario Validation

**Step 7 — Scenario Evals**
Owner: play
Depends on: Step 6

- **S1 (Code reviewer):** Each commit has a clear scope, traces to an issue, conventional format. A reviewer can assess the change from the commit alone.
- **S2 (Team lead):** All commits use consistent format. Progress is reportable per issue from git log.

---

### Phase: Evidence & Close

**Step 8 — Write Evidence**
Owner: play
Depends on: Step 7

Write evidence to `{stm_base}/{issue}/evidence/commit-code/{YYYYMMDD-HHMMSS}.md`.
Present summary to user.

Invoke `repo-orchestrator` to self-commit evidence files (ADR 012). Non-blocking on failure.

## Pause and Resume

Steps are in execution order — run top to bottom.

**Issue detection:** Extract issue number from branch name during pre-flight (e.g., `feature/95-slug` → `95`). This is needed before the status file can be checked.

**Status file:** `{stm_base}/{issue}/status/commit-code.json`

```json
{
  "play": "commit-code",
  "issue": 95,
  "started_at": "2026-03-06T10:00:00+0530",
  "tasks": {
    "analyze-changes": { "status": "completed", "completed_at": "..." },
    "resolve-issues": { "status": "completed", "completed_at": "..." },
    "create-commits": { "status": "in_progress", "started_at": "..." },
    "push-branch": { "status": "pending" }
  }
}
```

**Executor loop:**
```
resolve issue number from branch name
check for status file at {stm_base}/{issue}/status/commit-code.json

for each step in compiled order:
  if status file shows step "completed" → skip
  if status file shows step "in_progress" → reset to pending (may not have finished)
  mark step "in_progress" in status file
  execute step
  mark step "completed" in status file
```

**Fresh start:** No status file exists → execute all steps, create status file on first step.
**Resume:** Status file exists → skip completed, continue from first incomplete.
**Pause:** User terminates session. Status file reflects last completed step. Next invocation resumes.

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:a1b2c3d4... |
| compiled_by | create-play |
| compiled_at | 2026-03-06T12:00:00+0530 |
| workflow_structure | A |
| agents | 2 (repo-orchestrator, project-orchestrator) |
| step_evals | 4 (after Steps 1, 2, 5, 6) |
| scenario_evals | 2 (S1, S2) |
```
