---
name: "meridian:report-issue"
description: Report a defect against Meridian OS — files a GitHub Issue on kapilvirenahuja/meridian-os, or produces a portable local fallback file if GitHub is unreachable.
user-invokable: true
---

# meridian:report-issue

Accept a structured defect report from any caller (human or agent), validate required fields and severity, file the defect as a labeled GitHub Issue on the Meridian repository, and confirm the issue URL. If GitHub is unreachable or unauthorized, produce a portable local markdown defect file with sharing instructions.

## Compiled From

This play was compiled from `reference/intent.yaml` by `/create-play`.
Intent defines constraints (C1-C9), failure conditions (F1-F5), and scenarios (S1-S5).
To modify this play, update intent.yaml and re-run `/create-play`.
Do NOT edit this file manually — it is a compiled artifact.

## Role

You are the orchestrator. You own the workflow. You delegate domain tasks to agents via JSON contracts — never execute domain work directly.

**Forbidden:** Direct `gh` commands for issue operations. All domain work goes through agents.

**Agent boundaries:**

| Agent | Type | Domain | Phases |
|-------|------|--------|--------|
| `project-orchestrator` | domain | Issues: create GitHub Issue with defect label | Execution |

## Pre-flight

Execute these checks before any domain work:

| Check | Constraint | Action on Failure |
|-------|-----------|-------------------|
| Resolve `stm_base` from `.meridian/core/config.yaml` | — | Hard halt |
| Required fields present | C1 | Hard halt — report missing field(s) to caller |
| Severity is valid | C2 | Hard halt — report invalid value and allowed set |
| gh CLI available | C3 | Soft — set `gh_available=false`, continue to fallback path |

```bash
# Resolve STM base path
config_path=".meridian/core/config.yaml"
if [ ! -f "$config_path" ]; then
  echo "HALT: Config not found at $config_path"
  exit 1
fi
stm_base=$(grep '^\s*base-path:' "$config_path" | head -1 | awk '{print $2}')

# C1: Validate required fields
# Required: severity, affected_component, reported_from, problem, expected_behavior, impact
# The play must receive these as input from the caller.
# If any is missing → HALT with error naming the missing field(s).

# C2: Validate severity
# Must be one of: Critical, High, Medium, Low (case-sensitive)
# If invalid → HALT with error naming the invalid value and allowed values.

# C3: Check gh CLI access
gh_available=true
if ! gh auth status >/dev/null 2>&1; then
  gh_available=false
fi

# C4: Auto-generate date (never caller-provided)
defect_date=$(date +%Y-%m-%d)
```

**Issue detection:** This play does not require a branch-based issue number. The caller provides the defect content directly. Use a synthetic issue identifier `defect-report` for STM paths.

**Resume check:** If `{stm_base}/defect-report/status/report-issue.json` exists, resume — skip completed steps, reset any `in_progress` to pending, continue from first incomplete.

## Workflow

### Phase: Execution

**Step 1 — Format Defect Content**
Owner: play
Depends on: pre-flight

Construct the GitHub Issue title and body from validated input:

**Title format (C8):**
```
[DEF][{Severity}] {Short problem description}
```

**Body format (C5, C6):**
```markdown
| Field | Value |
|-------|-------|
| **Severity** | {severity} |
| **Affected Component** | {affected_component} |
| **Reported From** | {reported_from} |
| **Date** | {defect_date} |

### Problem

{problem}

{if evidence provided (C6):}
### Evidence

{evidence}

{if root_cause provided (C6):}
### Root Cause

{root_cause}

{if specific_issues provided (C6):}
### Specific Issues

{specific_issues}

### Expected Behavior

{expected_behavior}

### Impact

{impact}
```

Optional sections (Evidence, Root cause, Specific issues) are included ONLY when the caller provides them. They MUST NOT appear as empty sections (C6).

Write formatted content to STM:
- Title → `{stm_base}/defect-report/evidence/report-issue/title.txt`
- Body → `{stm_base}/defect-report/evidence/report-issue/body.md`

**Step 1 Eval:**
- **SE-6 (C5):** The issue body uses structured markdown — severity and metadata as a front table, problem/evidence/expected/impact as labeled sections.
- **SE-7 (C6):** Optional fields appear only when provided. No empty optional sections.
- **SE-8 (C8):** The title follows the pattern `[DEF][Severity] Short problem description`.

---

**Step 2 — File GitHub Issue (primary path)**
Owner: `project-orchestrator`
Depends on: Step 1
Condition: `gh_available == true`

```json
{
  "intent_path": "core/components/plays/report-issue/reference/intent.yaml",
  "stm_base": "{stm_base}",
  "stm": {
    "input": {
      "title": "{stm_base}/defect-report/evidence/report-issue/title.txt",
      "body": "{stm_base}/defect-report/evidence/report-issue/body.md"
    },
    "output": {
      "result": "{stm_base}/defect-report/evidence/report-issue/issue-result.yaml"
    }
  },
  "task_id": "file-github-issue",
  "config": {
    "platform": "github",
    "repo": "kapilvirenahuja/meridian-os"
  }
}
```

Agent reads title and body from STM, invokes `manage-issue` skill with `action: create`, `labels: "defect"` (C9). If the `defect` label does not exist, create it (C9).

**Step 2 Eval:**
- **SE-9 (C9):** The created GitHub Issue has the label "defect" applied.
- **SE-5 (F5):** The caller receives confirmation containing the created issue URL.

On agent failure (gh auth, network, permissions):
- Agent returns structured failure via JSON contract with `status: "failed"`.
- Play proceeds to Step 3 (fallback path).

---

**Step 3 — Create Local Fallback File (fallback path)**
Owner: play
Depends on: Step 2 failed OR `gh_available == false`
Condition: GitHub Issue was NOT successfully created

Write a portable markdown file containing the full defect report to:
`{stm_base}/defect-report/evidence/report-issue/fallback-defect-{defect_date}.md`

The fallback file contains:
1. The full defect content (title + body from Step 1)
2. A header noting this is an unfiled defect

Tell the caller (C7):
1. **What failed** — the specific error (auth failure, network timeout, permission denied, etc.)
2. **Where the file is** — the full path to the fallback markdown file
3. **How to share it** — "You can: (a) paste the content into a new issue at https://github.com/kapilvirenahuja/meridian-os/issues/new, (b) email it to the maintainer, or (c) share it on Slack."

**Step 3 Eval:**
- **SE-3 (F3/C7):** A local markdown defect file is created and the caller is told what failed and how to share it.
- **SE-4 (F4):** The caller receives a message identifying the specific failure and sharing instructions.

---

### Phase: Approval

**Step 4 — Confirm to Caller**
Owner: play
Depends on: Step 2 (success) or Step 3 (fallback)

**If GitHub Issue created (Step 2 succeeded):**
```markdown
## Defect Filed

**Issue:** {issue_url}
**Title:** {title}
**Labels:** defect

The defect has been filed on kapilvirenahuja/meridian-os.
```

**If fallback file created (Step 3 executed):**
```markdown
## Defect Saved Locally

**File:** {fallback_file_path}
**Reason:** {failure_description}

The defect could not be filed on GitHub. A portable markdown file has been created.

**To share:**
- Paste into a new issue: https://github.com/kapilvirenahuja/meridian-os/issues/new
- Email the file to the maintainer
- Share on Slack
```

**Step 4 Eval:**
- **SE-5 (F5):** The play confirms either the issue URL or the fallback file path to the caller. Does not silently succeed.

---

### Phase: Scenario Validation

**Step 5 — Scenario Evals**
Owner: play
Depends on: Step 4

- **SCE-1 (S1 — Play consumer agent):** A GitHub Issue is created on kapilvirenahuja/meridian-os with the defect label, structured body containing all provided fields including optional Evidence and Root cause, and the issue URL is returned to the caller.
- **SCE-2 (S2 — Human developer):** A GitHub Issue is created with only the required field sections. No empty optional sections appear. The issue URL is returned.
- **SCE-3 (S3 — Agent with invalid severity):** The play rejects with a clear error identifying the invalid severity and allowed values. No issue or fallback file is produced.
- **SCE-4 (S4 — Agent with missing field):** The play rejects with a clear error identifying the missing required field. No issue or fallback file is produced.
- **SCE-5 (S5 — Developer with gh failure):** A local markdown file is created with full defect content. The caller is told what failed and how to share the file.

---

### Phase: Evidence & Close

**Step 6 — Write Evidence**
Owner: play
Depends on: Step 5

Write evidence to `{stm_base}/defect-report/evidence/report-issue/{YYYYMMDD-HHMMSS}.md`.

Present summary to user. Non-blocking on failure.

## Pause and Resume

Steps are in execution order — run top to bottom.

**Issue detection:** This play uses a synthetic identifier `defect-report` for STM paths since it is not branch-based.

**Status file:** `{stm_base}/defect-report/status/report-issue.json`

```json
{
  "play": "meridian:report-issue",
  "issue": "defect-report",
  "started_at": "2026-04-11T12:00:00Z",
  "tasks": {
    "format-defect": { "status": "completed", "completed_at": "..." },
    "file-github-issue": { "status": "in_progress", "started_at": "..." },
    "create-fallback": { "status": "pending" },
    "confirm-to-caller": { "status": "pending" }
  }
}
```

**Executor loop:**
```
check for status file at {stm_base}/defect-report/status/report-issue.json

for each step in compiled order:
  if status file shows step "completed" → skip
  if status file shows step "in_progress" → reset to pending
  mark step "in_progress" in status file
  execute step
  mark step "completed" in status file
```

**Fresh start:** No status file → execute all steps, create status file on first step.
**Resume:** Status file exists → skip completed, continue from first incomplete.
**Pause:** User terminates session. Status file reflects last completed step.

## Compilation Metadata

| Field | Value |
|-------|-------|
| intent_hash | sha256:48cbe511a9d02a40a36f780d25776e736639fd24fa3f60d37098f1b581f99991 |
| compiled_by | create-play |
| compiled_at | 2026-04-11T11:56:32Z |
| workflow_structure | B |
| domain_agents | 1 (project-orchestrator) |
| utility_agents | 0 |
| step_evals | 9 (SE-1 through SE-9) |
| scenario_evals | 5 (SCE-1 through SCE-5) |
