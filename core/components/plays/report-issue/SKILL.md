---
name: report-issue
description: >
  DEPRECATED — use garura:capture (/capture). This alias will be
  removed in the next major release. Forwarding play.
user-invokable: true
alias: true
---
# report-issue (DEPRECATED)

This play is a deprecated alias for `garura:capture`.

Invoke `garura:capture` with the same arguments you would have passed here.
Append the following to any confirmation returned:

> [DEPRECATED] `/report-issue` is deprecated — use `/capture` instead.
> This alias will be removed in the next major release.

Do NOT edit this file with `/create-play --build` — it has no `reference/intent.yaml` by design.
